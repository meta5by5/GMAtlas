// buildInfo.js — the phase/version label and changelog shown in the Settings
// drawer's "Build" panel. Hand-maintained: bump PHASE/VERSION and prepend a
// note each time a phase ships, so a returning GM can see at a glance what
// changed without digging through git history.

export const BUILD = {
  phase: '14',
  version: '0.20.17',
  label: 'Phase 14: Storyboard re-layout — Composer/Navigator/Advisor (Steps 1–5 built; Steps 6–7 open)',
  notes: [
    '2026-08-17 Phase 14 Google Drive-linked documents, and the Reference Library download-prompt bug actually fixed this time (direct follow-up requests): the previous turn\'s fetch-and-cache fix for Reference Library PDFs never actually worked in the field — re-checked with a live cross-origin header probe (curl alone doesn\'t enforce CORS the way a browser does, which is why the earlier verification missed it) and confirmed GitHub\'s release-asset host sends no Access-Control-Allow-Origin header at all, so the browser\'s fetch() was being silently blocked by CORS every time, falling straight through to the exact original broken behavior. (1) Design decision, confirmed via direct question before touching anything: "picking a file located in GDrive" means pasting a Drive "Anyone with the link" share URL (Drive\'s own Share dialog, no app-side account connection) into a new inline-prompt field — not a live OAuth-based "browse my Drive" picker, which would\'ve meant registering a Google Cloud OAuth client/API key as a real new external dependency. (2) documents.js gained a third library-entry kind, \'drive\' — addDocument stores ONLY driveFileId (extracted from the pasted link via the new extractDriveFileId, validated against every share-link shape Drive\'s dialog actually produces — file/d/ID, ?id=ID, uc?id=ID) and the raw driveUrl for reference; never any file bytes — "the URL is saved... backed up and moved to other machines... pulls the file to the local cache each time," direct quote, satisfied structurally: a campaign export stays small regardless of how large the linked PDF is, and every machine re-resolves the same file id fresh. resolveDocumentTab resolves a drive entry to Drive\'s own direct-download endpoint (driveDownloadUrl) tagged with driveFileId for the viewer to key off. (3) shell.js\'s Reference-Library-only remote-doc handling (renderRemoteDocFrame, generalized from the previous turn\'s refDocBlobUrls/etc., renamed remoteDocBlobUrls/etc.) now serves BOTH sources with a two-tier strategy: tier 1 (unchanged) fetches the file once and caches it via the Cache API, handing the iframe a corrected-MIME blob: URL — genuine local caching, when the source actually grants this app\'s origin CORS access. Tier 2 (the actual fix, both for the new Drive case AND the still-broken GitHub case) is a per-source EMBEDDABLE VIEWER URL as the fallback — critically, NEVER the raw download URL again, since that\'s the literal thing that forces a download; a normal cross-origin iframe navigation to an embed-shaped URL isn\'t a fetch, so CORS doesn\'t apply to it at all. Drive gets its own OFFICIAL, documented embed shape (drivePreviewUrl, .../preview) since Drive explicitly designs that URL to be framed by third-party pages; GitHub Release assets have no equivalent of their own, so a Reference Library doc falls back to Google\'s own long-standing (if undocumented) generic PDF embed viewer instead (gviewEmbedUrl, docs.google.com/gview) — reachability and frameability of both confirmed via live header checks (200 OK, no X-Frame-Options/frame-ancestors restriction) before relying on either. (4) Documents drawer gained a "☁ Add from Google Drive" button (drawers/index.js) opening the same generic single-field inline-prompt mechanism every other free-text entry point in this app already uses (never window.prompt) — invalid/non-Drive input is rejected with a toast rather than silently stored. A Drive-linked entry\'s row renders like an uploaded file (clickable, opens in the viewer) rather than like a text note, with its own "☁ Google Drive" badge instead of a byte-size badge; findDocumentTabByTitle/search.js\'s Universal Search both updated so @mentioning or searching for a Drive-linked doc\'s title resolves and opens it the same as any other real document. Verified: all 464 domain tests (4 new — extractDriveFileId\'s share-link-shape parsing and rejection of a non-Drive URL, the three URL-builder functions\' exact output shapes, addDocument(kind:\'drive\')/resolveDocumentTab\'s no-bytes-stored/correct-src-with-page-anchor round trip, and findDocumentTabByTitle treating a Drive entry as openable), clean build, and three jsdom smoke tests mounting the real built bundle — one driving the ENTIRE add-from-Drive-link flow through the real UI (button click → inline prompt → paste a real share-link shape → Enter → entry created with the correct extracted file id and zero stored bytes → open in viewer → "Loading…" shown immediately → mocked fetch/cache/blob resolves → iframe visible), one confirming a fetch failure falls back to Drive\'s own /preview embed and explicitly NOT the raw download URL, and one reusing the established reversible local-file-rename technique (a real Reference Library PDF renamed aside so its manifest entry genuinely resolves to the GitHub URL, restored and rebuilt again afterward, confirmed via a clean git diff on assets/docs/) to confirm the Reference Library fallback now lands on the gview embed instead of the raw GitHub URL that caused the original bug.',
    '2026-08-16 Phase 14 fix: Reference Library PDFs forced a download instead of viewing inline on phone (a real reported bug, confirmed which docs via direct question — Reference Library specifically, not user uploads). Root cause, diagnosed via a live curl -I -L against the actual GitHub Release redirect: the signed release-assets.githubusercontent.com response carries Content-Type: application/octet-stream plus a forced Content-Disposition: attachment, a combination that makes most browsers download rather than render a PDF — and mobile Chrome has no inline-iframe PDF fallback the way desktop does, so it\'s the one platform where this was unavoidable rather than just occasionally ugly. User-uploaded docs were unaffected (already pure data: URLs, no network fetch, no download-forcing attribute anywhere in the code — confirmed via a full audit before touching anything). Fixed by no longer pointing the iframe straight at the GitHub URL for a Reference Library doc that isn\'t bundled locally: shell.js\'s new loadRefDocBlobUrl fetches it once, caches the raw response via the Cache API under its own gmatlas-refdocs-v1 cache ("save it to the local cache like the rest of app data," direct quote — the same mechanism sw.js already uses for the app shell, just a separate cache name), and hands the iframe a blob: URL built with the corrected application/pdf MIME type — blob: URLs have no Content-Disposition concept at all, so GitHub\'s bad header can\'t apply regardless of platform. A fetch/CORS/offline failure falls back to the original direct-URL behavior (no worse than before the fix) rather than leaving the GM with nothing; a "Loading…" placeholder covers the brief window before the first fetch resolves, and re-opening the same doc later reuses the resolved blob URL with no second network round trip. Locally-bundled Reference Library paths and user-uploaded docs are both untouched — this is scoped to exactly the one broken case. Caught and fixed a real second bug WHILE building this: sw.js\'s own activate handler deleted every Cache Storage entry it didn\'t recognize by name on every service-worker update, which would have silently wiped the new doc cache (a rebuilt bundle bumps nothing about ITS name) the moment the app next updated — fixed by keying activate\'s cleanup off a KEEP_CACHES list (both the shell cache and the new doc cache) instead of a single-name equality check. Verified: all 460 domain tests (unaffected — this is a browser-fetch/cache mechanism with no domain-layer surface), clean build, and two temporary jsdom smoke tests mounting the real built bundle against an ACTUAL Reference Library manifest entry (its real local PDF renamed aside and the build re-run so the entry genuinely resolved to the GitHub URL for the test, then restored and rebuilt again afterward, confirmed via a clean git diff on assets/docs/ — the same reversible technique already established earlier for this exact manifest) — one confirming the full success path (loading placeholder shown immediately, fetch called exactly once, the response cached via cache.put, the blob handed to createObjectURL carries the corrected application/pdf type, the iframe becomes visible, and reopening the same doc reuses the cached blob URL with zero additional fetch calls), the other confirming a mocked fetch failure correctly falls back to the direct GitHub URL rather than leaving the viewer stuck. sw.js\'s own activate-handler fix was verified by direct code review only — jsdom doesn\'t run real service workers, the same known gap already noted for every other browser-only mechanic this session.',
    '2026-08-16 Phase 14 WHERE System row: name moved off the thumbnail, thumbnail grows to fill the info block\'s height (direct follow-up request): the System\'s own name no longer renders underneath its thumbnail (the standard entityThumb layout every other thumbnail in this app uses) — it\'s now the FIRST line in the info block to the thumbnail\'s right, above the Star name, with Zone+Hex still the third line below that. The System row no longer reuses entityThumb for this one case (a bespoke inline photo/circle render, workspace/index.js, since entityThumb\'s shared "name always renders below" behavior is load-bearing everywhere else it\'s used and shouldn\'t change). The thumbnail itself is bigger now too — "allow the thumbnail to be bigger to fill the vertical height of the box containing the system, star and zone names": .location-hierarchy-row switched from align-items:center to align-items:stretch so its thumbnail wrapper (.location-hierarchy-thumb, new) grows to match the 3-line info block\'s own natural height, and the photo inside sizes itself to fill THAT via height:100% + aspect-ratio:1/1 (computing a matching width automatically, staying visually circular via the existing border-radius) — so it scales with however tall the text block ends up being rather than staying the small fixed NPC-thumbnail size; the row\'s 🔗 button opts back out with align-self:center so it doesn\'t stretch into an odd tall rectangle alongside them. Verified: all 460 domain tests (unaffected — pure render-layer), clean build, and a 9-check jsdom smoke test mounting the real built bundle — confirmed the System name renders as its own line, positioned above the Star line in DOM order, the thumbnail no longer shows a name underneath while its click-to-open behavior is unchanged, and the align-items:stretch/height:100% CSS rules are in place (real pixel-level stretch itself isn\'t verifiable without a layout engine, same known gap noted for every other layout-only change this session — based on direct box-model review, not a rendered screenshot).',
    '2026-08-16 Phase 14 clear "✕" for oracle fields in thumbnail-arrow popups (direct follow-up request — "e.g., Disposition"): oracleFieldRow (workspace/index.js, the shared label+🎲-roll+editable-text row behind both an NPC scene detail\'s 5 fields — Disposition/Motivation/Threat Rank/Challenges/Opportunities — and Location Details\' Sights/Smells/Sounds) gained a fourth, optional "✕" button that blanks the field the same way clearing the input by hand and blurring already would, only rendered once there\'s actual text to clear. Two new shell.js handlers — data-scene-npc-field-clear and data-location-sensory-clear — call the exact same editNpcSceneField/editLocationSensoryField (session.js) an empty-string manual edit would, so the "remembered edit vs. still-linked-to-a-roll" oracles.overrides bookkeeping those functions already handle stays correct with no new logic. .oracle-field-row\'s grid gained a 4th auto-sized track for the button; the other 3 tracks are unaffected when it\'s absent. Verified: all 460 domain tests (unaffected — a pure UI/render-layer addition over an already-tested pair of domain functions), clean build, and an 8-check jsdom smoke test mounting the real built bundle — confirmed no "✕" renders on an empty field, rolling Disposition makes it appear, clicking it blanks the field AND makes the "✕" disappear again, and the identical round trip for Location Details\' Sights field.',
    '2026-08-16 Phase 14 Advisor/drawer alignment fix, Factions active nearby parity with NPCs, Composer\'s pencil-styled toolbar toggle, and resizeable textboxes (direct follow-up requests, one batch): (1) The Advisor panel\'s left edge is now guaranteed to align with an open drawer\'s (Cast or any other) — a real reported misalignment. Root cause: two separately-maintained 420px tokens (tokens.css\'s --copilot-w for the Advisor\'s grid column, a --drawer-w shell.js set inline on .mc-drawer per render) happened to agree by coincidence, not by construction. Consolidated into ONE token, --drawer-w, declared once at :root (tokens.css) and read by both .cockpit\'s grid-template-columns AND .mc-drawer\'s own width (plus the doc-viewer-narrowing calc, shell.js) — --copilot-w is retired outright; shell.js no longer sets --drawer-w per-render at all (nothing left to override). (2) "Factions active nearby" thumbnails gained the same ▸/▾ expand + "✕" NPCs\' own thumbnails already have (direct follow-up request — parity). Since most factions here are DERIVED (region presence, an Actor\'s own faction membership), not a stored list, "✕" is a new scene-scoped dismiss (scenes.js\'s dismissedFactionIds/addSceneDismissedFaction) that hides one from THIS scene\'s view without touching whatever real relationship/presence made it appear — "curated convenience, not a restriction," the block\'s own pre-existing framing. ▸/▾ reveals a new factionDetailBody (workspace/index.js) — read-only Agenda/Fear/Need/HQ snippets, real existing entity fields, no new schema. The old inline "+ faction operating here" <select> is now a header "+" icon (direct follow-up request — "work like the (+) add buttons for other sections") opening the shared entityPicker overlay (a new entityType, \'where-faction-link\') — picking a faction still creates the same real located_at relationship, and additionally clears any stale dismissal so a deliberately re-added faction isn\'t immediately hidden again; a dismissed faction becomes reachable through this same "+" again since it drops out of the rendered "active" set. (3) The rich-text formatting toolbar\'s collapse arrow moves to a right-aligned pencil (✏️) on the SAME header row as the field\'s own label — Composer\'s Situation, WHY/HOW\'s Focus, and WHERE\'s Regional faction activity specifically (confirmed scope via direct question — the other 12 richToolbarHTML call sites app-wide, several with no adjacent header row at all, keep their existing ▸/▾ unchanged). mentionEditor.js gained richToolbarToggleHTML (the pencil alone, for a caller to place in its own header) and richToolbarHTML gained an `includeToggle` option (default true, preserving every untouched call site\'s exact prior output) so the 4 Composer sites can suppress the toolbar\'s own built-in arrow and render just the pencil instead — same data-rich-toolbar-toggle attribute/shell.js handler either way, purely a markup relocation. (4) "Allow all textboxes to be resizeable vertically" — every .mention-editor rich field (previously only Guide\'s own editor had a resize handle) gained one via the base class (the inline-only .mention-text variant explicitly opts back out, since it has no visible box chrome to drag); Latest Scene\'s auto-growing textareas (Opening/Driver/Clue/...) dropped their old ~4-row max-height cap-then-internal-scroll in favor of unbounded auto-grow PLUS a manual resize handle, with autoGrowSceneField changed to never shrink below whatever height is currently set (a prior manual resize included) so typing and dragging don\'t fight each other. Verified: all 460 domain tests (unaffected — every change this batch is UI/render-layer plus two new/renamed scene fields on an already-tested code path), clean build, and three jsdom smoke tests mounting the real built bundle — one confirming --drawer-w resolves correctly at :root and --copilot-w is fully gone from the stylesheets, Factions active nearby\'s dismiss/expand/re-link-clears-dismissal round trip, and the Situation field\'s pencil toggle sitting in its header row (not duplicated in the toolbar\'s own row) correctly expanding the formatting buttons on click; and one confirming the resize CSS lands on the right selectors (and NOT on the inline mention-text variant), the old scene-field max-height cap is gone, and a simulated manual resize survives a subsequent input event instead of snapping back to content-fit size.',
    '2026-08-16 Phase 14 scroll-position fix, Suggested-oracles-for-Situation routing, Nearby locations click-to-add, and Advisor Location proximity (direct follow-up requests, one batch): (1) Editing any field re-renders the whole shell (every mutation goes through store.update()) — a real reported bug, disorienting: this used to snap the page/panel back to the top mid-edit. render() now captures/restores window.scrollX/scrollY as a blanket top-level safety net, and — the actual root cause — .mc-workspace (Composer/Navigator)\'s innerHTML replace gained the same scrollTop capture/restore the drawer body already had (replaceBodyPreservingScroll, prior work) via a new replaceWorkspacePreservingScroll: each card scrolls independently via its own .workspace-card-body, matched up by index since renderWorkspace() always emits exactly two in the same order. (2) WHAT\'s "Suggest oracles" entries now carry a target:\'situation\' tag (pushSuggestedOracleEntry\'s new third param) — rolling one of them (data-suggest-oracle-roll, now reading a companion data-suggest-oracle-target) calls rollOracle with toJournal:false and appends the result straight into context.what.situation (editContextText) instead of logging a Journal entry — "the scene is still under development," direct quote; WHERE\'s Regional faction activity 💡 pushes with no target, keeping the ordinary journal-logging roll every other oracle roll in this app already has. (3) WHERE\'s "Nearby locations" chips now ADD the clicked sibling straight to Location details (new data-scene-location-add, addSceneLocation\'s existing one-per-tag dedup still applies) instead of opening its Entity Editor — once added, the block\'s own already-in-scene filter drops it from view on the next render, so no separate "already clicked" tracking is needed. (4) New: clicking any Location\'s thumbnail (data-open-entity, entities.js\'s new proximityToLocation — a breadth-first walk of the FULL containment chain, not just one hop, filtered to real Location children since an NPC\'s own located_at edge auto-mirrors a same-shaped `contains` edge back onto its own location that would otherwise leak into the walk) populates a new "Located at X" card in the Advisor listing every NPC located_at that Location OR any of its sub-locations at any depth ("iterative proximity," direct quote) plus every sub-location itself. Click-to-add, confirmed over drag-and-drop after asking directly (building genuine cross-panel drag for two new payload types was meaningfully larger/riskier than every other change in this batch combined, with no existing precedent to extend) — an NPC routes through the same #character-tag check Introduce NPC\'s create path already uses, a sub-location adds straight to Location details; the list itself is recomputed fresh on every render (not a frozen snapshot) and filters out anything already added to the scene, so it stays accurate as the GM works through it. Verified: all 460 domain tests (2 new — proximityToLocation\'s full-chain walk/NPC-collection/self-exclusion, including the auto-mirrored-contains-edge regression the first version of the function actually hit and this test now guards against), clean build, and a 16-check jsdom smoke test mounting the real built bundle — confirming a Composer scrollTop survives a full re-render triggered by an unrelated Intent edit, a Situation-targeted roll lands in context.what.situation with zero Journal growth, a Nearby-locations chip both adds to the scene AND disappears from its own list afterward, and Location proximity correctly finds an NPC two hops down through a sub-location (not just a direct child), offers the sub-location itself, and both add-buttons work and self-remove once used.',
    '2026-08-16 Phase 14 Location Story merged into Location details, Nearby locations relocated+collapsed, "Regional faction activity" 💡, and the Advisor\'s Suggested-oracles history stack (direct follow-up requests, one batch): (1) "Location Story" is no longer its own WHERE section — its rich-text field (loc.locationStory, unchanged, still one per current-scene Location) is merged into locationDetailBody, the exact same expand-arrow dropdown that already shows Location type/Sights/Smells/Sounds for a Location details thumbnail — "merge the... text field as part of the dropdown opened by the thumbnail arrow," direct quote. Relabeled from "{name} — Location Story" to a proper field with a "Regional faction activity" header and a right-aligned 💡 (data-suggest-faction-oracles) that draws a fixed #faction/#agenda Oracle set (session.js\'s new drawFactionActivityOracles, sharing a new drawOraclesForTags core with WHAT\'s drawSuggestedOracles) and pushes it into the Advisor. Only reachable for Location details entries now (whichever have the expand arrow) — the System/Star row never had one, so a #system/#star-tagged Location\'s own Regional faction activity field is no longer surfaced anywhere in WHERE, an accepted consequence of "the dropdown opened by the thumbnail arrow" being scoped to Location details specifically. (2) Nearby locations moves to WHERE\'s last section slot (where Location Story used to sit) and is collapsed by default (collapsibleThumbGroup gained a defaultCollapsed param — the shared ui.collapsedActorGroups Set is read inverted when set, so the one existing toggle handler still works unmodified either direction) — "hide the list... by default," direct quote. It also now excludes any sibling already present in the current scene (System/Star/Location details, getCurrentWhereLocations) — "available entities that have not already been added to the scene," direct quote — so it reads as "what else could be added" rather than duplicating Location details\' own list. (3) The Advisor\'s "Suggested oracles" card is now a history stack instead of a single overwritten slot — "whenever anything is added to the Advisor, place it as a new entry in a box at the top... move previous entries down. Show three entries plus collapsed boxes for three before that," direct quote. A new shared ephemeral ui.suggestedOracleEntries (shell.js, replacing the old single-array suggestedOraclesDraw) holds up to 6 `{id, label, paths}` entries, newest first; every draw trigger — WHAT\'s "Suggest oracles" button, its existing Intent/Latest-Scene auto-trigger, and the new Regional faction activity 💡 — pushes a new entry via a shared pushSuggestedOracleEntry helper rather than replacing the array. copilotPanel.js renders the first 3 as open boxes and the next 3 as native `<details>` elements (no new JS toggle state needed) so older entries stay available to expand and look up without competing for space by default; anything past 6 is dropped. Verified: all 459 domain tests (1 new — drawFactionActivityOracles\'s fixed #faction/#agenda pool, independent of Intent/Latest Scene), clean build, and three jsdom smoke tests mounting the real built bundle — one confirming Nearby locations is WHERE\'s last group, starts collapsed, and correctly excludes an already-in-scene sibling while still offering one that isn\'t; one confirming the merged field\'s "Regional faction activity" header/💡 render inside Old Town\'s dropdown with the old "Location Story" label and top-level standalone field both gone, clicking 💡 pushes a location-labeled entry into the Advisor, and six pushes settle into exactly 3 open + 3 collapsed `<details>` entries; and one confirming WHAT\'s own "Suggest oracles" button still pushes correctly into the same shared stack rather than a now-removed separate slot.',
    '2026-08-16 Phase 14 WHERE Location details consolidated to one "+", and WHO\'s add-actions moved to icons (direct follow-up requests, two batches): (1) Location details\' "Pick location" text chip and Colony-Base/District\'s two dedicated role-specific "🏘＋"/"🏙＋" icon buttons are replaced by ONE icon "+" — "just one (+) like the approach for NPCs," direct quote — reusing the exact same data-entity-picker-open="location-current" flow every entry already went through. Colony-Base/District\'s own colonyBaseId/districtId fields and their dedicated picker mode (entityPicker entityType \'location\', shell.js) are retired as a result — left declared-but-inert on the entity per migration rule 5, no longer read or written by this UI — since the pre-existing generic tag-based one-per-type dedup (addSceneLocation, scenes.js, from the prior batch) already covers #colony/#district/#site/whatever equally well through the one remaining button. Every Location details thumbnail also gained a small top label (entityThumb\'s new topLabel param, locationTypeTopLabel()) reading "District" or "Site" — flush against the top of the circle, mirroring the name label\'s own look but above instead of below — "for quick clarification," shown only for those two tags; nothing else gets one. (2) WHO\'s three add-actions — Introduce NPC/New NPC/New Faction — move from a text-chip row inside the section body to three icons (🔎＋/🧑＋/🏛＋) right-aligned on the "WHO is here" header row itself (dashboardSection gained an optional headerExtra param, threaded through a shared .entity-chip-row wrapper alongside the existing help-toggle/collapsed-summary so the space-between layout still reads as "toggle left, everything else right") — reachable even while WHO is collapsed. Introduce NPC\'s own behavior is unchanged (still the free-text/@mention inline prompt, "select from list and/or @ mentions" per direct quote — typing an existing NPC\'s name adds it, an unrecognized name creates one); New NPC/New Faction now open a fresh blank entity straight into the Entity Editor (openDrawerTab + focusInspectorNameNextRender + setActiveEntity, the same create-then-open shape WHERE\'s own "New Location" uses) instead of a silent background create-and-toast — "start new Entity Editors with the appropriate type," direct quote. Verified: all 458 domain tests (unaffected — both changes are UI/render-layer only, no domain function signatures moved beyond the prior batch\'s already-tested addSceneLocation), clean build, and two jsdom smoke tests mounting the real built bundle — one confirming Location details renders exactly one "add" control (icon-only "+", not "Pick location" text or the retired 🏘＋/🏙＋ pair), no leftover Colony-Base/District compound-value picker trigger remains, and a #district/#site entry each show the correct top label ("District"/"Site"); the other confirming all three WHO icons sit on the section header row and stay clickable while WHO is collapsed, the old text-chip row inside the body is gone, New NPC/New Faction each create the right entity type AND land it as the active Entity Editor tab, and Introduce NPC\'s inline prompt still opens unchanged.',
    '2026-08-16 Phase 14 WHERE System picker/create + one-per-type Location details, and WHAT\'s "Suggest oracles" (direct follow-up requests, two batches): (1) WHERE\'s System row is reachable from a totally empty scene now — it renders unconditionally (previously it only appeared once a Location Details anchor already existed) so its blank "＋ Select System" state is always the entry point. That control ("New Location," data-where-add-location) no longer jumps straight to the Entity Editor\'s Relationships section — it first lists existing #system-tagged Locations to pick from (a new entityPicker entityType, \'system\'); only when NONE exist does it fall back to a window.confirm ("create one?") that creates a fresh #system-tagged entity, wires it up as the scene\'s System immediately, and opens the Entity Editor on it. The underlying mechanism is still a pure relationship-graph read (getSystemForLocation, unchanged, per the prior batch\'s "just use the Relationships" decision) — a new scenes.js function, setSceneSystem, just makes SETTING it up nicer: with no Location Details anchor yet, the picked System becomes the anchor directly (self-resolving via its own #system tag, no relationship needed); with a real anchor (a site/district), it gets a located_at edge to the System, replacing any prior one so there\'s still only ever one parent; an anchor that was only ever a self-#system-tagged stand-in from the first case gets swapped for a new pick directly, never linked to itself. (2) Location details\' generic "+" is now a labeled "Pick location" button, and its picker excludes #star/#system-tagged Locations (those belong to the System row above, not here) — "filter on other tags than #star or #system," direct quote. Picking now enforces ONE entity per tag-type (addSceneLocation, scenes.js) — picking a second #district swaps out the first instead of accumulating both; an untagged pick, or one sharing no tag with anything already present, just adds alongside — entirely tag-driven, not a hardcoded type enum, so it works for whatever vocabulary (#district, #site, ...) the GM\'s own tags happen to use. Colony-Base/District keep their own separate dedicated "🏘＋"/"🏙＋" pickers per direct confirmation ("keep them separate, but selecting from the pickers should add the thumbnails and have the same end result") — unchanged, already rendering into the same entityThumb list Location details uses. (3) WHAT: the read-only combined-narrative block (.scene-text, a <pre> duplicating what the Opening/Driver/Clue/Complication/Decision point/Likely consequence/Current thread fields already show individually) is removed from Latest Scene — the collapsible per-field editors (sceneField()) are untouched and are now the only way to read/edit a scene\'s narrative here; scene.text itself still exists and still feeds the Journal/recap, just isn\'t rendered a second time in Composer. A new "🔮 Suggest oracles" button sits between Intent and Latest Scene (direct placement request) — clicking it (data-suggest-oracles) draws a capped, random-without-replacement set of Oracle tables (session.js\'s new drawSuggestedOracles) from whichever tables carry a tag matching either the current Intent (a new INTENT_ORACLE_TAGS map, entityFieldOracleLinks.js, reusing the SAME locked tag vocabulary — character/leadership/secret/fear/setting/hook/agenda/faction/trade/discovery — field 🔮 links already rely on, so every entry is already guaranteed a real seeded table) or one of the Latest Scene\'s own populated fields (oracleLinkTagsFor(\'scene\', field), the exact same per-field links the 🔮 icons already use — no new content or NLP-guessing, just reusing what\'s already linked). The result renders as a new "Suggested oracles" card in the Advisor (copilotPanel.js, alongside Story Options/Suggest a Lens) — a cross-panel action (the trigger lives on WHAT, the output in the Advisor) matching how every other suggestion control in this app already separates "where the button lives" from "where results are read." Per direct request ("triggered every time the Intent or Latest Scene changes"), the draw also re-runs automatically — shell.js\'s onChange handler re-invokes it right after Intent\'s <select> or any of the 5 linked scene fields (opening/driver/clue/complication/consequence — decisionPoint/situationLine have no oracle link and don\'t trigger it) actually commits a change, not on every keystroke. Verified: all 458 domain tests (3 new — setSceneSystem\'s three anchor cases [bootstrap/real-anchor-relink/self-tagged-stand-in-swap] plus a same-target-replaced-not-duplicated check, addSceneLocation\'s one-per-tag dedup, and drawSuggestedOracles\'s Intent-only vs. Intent+populated-field vs. field-cleared-again pool growth/shrinkage), clean build, and two jsdom smoke tests mounting the real built bundle — one confirming the System row renders even with zero scene locations, the New Location button opens a picker when a #system entity already exists (not the create-prompt) and correctly falls back to window.confirm + auto-select when none do, Pick location excludes #system/#star and enforces one #district at a time, and the other confirming the .scene-text block is gone while the field toggles remain, the Suggest oracles button\'s Advisor card populates and rolls correctly, and changing Intent alone (no button click) surfaces a newly-relevant table without any other interaction. Also removed three now-dead CSS rules (.scene-text and its two .last-scene-body layout overrides) left over from the removed side-by-side text/fields split.',
    '2026-08-16 Phase 14 WHO/WHERE consolidation + doc-viewer/drawer-width fixes (direct requests, three batched messages): (1) Desktop/tablet doc viewer no longer covers the drawer tab strip while a document is open — drawer.dataset.open\'s desktop/tablet formula dropped its old `docViewerOpenTabs.length === 0` condition entirely (phone/compact is untouched, doc still fully takes over there), and the doc viewer itself now sets `viewer.style.right` to leave room for the 420px drawer panel whenever one is open alongside it, resetting to the CSS default (full width) otherwise. (2) WHERE\'s Current Location banner and Location Details are fully consolidated into one design: a System row (only rendered once a primary current location resolves a System via the existing getSystemForLocation) shows the System as an entityThumb left-aligned with Star name / "(Zone) Hex" in a two-line info block beside it; when no System resolves yet, a blank actor-thumb-photo-empty "＋"/"Select System" thumbnail takes its place, wired to the same data-location-edit-relationships deep-link the resolved row\'s 🔗 already used. Below it, a new "Location details" collapsible group (matching WHO\'s Protagonists/etc. styling via the same new collapsibleThumbGroup, see below) lists every OTHER current-scene location as an entityThumb, each expandable (a new expandAttrHtml param on entityThumb) to locationDetailBody\'s Location type (renamed from "Object type," sector field dropped per direct request — it duplicated the System\'s own Zone) plus Sights/Smells/Sounds. This group carries its own general "+" (any #location entity, not tag-filtered) alongside Colony-Base/District\'s existing role-specific "🏘＋"/"🏙＋" pickers, coexisting rather than merged (Colony-Base/District were never asked to change). Focus\'s free-text field, the Location tags listbox, and the Matching-locations candidate panel are removed outright — whereLocationPicker() deleted, its dead data-where-tag-select/data-insert-where-mention-select handlers removed from shell.js, and its "+ New Location" quick-create preserved as a standalone chip at the top of WHERE. This required getCurrentWhereLocations (factionTurnEngine.js) to stop parsing @mentions out of context.where.summary and instead read the current scene\'s own curated scene.locationIds — mirroring WHO\'s existing Actor-list pattern (new addSceneLocation/removeSceneLocation, scenes.js) — which transparently propagates the same "current location" concept to every other existing consumer (WHO\'s Factions-active-nearby, locationFactionsBlock, locationConflictsBlock, nearbyLocationsBlock, buildStoryOptions, gatherSceneContext, isWitnessedAt, ...) with no other call site touched. A stray leftover second call to the old currentLocationBanner() at the very top of Composer (predating this session, never caught until grepped for during cleanup) is also gone. (3) WHO\'s "Factions active nearby" now also auto-pools whichever real faction each of the current scene\'s Protagonists/Antagonists/Bystanders individually belongs to (getEntityFaction, skipping the synthetic "Unaligned" placeholder), on top of its existing region-presence and manual located_at sources, and renders every result as an entityThumb instead of a plain chip. A new "Assets present" group (scene.assetIds, a fifth GM-curated scene list alongside Bystanders, new addSceneAsset/removeSceneAsset) sits below it with its own "+" picker for any #asset entity. All 5 WHO groups (Protagonists/Antagonists/Bystanders/Factions active nearby/Assets present) plus WHERE\'s new Location details are now independently collapsible via one new shared collapsibleThumbGroup (workspace/index.js) and one shared collapsedActorGroups ephemeral Set (shell.js, data-actor-group-toggle) — per direct request ("without changing the current font style"), the toggle button carries .field-label-static directly for its exact prior type styling, with only a CSS chrome reset (button.actor-group-toggle) stripping the browser\'s own button look. (4) Every drawer panel and the Advisor now share one fixed 420px width (Cast\'s own width) instead of a per-drawer doc.drawers.widths lookup — auditing that lookup during this change found its keys (`entities: 520`, ...) never actually matched any real drawer id (Cast is `cast`, Entity Editor is `entity-detail`), meaning Cast/Entity-detail were already silently on the 420px fallback and the schema\'s widths map was dead; --drawer-w is now hardcoded to 420px everywhere it\'s set (including the new doc-viewer-narrowing calc above) rather than "fixing" the stale keys, and tokens.css\'s --copilot-w changed from 22rem to 26.25rem (=420px) to match. Verified: all 455 domain tests (8 rewritten — every getCurrentWhereLocations-dependent fixture switched from seeding context.where.summary text to seeding scene.locationIds directly), clean build, and a 29-check jsdom smoke test mounting the real built bundle — confirmed all 5 WHO toggles carry .field-label-static, Factions-active-nearby auto-includes a faction via pure NPC membership alone (no location or region presence needed — caught and fixed a real bug where the block returned empty with no current location set, since faction-via-NPC-membership doesn\'t depend on one), Assets-present\'s picker round-trips into scene.assetIds, WHERE has no Focus field/Location-tags/Matching-locations text but keeps "+ New Location", "Location details" renders even in the zero-current-location bootstrap case, adding a location via its general picker makes the System row appear with a working blank-thumbnail state beforehand, the doc viewer\'s right offset narrows by exactly 420px while the drawer stays open, and --drawer-w reads 420px regardless of which drawer is active. Also removed three CSS rules (.location-details, .location-details-summary/-toggle-label/-objecttype) left over from an earlier iteration of the WHERE redesign (individually-collapsible per-row mini-sections with an inline Object Type) that the final collapsibleThumbGroup-based design superseded before shipping — confirmed genuinely unreferenced by any markup before removal.',
    '2026-08-15 Phase 14 System/Star -> pure Relationships (direct follow-up request, reversing part of the prior batch after trying it): "just use the Relationships for setting the choice, but list it in the world profile for any location." System and Star are no longer separately picked/stored fields (starSystemId/starId, retired — left declared-but-inert in the schema per migration rule 5, same posture as the older zone/starSystem fields they themselves replaced) — they\'re now a pure relationship-graph read, three new pure functions in entities.js: getSystemForLocation (a Location IS its own System if #system-tagged, else its own located_at parent counts only if THAT is #system-tagged — one hop, no unbounded walking, same posture as the existing getContainingLocation/isSameDistrict), getStarForLocation (one hop further, chained off the resolved System), and getHexZoneForLocation, which resolves the new Hex/Zone override rule below. Current Location\'s System/Star rows lost their "+"/"-" pickers (setLocationHierarchyField, the whole picker-driven cross-reference/reconciliation/missing-relationship-prompt system from the prior batch, is gone) in favor of a "🔗" that opens the relevant Location\'s own Entity Editor with the "add relationship" type select focused (reusing focusInspectorRelationshipNextRender, repurposed from the retired confirm-prompt flow it came from) — no confirm dialog this time, since clicking it is already a deliberate "go set this up" action. Colony-Base/District are UNCHANGED (still real picker-set fields — the request only named System). Location Details\' System/Star entries lost their "✕" for the same reason (nothing stored to clear — editing/removing the located_at relationship in Cast is the one way to change it now); Colony-Base/District entries keep theirs. World Profile (drawers/index.js\'s worldProfileSection) gained a read-only System/Star summary row for ANY Location entity (not just the current WHERE one, per "for any location"), and its old Hex/Star-System(-by-name)/Zone row — a SEPARATE, pre-existing tracking mechanism this whole request was pointing at ("not be two different location tracking mechanisms") that had gone unnoticed until now — is replaced: Hex/Zone are now only shown/editable when the Location itself is tagged #system or #star (the old always-visible-for-any-location-with-any-UWP-data behavior is gone), and per "you can define the Hex and Zone in either type... but only one can be correct and the #star wins out" — a #system\'s own Hex/Zone only apply when its related Star (findRelatedStar) has neither set; once the Star has either, the System\'s fields become a disabled, read-only display of the Star\'s values with a note pointing at where to actually edit them. The old self-referential isStar check (a Location\'s starSystem field equalling its own name, a HOSTILE-import-era hack) is replaced by a plain #system/#star tag check, used consistently to hide planet-only fields (World Size/Atmosphere/Biome/Hydrographics/gas-giant) in World Profile and the entire World Demographics card, same as before just via a real tag instead of name-matching. Verified: all 455 domain tests (2 new, replacing the retired setLocationHierarchyField test — getSystemForLocation/getStarForLocation\'s self-tagged/one-hop/null-beyond-one-hop behavior, and getHexZoneForLocation\'s full override matrix including a Star gaining Hex/Zone AFTER a System was already relying on its own), clean build, and a 20-check jsdom smoke test mounting the real built bundle — confirmed System/Star rows have no +/-/✕ anywhere (Current Location or Location Details) while Colony-Base keeps both, the 🔗 opens Entity Detail on the right Location with the relationship type select actually focused (document.activeElement checked), World Profile shows the resolved Star as a summary row and lets the System edit its own Hex directly until the Star gains one, at which point the System\'s Hex input becomes disabled and shows the Star\'s value instead.',
    '2026-08-15 Phase 14 Location Details/Current Location unification (direct follow-up request — "not be two different location tracking mechanisms"): locationDetailsBlock (workspace/index.js) used to show one expander per WHERE-Focus-@mentioned location only (getCurrentWhereLocations) — a System/Star/Colony-Base/District picked via Current Location\'s own "+" picker had NO Location Details entry at all if it wasn\'t separately @mentioned, so there was no way to reach/edit that entity\'s Sights/Smells/Sounds. The entry list is now that same Focus-mentioned set PLUS whichever of the PRIMARY location\'s four hierarchy references (starSystemId/starId/colonyBaseId/districtId) resolve to a real entity, deduped by id — every entity Current Location can point at now gets a real Location Details entry, same sensory fields, no separate mechanism. Each hierarchy-sourced entry\'s toggle label is prefixed with its role ("System: Kepler System — Location Details") so multiple entries stay distinguishable collapsed, and carries a "✕" (reusing data-location-field-clear, the exact same attribute/handler Current Location\'s own "-" button already used — no new shell.js code) that clears that one role field on the primary location: removing the Location Details entry and unsetting Current Location\'s value are literally the same action now, not two things that could drift out of sync. A plain Focus-mentioned entry (not one of the four roles) gets no "✕" — there\'s no single field backing it to unset; removing it means editing the @mention out of Focus text, unchanged from before. Verified: all 454 domain tests (unaffected — this is a pure UI/render-layer change, no domain function signatures moved), clean build, and a 13-check jsdom smoke test mounting the real built bundle — confirmed a System/Star pair with no Focus mention still gets its own Location Details entries (role-prefixed), the System entity\'s Sights field is reachable/editable through that entry, the primary Focus-mentioned entry has no "✕", and clicking the System entry\'s "✕" both unset Current Location\'s System value AND made that entry disappear from Location Details in the same render, without cascading to clear Star.',
    '2026-08-15 Phase 14 WHERE hierarchy reconciliation batch (direct requests): (1) addRelationship/updateRelationshipType now auto-mirror located_at <-> contains onto the OTHER side of a relationship (a new INVERSE_REL_TYPE map, entities.js) — the one pair that\'s a true structural inverse of itself, unlike every other type (Member Of, Owns, ...), whose reverse edge still starts/stays generic \'linked\' until the GM retypes it themselves. Applies both at fresh creation and when an existing relationship is retyped from either side. (2) Colony-Base now cross-references too, not just System/Star: setLocationSystemOrStar is replaced by setLocationHierarchyField (entities.js), which reconciles the WHOLE System/Star/Colony-Base trio on every pick — picking a Colony-Base correlates System AND Star (two hops: Colony -> located_at -> System -> located_at -> Star); picking a System or Star that invalidates the CURRENTLY-set Colony-Base (it no longer chains up to the new pick) clears it rather than leaving a silently stale reference; a pick that resolves nothing reports back which entity is missing a relationship (`missingRelationshipFor: {entityId, wantTag}`) instead of just leaving fields blank with no explanation. (3) shell.js\'s entityPicker-select handler surfaces that report as a new promptMissingLocationRelationship() — window.confirm (a yes/no decision, same posture as every delete confirmation, not data entry) asking whether to open the entity\'s own Entity Editor and set the relationship up; "yes" opens Entity Detail on that entity and focuses its "add relationship" type select (a new focusInspectorRelationshipNextRender flag, mirroring the existing focusInspectorNameNextRender pattern) instead of leaving the GM to hunt for it. (4) Current Location\'s four rows gained a "-" clear button (data-location-field-clear) alongside the existing "+" picker — shown only once a value is set, a plain field clear with no cascade (replacing a value is what cascades, via setLocationHierarchyField above; clearing one field outright has nothing to reconcile). (5) Layout: each row now splits into a left cluster (label + value) and a right cluster (+/-), .location-summary-item switched from a single right-packed flex line to justify-content:space-between, per direct request that label/value read left-aligned while +/- stays right-aligned; .current-location-banner also claws back part of Composer\'s standard --sp-6 right gutter (a new negative right margin) so those buttons sit closer to the panel\'s own scrollbar. (6) The Actor thumbnail\'s expand/remove badges (WHO\'s "who is there") move further apart and further off the photo circle (bottom offset -0.15rem -> -0.3rem, left/right offset -0.35rem -> -0.6rem) so they only touch its outside edge instead of still clipping into it, a second direct correction against the reference mockup. Verified: all 454 domain tests (2 new/rewritten — the located_at/contains auto-mirror across both creation and retype, both type-direction, and a non-hierarchy type staying unaffected; setLocationHierarchyField\'s full Colony-Base correlation + invalidation-clearing + missing-relationship report), clean build, and an 18-check jsdom smoke test mounting the real built bundle — confirmed the row\'s left/right cluster markup, a Colony-Base pick correlating both System and Star, the "-" button clearing only its own field with no cascade, a new System pick correctly clearing a now-inconsistent Colony-Base AND Star, and — with window.confirm mocked true — the missing-relationship prompt correctly opening Entity Detail on the entity that needs a #star link with its "add relationship" type select actually focused (document.activeElement checked directly). The two CSS-only spacing changes (padding pull-in, badge offsets) are, like every layout-only change this session, not visually verifiable by this session\'s tooling (no real layout engine) — based on direct box-model review, not a rendered screenshot.',
    '2026-08-14 Phase 14 batch: phone gear relocation, Actor badge correction, WHERE System/Star/Colony-Base/District as real entity references (all direct requests): (1) The header\'s Settings gear (.settings-menu-wrap) now physically relocates to the bottom of the edge nav, below Advisor, at phone widths (<=480px, a new isPhoneWidth()/PHONE_BREAKPOINT distinct from the wider phone+tablet isCompactTab()) — shell.js\'s render() detaches the one real DOM node (no duplicate menu markup) before rebuilding the edge nav\'s innerHTML each render, then reattaches it to .mc-edge or back to .header-actions depending on width; delegated click handling doesn\'t care which parent it\'s under. The dropdown itself now opens upward instead of off the bottom of the screen there (.mc-edge .settings-menu, scoped to the actual DOM relationship, not a second media query), and the button picks up an edge-nav-tile look (icon + "Settings" label, hidden by default in its normal header spot) via explicit higher-specificity CSS (.mc-edge button\'s own descendant-selector rule turned out too low-specificity to beat .btn.ghost.sm\'s two classes). (2) The Actor thumbnail\'s expand/remove badges move from the photo\'s upper edge to its LOWER edge (bottom: instead of top:, direct correction against a reference mockup) and sit further outside horizontally so they touch the circle\'s boundary rather than cutting into it. (3) WHERE\'s Current Location System/Star/Colony-Base/District row are real entity references now (Location.starSystemId/starId/colonyBaseId/districtId, entities.js — the old free-text zone/starSystem/bases[] fields and District\'s automatic-only getContainingLocation walk are gone from the UI, left inert in the schema per migration rule 5), each with its own "+" picker aligned right on that row (data-entity-picker-open="location:<mode>:<locId>", a new compound-value form the existing WHO-Actors entity-picker overlay now also recognizes alongside its original bare-mode form) filtered to Locations tagged #system/#star/#colony/#district respectively. Picking a System or Star cross-references the other where resolvable — a new setLocationSystemOrStar (entities.js) reads the System\'s OWN located_at edge (getContainingLocation) to find its Star, and searches for a #system Location whose located_at edge points at a picked Star, deliberately NOT getContainedLocations for the reverse direction, since a freshly created relationship\'s auto-mirrored reverse edge is only ever generic "linked", never "contains" (would silently miss the natural System-side setup). Location Details (the per-location expander further down WHERE) no longer has its own separate free-text "Star system" field — it now duplicated/could disagree with the one true reference set above — and its title line reflects loc.starSystemId\'s resolved name instead. Verified: all 453 domain tests (1 new, for setLocationSystemOrStar\'s both-direction cross-reference and its "no getContainedLocations" edge case), clean build, and a 20-check jsdom smoke test mounting the real built bundle at two widths — confirmed the gear wrap\'s DOM parent flips correctly between .mc-edge (375px) and .header-actions (1440px) with its dropdown still opening correctly after relocation, the System row\'s picker excludes a #star-tagged candidate and includes a #system-tagged one, picking the System auto-fills Star from its located_at parent, both now render as real entity chips in the banner, and Location Details\' title updates to match with no leftover free-text Star-system input. The two CSS-only layout changes (gear icon/label styling, badge repositioning) are, like every phone-layout change this session, not visually verifiable by this session\'s tooling (no real layout engine) — based on direct box-model/specificity review, not a rendered screenshot.',
    '2026-08-14 Phase 14 WHO Actors batch (direct request, three items): (1) "+ Introduce NPC" now actually does something — it used to silently write into WHO\'s now-removed Focus field via the generic shift-prompt mechanism (SHIFTS[\'Introduce NPC\']/mergeSummary, context.js, both deleted as dead code). It\'s now its own inline-prompt kind (data-who-introduce-npc, distinct from data-shift-prompt): typing an existing NPC\'s name (plain text, or @Name/@[Name]) adds THAT entity to whichever group its #character tag routes it to (Protagonists if tagged, Antagonists otherwise); typing a name matching no NPC creates a brand-new one (same as "+ New NPC") and routes it the same way (shell.js\'s new resolveOrCreateIntroducedNpc). (2) Every Actor thumbnail (Protagonists/Antagonists/Bystanders — all three equally GM-curated lists as of the prior entry below) is now draggable to move it between groups: data-drag-actor="kind::npcId" on the thumbnail, data-drop-actor-group="kind" on each group container, sharing the exact same onDragStart/onDragOver/onDrop delegated-handler pattern (and .drop-hover CSS) the Guide-tree/Battlemap-icon drag features already established, plus a touch-gesture equivalent (the existing generalized touchDrag object gained an actorRef field alongside guideNodeId/entityId/documentId). A new domain function, moveSceneActor (scenes.js), removes from the source list and adds to the destination in one atomic pass; no tag-based restriction on the drop side — a #character NPC can be dragged into Bystanders same as anyone else (explicitly asked for), only the "+" picker\'s candidate filtering and Introduce NPC\'s routing are tag-gated. (3) The expand-scene-details (▸/▾) and remove (✕) controls moved from a row below each thumbnail to small circular badges overlaid on the photo\'s upper-left/upper-right edge (reference mockup) — .actor-thumb-circle is a position:relative anchor around the photo <button> and both badge <button>s, all three as siblings (a <button> can\'t contain another <button>, so the badges couldn\'t live inside the photo\'s own click target). Also, per a separate direct request: the Composer\'s WHO/WHERE/WHAT/WHY/HOW collapsible section headers (dashboardSection(), workspace/index.js) now read like the Navigator\'s "Scene Summary" heading — small/bold/accent-colored text, no boxed-button chrome — instead of a .btn.ghost.sm button; a plain CSS restyle of .dashboard-section-toggle, no markup/behavior change beyond dropping those three utility classes. Verified: all 452 domain tests (1 new, for moveSceneActor), clean build, and a 21-check jsdom smoke test mounting the real built bundle — confirmed Introduce NPC\'s three paths (exact-name dedupe onto an existing Antagonist with no duplicate entity created, an @[Name]-resolved #character NPC landing on Protagonists only, and a fully novel name creating and placing a new NPC on Antagonists), a full drag-and-drop round trip via the REAL onDragStart/onDragOver/onDrop handlers (a hand-built fake DataTransfer standing in for jsdom\'s incomplete native DragEvent support, carrying the real MIME payload through dragstart→dragover→drop) moving an existing Antagonist into Bystanders and confirming .drop-hover toggles correctly, a #character Protagonist successfully dragged into Bystanders with no restriction, and the badge markup\'s sibling (not nested-button) structure. The touch-equivalent drag path shares completeActorDrop with the mouse path (exercised above) but its own elementFromPoint-driven hover-target logic isn\'t independently verifiable here — jsdom has no real layout/hit-testing engine, same known gap noted for every other phone-input verification this project has done.',
    '2026-08-14 Phase 14 WHO redesign, follow-up correction (direct request, reversing part of the previous entry below after trying it): the Focus free-text @mention textbox is GONE from WHO entirely, not kept alongside the "+" picker — "only use a textbox for selection" (the picker\'s own typed-autocomplete search), never for authoring prose with embedded mentions. Protagonists/Antagonists are no longer derived by parsing Focus text + the #character tag; all three groups (Protagonists/Antagonists/Bystanders) are now equally GM-curated id lists on the scene (scenes.js\'s new protagonistIds/antagonistIds, mirroring the existing bystanderIds — addSceneProtagonist/removeSceneProtagonist/addSceneAntagonist/removeSceneAntagonist added, sharing one internal addSceneActor/removeSceneActor with addSceneBystander), each with its OWN "+" picker (data-entity-picker-open="protagonist"|"antagonist"|"bystander") and its OWN remove control on every thumbnail (data-scene-protagonist-remove/-antagonist-remove/-bystander-remove) — previously only Bystanders had either. The picker further filters candidates by kind: Protagonists\' to NPCs tagged #character, Antagonists\' to NPCs NOT so tagged, Bystanders\' to any NPC (renderEntityPickerOverlay, shell.js). gatherSceneContext\'s whoEntities (domain/copilot.js, feeds the Advisor\'s buildStoryOptions) now reads the union of the current scene\'s three id lists directly instead of parsing @mentions out of who.summary text — a straightforward swap since nothing populates that text anymore; context.who.summary itself stays in the schema untouched (migration rule 5) but is now permanently unused by any UI, so composeNarrativeDraft\'s WHO contribution (Navigator\'s draft) will read as empty going forward on any campaign using the new Actor-list UI — a known, accepted side effect of "only use a textbox for selection," not something asked to be separately replaced. whoEntityPicker (WHO\'s old tag-listbox/mention-insert picker, dead once Focus was gone) is deleted; its "+ New NPC"/"+ New Faction" quick-create buttons move up next to "Introduce NPC" so that capability isn\'t lost. Ephemeral whoTagFilter and the data-who-tag-select/data-insert-who-mention-select handlers (shell.js) are removed as dead code alongside it; WHERE/WHY\'s own identical-shaped pickers are untouched. Verified: all 451 domain tests (2 rewritten/added for gatherSceneContext\'s new derivation and the new addSceneProtagonist/addSceneAntagonist pair), clean build, and a 25-check jsdom smoke test mounting the real built bundle — confirmed no Focus field or the word "Focus" appears anywhere in WHO anymore, all three groups render with their own "+" and correct tag-based candidate filtering (Antagonists\' picker offers an untagged NPC and excludes a #character one; Protagonists\' picker excludes both that same untagged NPC AND an already-added protagonist, correctly landing on "no matching NPCs"; Bystanders\' picker imposes no tag filter at all), a pre-seeded protagonist\'s thumbnail carries its own remove control, and the full add-to-all-three/remove-one round trip lands correctly in the persisted campaign document with who.summary never touched.',
    '2026-08-14 Phase 14 WHO redesign (direct request, later corrected above): Focus/Protagonists/Antagonists/Bystanders ("Actors") now render as rows of small, read-only, mention-style thumbnails (new actorThumb(), workspace/index.js — photo circle + name, data-open-entity click, same as any other @mention) instead of always-visible free text/name-chip rows. Protagonists/Antagonists keep their existing derivation (live from WHO Focus\'s @mentions + the #character tag, gatherSceneContext) unchanged and get NO add control, confirmed via AskUserQuestion before building; Bystanders keeps its GM-curated addable list but the old native <select data-scene-bystander-add> is replaced by the same new "+" picker Focus now also has. The 5 scene-scoped fields (Disposition/Motivation/Threat Rank/Challenges/Opportunities) + Current goal, previously inline inside each name-chip\'s own expand, are preserved via a small separate expand toggle beneath each thumbnail (npcSceneDetailBody()) so the thumbnail\'s own click stays a pure "open like a mention" action. New shared "+" entity picker (data-entity-picker-open="focus"|"bystander", shell.js\'s new entityPicker ephemeral state + renderEntityPickerOverlay(), reusing .mc-search-overlay/.search-panel\'s existing centered-modal CSS wholesale) is a typed-autocomplete overlay filtered to NPC entities (excluding already-added bystanders in that mode); selecting a candidate either inserts a real @mention into Focus (the existing insertContextMention, unchanged) or calls addSceneBystander — Focus keeps its full free-text @mention editor as-is, the picker is an additional faster path, not a replacement. Every inline subtitle under WHO (each dashboard section\'s lead paragraph, plus Protagonists\'/Antagonists\'/Bystanders\' hint text) is now collapsed by default behind a "?" icon right-aligned with its section title (helpToggle, reusing drawers/index.js\'s existing pattern; a new local wsHelpBody() companion since that module keeps its own helpBody private) instead of always-visible prose. Verified: all 450 domain tests, clean build, and a 27-check jsdom smoke test mounting the real built bundle — confirmed the Focus thumbnail row and "+" icon, all three NPC groups render as thumbnails with Protagonists/Antagonists correctly lacking any add icon and Bystanders correctly having one and no longer using the old <select>, the "?" toggles are present and their text hidden until opened, the picker overlay opens/filters/closes on selection, a picked bystander both appears as a thumbnail and is addable via its own scene-detail expand toggle (which correctly reveals the preserved 5-field body), and picking an NPC from Focus\'s picker lands a real mention in who.summary end to end.',
    '2026-08-13 Phase 14 Entity Editor polish batch #2 (direct follow-up requests): (1) Strain now reuses the SAME .statblock-row.track-row/.track-widget/.track-boxes/.track-value-badge markup Health/XP/Momentum tracks use (drawers/index.js\'s strainMeter(), read-only spans instead of trackRow()\'s clickable buttons) instead of the bespoke fill-bar from the previous batch -- the now-unused .strain-meter/-fill/-label CSS is removed. (2) Track/attribute row label column narrowed again (minmax(5rem,34%) -> minmax(4.5rem,28%)) with track boxes shrunk slightly (1.75rem->1.55rem) so a full track fits in less width on phone, and the label gets an explicit align-self:center (redundant with the grid\'s own align-items:center, stated directly per request rather than relying only on inheritance). (3) Every collapsible section-header button in the Entity Editor (Overview, Revealed/hidden, World Profile, World Demographics, Conflict\'s "Add depth", Enhancements) now shares one new .section-toggle class (small/dim/uppercase, explicit properties rather than the old .overview-toggle\'s font:inherit so it looks identical regardless of which element wraps it) instead of a mix of that class and generic .btn.ghost.sm boxy buttons. (4) The entity Type dropdown and both selects in the "add new relationship" form now match .rel-label-input\'s 0.72rem (the generic textarea/select/input rule otherwise defaults every <select> to 0.9rem) -- the relationship chip\'s own .rel-type-select was already 0.72rem. (5) WHERE\'s System/Star/Colony-Base/District quick-reference strip (formerly its own locationSummaryHeader() block at the top of the WHERE section) is now merged directly into currentLocationBanner (workspace/index.js) -- it renders wherever "📍 Current location" does instead of as a separate block, and the standalone function is gone. (6) Statblock groups\' own always-visible "+ Field"/"+ Track" buttons (previously one pair per group card) and the whole Enhancements section (previously its own always-visible section) are now consolidated under the "Add a Statblock or Attribute" toggle (renamed from "Add a statblock") -- collapsed by default, so neither competes for attention until the GM actually wants to add something; a new shared statblockGroupLabel() helper (used by statblockGroupBlock/characterSheetGroupBlock\'s own headers too, replacing three copies of the same label logic) labels which group each relocated +Field/+Track pair applies to, since there can be more than one group. Verified: all 450 domain tests, clean build, and a 24-check smoke test importing the actual render functions directly -- confirmed +Field/+Track/Enhancements are absent when the toggle is collapsed and present (correctly group-indexed) when open, Strain renders the real track-box markup with correct used/cap and on/off box states (a real 3-strain/8-capacity case), every named section uses .section-toggle, the relationship dropdown/name order and font-size CSS hooks are all in place, and Current Location\'s banner now carries System/Star/Colony-Base/District for a real @-mentioned location. The label-width/box-size/vertical-centering CSS changes are, like every phone-layout change this session, not visually verifiable by this session\'s tooling (no real layout engine) -- based on direct box-model review, not a rendered screenshot.',
    '2026-08-13 Phase 14 Entity Editor polish batch (direct request, phone-screenshot-driven): (1) removed the "Frictionless Empowerment" tagline from the header titlebar (ui/shell.js) to reclaim vertical space -- the About overlay keeps its own copy of the same line, untouched. (2) The Revealed/hidden (GM) block is now nested INSIDE Overview\'s own expanded branch (drawers/index.js\'s inspector()) instead of being a sibling section with its own independent visibility -- collapsing Overview now hides Revealed/hidden too; its own open/closed toggle still works independently once Overview itself is expanded. (3) A relationship chip\'s type <select> now renders BEFORE the entity-name button (previously name first, type second) to match the order the "add new relationship" form below it already uses. (4) Statblock fields: plain text fields (Role, Notable Gear, ...) now stack label above value across two rows instead of a shrunk side-by-side split -- free text needs real width to read, unlike a track\'s boxes or an attribute\'s short number; track/attribute fields keep one side-by-side row but with a narrower label column (minmax(5rem,34%), was 42%) so their compact controls get more room. (5) Strain now renders as a read-only fill-bar meter (new .strain-meter, drawers/index.js\'s strainMeter()) instead of a plain "Strain: X/Y" text line -- still just a display (strain is a derived sum of installed enhancements\' costs, never a directly-set value), turns --danger red over capacity via the same .over-strain convention already used elsewhere. (6) Character-sheet compact stat/resource pills (.track-row-compact/.attr-row-compact -- the Edge/Heart/Iron/Shadow/Wits-style top row and its resource meters) widened 4.4rem->5.2rem with their label font bumped 0.6rem->0.68rem and allowed to wrap, plus slightly larger track boxes (15px->18px) and attribute input text (0.78rem->0.85rem) -- .character-sheet-stats\' existing overflow-x:auto absorbs any row that now runs past the visible width, same escape valve as before. Verified: all 450 domain tests, clean build, and an 8-check smoke test importing the actual render functions directly -- confirmed the tagline is gone from the header (and untouched in the About overlay), Revealed/hidden\'s presence tracks Overview\'s own collapsed state exactly, the type-select now precedes the name button in a real rendered relationship chip, the strain meter renders with a fill width matching a real 3-strain/8-capacity installed enhancement (~38%), and the old plain-text Strain line is gone. The statblock label/value and character-sheet-pill sizing changes are CSS-only and, like the prior two phone-layout fixes, not visually verifiable by this session\'s tooling (no real layout engine) -- based on direct box-model review, not a rendered screenshot.',
    '2026-08-13 Phase 14 fix: statblock field labels were ellipsis-truncated to 1-3 letters on phone, with barely-tappable value boxes -- a real reported bug, screenshot-confirmed (a "Disposition"/"Notable Gear"-style field name had no room in the ~4.4rem flex-wrap badge layout the <=480px tier squeezed ~5 fields onto one line into). Reworked to one full-width row per field, label left / control right, deliberately trading the "fit several per row" density goal for every label being fully readable and every control being a comfortable tap target -- a bottom-border list (no per-row pill background) reads as a familiar native settings list. Covers all three field kinds (plain text, attribute/roll, track) with a shared label-column width so a group\'s rows still line up; the read-only Combat/Danger-style summary badges above the field list got the same breathing room. CSS-only (styles/cockpit.css); all 450 domain tests unaffected, clean build. Not verifiable via this session\'s jsdom tooling (no real layout engine) -- based on the reported screenshot and the same box-model reasoning the earlier edge-nav-width fix used; worth a follow-up look on a real phone.',
    '2026-08-12 Phase 14 addition: entity relationship linking on phone (design/UX-ROADMAP.md, not one of the original 7 steps -- a gap Steps 4/5 exposed rather than fixed). Creating a relationship normally means dragging one entity onto another, which needs both visible at once -- no longer true once the drawer became the one visible panel at compact widths (Cast can\'t sit beside the Entity Editor there the way it can on desktop). The Relationships section already had a no-drag fallback (a plain <select> + Link button, unaffected/untouched), but nothing better for a long Cast. Added: drawers/index.js\'s new relPickerBlock() -- a "🔍 Find entity to link" button opens a searchable list reusing the same filterEntities() (domain/entities.js) Cast\'s own search bar already calls, rendered as a smaller floating window over the top half of the Entity Editor panel (.rel-picker, styles/cockpit.css) -- position:fixed nested inside .mc-drawer, which always carries a `transform` for its slide animation and per spec becomes the containing block for any position:fixed descendant, so the overlay anchors to the panel itself (not the full viewport) with no extra positioning math, and stays pinned to the top half regardless of how far the editor content underneath has scrolled. Tapping a result (shell.js\'s new data-rel-picker-pick handler) calls the same addRelationship(d, activeId, targetId, \'linked\') a desktop drag-drop onto the Entity Editor already does, then closes the overlay -- deliberately tap-to-link, not drag-to-link, since cross-panel touch drag is one of the more fragile mobile interactions to get right without a real device to verify it on (same reasoning Step 5 used). New ephemeral relPickerOpen/relPickerFilter (shell.js), plumbed through buildDrawerUi() like every other drawer-scoped ephemeral flag. Verified via all 450 domain tests (unaffected), a clean build, and a 10-check jsdom mount of the real built bundle: seeded two NPCs via the exposed debug store, opened one\'s Entity Editor from Cast, opened the picker, confirmed the other entity appears and the entity itself doesn\'t, confirmed the search filter narrows the list, tapped the result, and confirmed a real `linked` relationship landed on the campaign document and the picker closed.',
    '2026-08-12 Phase 14 fix: .mc-drawer wasn\'t reliably reaching the edge nav at compact widths (real-device follow-up to Step 4/5) -- it fell back to `width: min(var(--drawer-w, 420px), 88vw)` above 480px (the width cap meant for desktop/tablet\'s occasional-drawer case, where other panels are visible alongside it), leaving a gap between the drawer\'s own right edge and the always-visible edge nav on many phone/tablet widths -- read as the edge nav covering the Storyboard, since the drawer is now the ONE visible panel at every compact width (Steps 4/5), not an occasional overlay. Fix: the existing `left: 0; width: auto` full-width treatment (previously scoped to <=480px only) now applies across the whole <=1023px compact tier (styles/cockpit.css) -- the standard two-inset computed-width form (paired with the base rule\'s `right: var(--edge-w)`), so the box is defined by its edges and can\'t fall short of either one regardless of viewport width within that tier. The <=480px block keeps only what\'s still genuinely phone-specific (tighter padding, single-column field-row reflow). CSS-only change; all 450 domain tests unaffected, clean build.',
    '2026-08-12 Phase 14 Step 4 built (design/UX-ROADMAP.md): tablet now shares Step 5\'s always-open, permanently-pinned Composer/Navigator/Advisor tab menu instead of the old desktop-style two-column split -- the same "no room for 3 columns plus a drawer side by side" problem phone had turned out to apply to tablet too. isPhoneTab()/PHONE_TAB_BREAKPOINT (shell.js) renamed to isCompactTab()/COMPACT_TAB_BREAKPOINT and widened from 767 to 1023 -- a single constant change, since Step 5\'s tab-pinning/drawer-panel machinery never actually assumed phone specifically. The Advisor\'s old tablet-only slide-in panel (a separate data-toggle-copilot summon/dismiss toggle, distinct from phone\'s retired bottom sheet) and its edge-nav button are both hidden at compact widths now (styles/cockpit.css) -- redundant once Advisor is always reachable as a pinned tab there too; the phone/tablet .mc-copilot media queries collapsed into one `display:none` rule. .mc-workspace\'s own hide-and-skip-rendering breakpoint (styles/cockpit.css + shell.js\'s render()) widened to match. Verified via all 450 domain tests (unaffected), a clean build, and three jsdom mounts of the real built bundle: tablet width (850px, confirmed the 3 pinned tabs, no collapse button, composer active by default) including a resize-across-breakpoints check (tablet to phone width stays compact; tablet to desktop width correctly reverts to inline Composer/Navigator with the drawer closed) confirming the debounced resize listener handles both directions, plus a full phone (375px) and desktop (1440px) regression pass confirming zero change to their already-verified Step 5/Step 3 behavior. Step 6 (a dedicated touch-target/gesture polish pass) still has no real-device visual verification behind it; 7 depends on the still-unbuilt Moves menu.',
    '2026-08-12 Phase 14 Step 5 built (design/UX-ROADMAP.md), plus Composer/Navigator layout refinements: independent per-panel scrolling (each of .workspace-card-body now has its own overflow-y:auto instead of Navigator using position:sticky, which silently clipped its own content whenever Navigator was taller than the viewport -- its bottom was unreachable until Composer had been scrolled all the way down), equal-width columns (.storyboard-grid was minmax(20rem,2fr)/minmax(16rem,1fr), now minmax(18rem,1fr) each), both cards\' lead paragraphs removed (card() now omits <p class="lead"> entirely on a falsy lead instead of rendering an empty one), and tight top/left/right title padding (.workspace-card padding 5px top/left, 2px right -- a new .workspace-card-body-inner wrapper keeps the actual scrolling text at a normal right inset so only the scrollbar track itself sits at the panel edge; the <h2> keeps a 10px bottom margin as the gap to whatever\'s under it). Step 5 itself: real phone testing (a deployed build, on an actual phone) found the Step 1-3 phone behavior -- Composer and Navigator simply stacked, .mc-workspace scrolling as one long page -- didn\'t work in practice: Navigator was effectively unreachable below a long Composer, not merely inconvenient. Fix is the real Step 5 design: DRAWER_META gained \'composer\'/\'navigator\' entries alongside \'advisor\'; a new isPhoneTab() (window.innerWidth <= 767, matching styles/cockpit.css\'s own breakpoint exactly) plus a debounced window resize listener (mountShell()) decide, at render time, whether the tab strip pins all three permanently (phone) or just lets Advisor ride along conditionally as before (desktop/tablet, Step 3 unchanged); on phone the strip is never hidden, none of the 3 pinned tabs render a close button, real drawers (Guide/Oracle/...) add their own closable tab alongside the pinned three rather than replacing them, and .mc-workspace itself is hidden by CSS and skipped at render time (not just visually hidden -- its content isn\'t generated at all on phone, avoiding duplicate DOM). workspace/index.js exports composerBodyHtml/navigatorBodyHtml (the inner content, no outer card chrome) so shell.js\'s renderActiveDrawerHtml can render either directly into the shared drawer panel, same special-casing pattern already used for \'advisor\'/\'faction-events\'. The drawer\'s "collapse to peek behind it" control is hidden on phone too (nothing behind it to peek at once .mc-workspace is gone) and the drawer panel itself is always considered open there, ignoring drawerCollapsed. Verified via all 450 domain tests (unaffected), a clean build, and two separate jsdom mounts of the real built bundle -- one at a 375px phone width (confirmed the 3 pinned tabs with no close buttons, Composer as the default landing tab, switching to Navigator and back, a real drawer correctly adding a 4th closable tab and the pinned three surviving its close) and one at 1440px desktop width (confirmed zero regression to Steps 1-3\'s existing desktop behavior). Steps 4 (tablet\'s own placement) and 6 (a dedicated touch-target/gesture polish pass) still have no real-device visual verification behind them; 7 depends on the still-unbuilt Moves menu.',
    '2026-08-11 Phase 14 Steps 1-3 built: Composer/Navigator/Advisor rename + the Advisor drawer-tab-strip integration (design/UX-ROADMAP.md, requirements/functional-requirements-v3.md\'s Terminology/Storyboard sections). Step 1: every user-facing "Story Dashboard"/"Narrative Composer"/"Co-Pilot" label renamed to Composer/Navigator/Advisor (ui/shell.js, ui/workspace/index.js, ui/copilotPanel.js, ui/drawers/index.js) -- underlying module/function names unchanged, label-only. Step 2: ui/workspace/index.js\'s renderWorkspace() split from one "Story Dashboard" card with an internal 2-column grid into two real sibling .workspace-card elements (composerCard/navigatorCard) inside a new .storyboard-grid wrapper (styles/cockpit.css, replacing .dashboard-grid/.dashboard-col/.dashboard-composer-col) -- Navigator\'s former internal "Narrative Composer" <h3> is now "Scene Summary" under its own card-level "Navigator" <h2>. Navigator\'s Send-to-Journal button (data-composer-journal) now also calls openDrawerTab(\'journal\') so the GM sees the note land, not just a toast. Step 3: the Advisor (.mc-copilot) is a permanent desktop column only while no drawer is pinned open; DRAWER_META gained a synthetic \'advisor\' entry that rides along in the drawer tab strip (unclosable, no ✕) whenever >=1 real drawer is open, flagged (.drawer-tab.pending, a warm highlight + a literal "★" in the label -- never color-only) when hasPendingAdvisorDecision() finds a live, non-dismissed Story Option; renderActiveDrawerHtml special-cases \'advisor\' to render renderCopilot() into the drawer body, same pattern already established for \'faction-events\'; .cockpit.has-open-drawer (desktop-tier CSS only) hides the standalone Advisor panel while this is active so its content isn\'t duplicated/unreachable underneath the drawer. Verified via all 450 domain tests (unaffected) plus a jsdom mount of the real built bundle (fake-indexeddb-polyfilled, dev-only, not added to package.json) driving the actual open-drawer -> Advisor-tab-appears -> switch-to-it -> close-last-drawer sequence end to end. Steps 4-7 (tablet\'s own placement decision, the phone unified tab menu, a phone-tier polish pass, and the (still-unbuilt) Moves-menu icon\'s placement check) remain open -- Step 5 in particular is a larger change than 1-3 and this pass had no real device/browser available to visually verify a mobile layout against (jsdom checks structure, not appearance), so it\'s left for a pass with that tooling, same posture as docs/adr/0033 once recorded for the same reason.',
    '2026-07-16 Phase 13a/13b built: Location Details + WHO\'s scene-scoped NPC groups (docs/adr/0041-scene-operating-model.md, same-day follow-up to the roadmap -- the confirmed first increment). 13a: WHERE\'s IL chip gains a "Location Details" expander -- [immediate location] at/on the [object type] in the [star system] [sector], all flat fields on the location entity itself (entities.js gained sector/objectType/sights/smells/sounds) -- plus oracle-populated Sights/Smells/Sounds (new Location Themes.Sight/Smell/Sound tables). 13b: WHO now shows three scene-scoped NPC groups -- Protagonists (#character-tagged, derived from WHO\'s own @mentions) / Antagonists (everyone else mentioned) / Bystanders (a GM-curated add-list) -- each NPC expandable to its already-existing currentGoal plus 5 new oracle-seedable fields (Disposition/Motivation/Threat Rank/Challenges/Opportunities, new scenes.js npcStates living on the current scene object, not a permanent entity mutation). Both features share the "auto-populate, then remember my edits" mechanism the roadmap scoped down to: rolling a field records which oracle table+index it came from; editing it afterward writes that SAME index back through oracles.js\'s already-proven updateOracleEntry -- so the same roll produces the GM\'s own phrasing campaign-wide from then on. A hand-typed value (never rolled) has no source to remember, so it\'s just a plain edit. New session.js orchestration functions (rollNpcSceneField/editNpcSceneField/rollLocationSensoryField/editLocationSensoryField) mirror generateNpc/deepenNpc\'s existing "roll then patch" shape. 5 new domain tests (447 total) plus a direct Node smoke test of the full round trip against both collapsed and expanded UI states. npm test/node scripts/build.js both clean. 13c-13k remain roadmap-only (docs/adr/0041).',
    '2026-07-16 Phase 13 roadmap (Scene Operating Model), docs/adr/0041-scene-operating-model.md. Direct follow-up, much larger than Phase 12: the Dashboard/Co-Pilot shell is right but still mostly empty of the actual scene content a GM needs -- a real WHERE location hierarchy, WHO NPCs organized into three scene-scoped groups with per-NPC situational fields, WHAT\'s tracker-linked News Events/cross-scene Dangers/an algorithmic Shared Circumstances generator, WHY\'s carried-over-situation oracle filtering, HOW\'s asset/gear suggestions, an oracle "auto-populate, then remember my edits" loop, an optional guided walkthrough mode, manual-dice-result-driven consequences, and Co-Pilot merged into the persistent drawer tab group. Asked 3 direct questions before writing anything: this pass was roadmap+ADR only; oracle learning scoped to extending oracles.overrides; first phase to build next was the scene data model (13a/13b). New ADR 0041 records the full phased roadmap (13a-13k); docs/adr/0040\'s still-open 12c recorded as superseded by 13d.',
    '2026-07-16 Phase 12f: 5 W tabs retired, Co-Pilot becomes the decision sandbox, Focus-text bug fixed (docs/adr/0040-story-dashboard.md, direct follow-up -- supersedes 12a\'s "additive, not a replacement" framing). Two-part request. (1) Bug: Story Options/the Narrative Composer weren\'t reflecting free prose actually typed into WHO/WHERE\'s Focus fields -- gatherSceneContext/composeNarrativeDraft only ever read parsed @mentions and specific structured entity fields, never the surrounding text itself. Fixed by having composeNarrativeDraft use the raw Focus text (gatherSceneContext\'s new whoSummary/whereSummary) directly instead of re-deriving a synthetic "the scene is set at X" sentence from it. (2) Design: confirmed via direct follow-up -- retire the 5 individual WHO/WHERE/WHAT/WHY/HOW tabs entirely (not keep them alongside Dashboard) and fold every suggestion/oracle-generating control into the always-visible Co-Pilot panel as "an active decision sandbox." ui/workspace/index.js is now a single Story Dashboard: header dials/Activity + 5 open/collapsible sections (one per former tab, same toggle convention as Bases of Influence) + the Narrative Composer moved to the workspace\'s top-right (position: sticky). Co-Pilot (ui/copilotPanel.js) absorbs the full Story Options list (was condensed to 3), both Suggestion Lens pickers, Site Concept/Adventure Seed, the Activity -> Rules Lens suggestion, "Continue Story," and the 6 WHAT_ACTIONS shift chips -- every relocated control keeps its original data-* handler, markup relocation only. ui/shell.js\'s tab strip and [data-question] handler are deleted; Ctrl+Left/Right and the docked-Faction-Events jump now target the new expandedDashboardSections ephemeral state instead of the retired context.active. advise() drops its active===\'who\'/\'where\'/\'why\' suggestedOraclePath branches -- the existing dial-based fallback is now the only path. 2 new/updated domain tests (442 total) plus a direct Node smoke test confirming the bug fix and the Co-Pilot<->Composer selection wiring. npm test/node scripts/build.js both clean.',
    '2026-07-15 Phase 12a/12b built: the Story Dashboard + Narrative Composer (docs/adr/0040-story-dashboard.md, same-day follow-up to the roadmap). New `dashboard` view -- a 6th strip button alongside WHO/WHERE/WHAT/WHY/HOW, additive (the five focused tabs unchanged). Deliberately NOT added to schema.js\'s CONTEXT_QUESTIONS (that array is the persisted 5-question model; Dashboard has no context.dashboard sub-object) -- the strip appends one more button reusing the exact same generic data-question click handler instead; Ctrl+Left/Right cycling still only cycles the 5 real questions. Layout: a header (current location, WHAT\'s Threat/Mystery/Stress dials, HOW\'s Activity -- the SAME editable controls those tabs already use) over a 2-column grid -- WHO/WHERE context digests left, the full buildStoryOptions() list (now with a `selectable` mode, a checkbox per row) plus the new Narrative Composer right. copilot.js\'s new composeNarrativeDraft() generalizes recomposeSceneText + buildSessionRecap into one draft paragraph seeded by WHO/WHERE/WHAT/WHY plus checked Story Options, preserving raw @[Name]/markup verbatim since both consumers (buildMentionEditorHTML for the preview, addNote for the Journal) already expect that format. Shipped as a READ-ONLY live preview rather than the ADR\'s originally-sketched editable field -- implementation surfaced a real conflict (a live-recomputed contenteditable would clobber a hand-edit on the next unrelated re-render); polishing happens after "Send to Journal," in the Journal note itself. 3 new domain tests (441 total) plus a direct Node smoke test of the dashboard\'s render output against a bare and a populated campaign (no browser automation available in this environment). 12c-12e (oracle-tailored dropdowns beyond WHY, closing the SHIFTS reachability gap, dead-export housekeeping) remain roadmap-only.',
    '2026-07-15 Phase 12 roadmap (Story Dashboard) + doc rebaseline (docs/adr/0040-story-dashboard.md). Direct request for a genuinely interactive, narrative-building 5-W workspace -- oracle suggestions and cumulative story options steerable live at the table. A full audit (every src/domain/*.js export grepped against src/ui/**/*.js) found the building blocks already exist, underused: scenes.js\'s recomposeSceneText (a proven live field->narrative composer, scoped only to Scenes), recap.js\'s buildSessionRecap (a proven multi-signal->prose assembler, one fixed shape), activities.js\'s suggestRulesLens (the only other "selection->oracle suggestion" hook besides this session\'s own WHY-only Story Options), 8 of context.js\'s 17 SHIFTS reducers reachable from no UI control at all, and five confirmed-dead exports. Asked directly whether to strengthen the existing always-visible strip/Co-Pilot instead of merging tabs (lower-risk, preserves Article X\'s "workspace changes, not the application") or design a real merged dashboard despite reversing that principle -- confirmed: a real merged dashboard, recorded as a deliberate ADR-scoped exception (additive -- the five focused W-tabs are untouched). ADR 0040 phases the result into 12a (new `dashboard` view) through 12e (dead-export housekeeping) -- roadmap only, none of it built this session. Alongside the roadmap: DESIGN-NEW-FUNCTIONALITY.md and CLAUDE.md archived verbatim to docs/archive/ and rebaselined -- DESIGN-NEW-FUNCTIONALITY.md\'s ~250-line phase-by-phase section (duplicating both PROGRESS.md and the ADRs) collapsed to a short pointer plus Phase 12 in full (476 lines -> 131); CLAUDE.md kept fully intact (every architectural rule/Known non-issue unchanged, confirmed via diff against the archived original) with only 5 targeted fixes: a badly stale phase-status line, a directory-map update, a confirmed-absent Playwright note, a stale cross-reference, and a new paragraph recording the Article X exception.',
    '2026-07-15 Reference Library Release migration complete (docs/adr/0039). The reference-library-v1 GitHub Release now has all 29 PDFs uploaded (web UI). Live verification -- not just trusting the plan -- caught a real gotcha: GitHub sanitizes an uploaded release asset\'s filename (whitespace and characters like parentheses collapse to a single "." -- "Hostile setting.pdf" became "Hostile.setting.pdf" on GitHub\'s side), which the original releaseAsset catalog values (naively "the local filename, URL-encoded") didn\'t account for -- the very first spot-check curl 404\'d. Diagnosed by comparing GET /repos/meta5by5/GMAtlas/releases/tags/reference-library-v1\'s real assets[].name values against the catalog; confirmed the 5 affected files\' size fields matched byte-for-byte (same content, sanitized name only, not a bad upload) before fixing referenceLibraryManifest.js\'s releaseAsset field for exactly those 5 entries -- no code/algorithm change needed, releaseAsset was already plain data, just wrong data for 5 rows. All 29 asset URLs re-verified resolving via a HEAD-request sweep (HTTP 200) plus a byte-range request confirming real %PDF content on one (ruling out a disguised HTML error page). deploy-pages.yml\'s verification step is back to a hard failure (using the corrected Hostile.setting.pdf URL), same posture as the pre-ADR-0039 local-PDF check.',
    '2026-07-14 Story Options Phase 2: accept/dismiss tracking (docs/adr/0039 same-day follow-up -- closes the Phase 2 roadmap). New ephemeral dismissedStoryOptionIds (shell.js) mirrors ADR 0036\'s dismissible-suggestion conflictEscalationSuggestions pattern exactly (not persisted to the campaign, cleared on reload) -- filtered out of both storyOptionsBlock (WHY, workspace/index.js) and the Co-Pilot panel\'s storyOptionsCard, both now fetching a deeper pool (limit:12) than they actually display (6/3) so a used or dismissed option makes room for the next-ranked one instead of the list just shrinking. Rolling an option\'s linked oracle (a new data-story-option-id attribute alongside the existing data-story-option-roll path lets that handler know which option triggered it) or adding it to the Journal both count as "accept" (the GM DID something with it); a new explicit data-story-option-dismiss (✕) covers "not interested." All three call the same dismissedStoryOptionIds.add(id) in shell.js. renderCopilot/storyOptionsCard now take a ui param (shell.js\'s render() now hoists buildDrawerUi() into a shared workspaceUi var passed to both renderWorkspace and renderCopilot, instead of computing it twice). No new domain tests -- pure UI wiring of already-tested domain logic.',
    '2026-07-14 Story Options Phase 2: condensed card in the Co-Pilot panel (docs/adr/0039 same-day follow-up). copilotPanel.js\'s always-visible <aside> (not tab-scoped, unlike WHY) gained a new storyOptionsCard() showing the top 3 buildStoryOptions() results, so a GM working any of the 5 W-tabs sees the cumulative suggestion without switching to WHY. Reuses WHY\'s own data-story-option-roll/-journal attributes verbatim -- shell.js\'s handlers already recompute buildStoryOptions and look an option up by id at click time, so they don\'t care which DOM location triggered them; zero new shell.js wiring needed, just the new render function plus ~5 lines of CSS (.copilot-story-option spacing). No new domain tests -- pure UI reuse of already-tested domain logic.',
    '2026-07-14 Story Options Phase 2: oracles.usage tie-break (docs/adr/0039 same-day follow-up). buildStoryOptions now reads campaign.oracles.usage (already tracked on every real roll via session.js\'s rollOracle, previously read by nothing at all -- its own write-site comment said "drives Co-Pilot suggestions later," this is that later) as a secondary sort key, breaking an EXACT weight tie between two options (e.g. a non-negotiating faction\'s agenda and an open World Flag, both weight 5) in favor of whichever option\'s oracleGroup the GM actually rolls from more often. Only a tie-break, never an override -- a higher-weighted option (e.g. Negotiate\'s fear/need-over-agenda boost) always wins regardless of usage. 1 new domain test (440 total): two campaigns with identical tied options but opposite oracles.usage weighting produce genuinely different (not just coincidentally matching) rankings.',
    '2026-07-14 Story Options Phase 2: weighted lens draw wired to WHY (docs/adr/0039 same-day follow-up). drawSuggestionLenses\' sceneContext param (built and tested alongside Story Options, but not yet consumed by any UI) now has a real trigger: a new "Suggest a Lens" button on WHY (whyLensSuggestBlock, workspace/index.js) opens the exact same lens-picker -> suggestNextWithLens flow WHAT\'s "What Happens Next?" already offers, just drawn with gatherSceneContext(campaign) so a Conflict/faction/Negotiate-activity currently in play gets matching lenses better odds -- never a guarantee, still a random draw, just no longer context-blind. lensPickerHtml generalized to take (open, draw, {intro}) as plain params instead of always reading ui.lensPickerOpen/ui.lensDraw, so both WHAT\'s and WHY\'s pickers reuse identical rendering with their own separate ephemeral state (new whyLensPickerOpen/whyLensDraw, shell.js); picking a lens closes whichever picker was open and calls the same, completely unchanged suggestNextWithLens either way. WHAT\'s own button and its pure-random draw are untouched. No new domain tests needed -- pure UI wiring of already-tested domain logic.',
    '2026-07-14 Reference Library -> GitHub Releases (code side) and WHY Story Options (docs/adr/0039-reference-library-release-hosting-and-story-options.md). Part A, on direct request ("I need to restructure the Github repo to move the files in LFS to GitHub Releases. LFS used up all available 10GB bandwidth"): diagnosed deploy-pages.yml checking out with lfs:true on every push (pulling all ~469MB of assets/docs/*.pdf every run) as the real cause, not anything about the PDF files themselves. New COMMITTED src/data/referenceLibraryManifest.js (the permanent 29-PDF catalog, unlike the gitignored build-generated docsManifest.js) and src/data/releaseConfig.js (releaseAssetUrl(), URL-encoding filenames with spaces/parens). scripts/build.js reworked: a new isRealFile() check (exists AND isn\'t an unresolved Git LFS pointer-stub text file, detected by its standard signature) decides, per catalog entry, whether the generated manifest\'s new `src` field is the local relative path (real bytes present, today\'s exact behavior) or releaseAssetUrl(...) (missing/pointer) -- decided once per build, per machine, no runtime fallback logic anywhere. `file` stays the untouched stable identity refOverrides/tab-keys are keyed by; three consumers (documents.js resolveDocumentTab, ui/mechanicsScan.js, ui/tocScan.js) now read the new `src` field instead of reusing `file` as a fetch target -- caught and fixed mid-implementation after the first design draft would have silently orphaned a campaign\'s saved title/tag override for a PDF the moment its local copy went missing. deploy-pages.yml drops lfs:true from checkout entirely (the actual bandwidth fix) and no longer copies assets/docs/ into the deployed site; its old "curl a local PDF, expect %PDF" verification now checks a real Release asset URL instead, soft-warning (not failing the build) until the Release actually exists. No git history rewrite, no .gitattributes change, no git rm -- the already-committed PDF blobs stay in history (a static storage cost, not a recurring bandwidth one), which alone fixes the reported problem. No `gh` CLI or GitHub token available in this environment, so the actual `gh release create`/`gh release upload` steps are a short manual walkthrough handed to the user. Verified end-to-end by hand: renamed a real PDF, wrote a synthetic LFS-pointer-stub in its place, rebuilt (manifest correctly fell back to the Release URL with the catalog\'s cached size), restored the real file, rebuilt again (manifest reverted exactly, same size, local path). Part B, on direct request ("the 5-W\'s workspace is still missing a comprehensive and interactive GM guide... intuitive oracle suggestions, cumulative option-building based on who is involved, where they are located"): WHY had zero entity-selection UI before this (unlike WHO/WHERE, both upgraded earlier this session) and copilot.js\'s advise() was a single first-match-wins priority chain that never read entity data, Foreshadowing, or World Flags and never combined signals. New copilot.js exports: gatherSceneContext(campaign) (a pure snapshot reusing existing queries -- WHO\'s in-scene NPCs/Factions via the same @mention-parsing getCurrentWhereLocations already established for WHERE, WHERE\'s present factions/Conflicts-here, WHY\'s own open Threads/Foreshadowing, plus World Flags read outside WAT-tab UI for the first time) and buildStoryOptions(campaign, {limit}) (turns that snapshot into a genuinely CUMULATIVE ranked list -- an agenda-bearing faction present, an open Conflict here, and an unpaid Foreshadowing plant each contribute their OWN option in the same call, every option naming a real already-used Oracle table a GM can roll on). activity===\'negotiate\' boosts an in-scene faction\'s fear/need option above its agenda option, finally building docs/adr/0009\'s long-named-but-deferred idea, generalized to every in-scene faction rather than one hardcoded case. session.js\'s drawSuggestionLenses gained an opt-in sceneContext param (weighted lens draw, proven via a 40-seed frequency test) -- additive, the existing WHAT-tab "What Happens Next?" call site is completely unaffected; not yet wired to a new UI trigger (Phase 2). New workspace/index.js pieces: whyEntityPicker (the identical WHO/WHERE tag-listbox pattern, pooling NPC/Faction/Conflict) and storyOptionsBlock (each option\'s 🔮 rolls its linked table via the existing rollOracle, ＋ Journal drops it into the session log via the existing addNote -- both reuse generic mechanisms, Article II, nothing auto-applies). 6 new domain tests (439 total).',
    '2026-07-14 WHERE/WHO polish batch, no-viable-action diagnostics, Bases of Influence cleanup, sticky Entity Editor header, Ctrl+arrow tab nav (same-day follow-up to docs/adr/0038, several direct requests). WHO\'s "Matching people" and WHERE\'s "Matching locations" candidate panels converted from chip clouds to <select size> listboxes matching the adjacent tag listbox\'s aesthetic (new shared candidateListbox() in workspace/index.js; a "pick, act, reset" select mirroring data-conflict-faction-link\'s existing pattern, not a persistent filter). factionTurnEngine.js\'s "no viable action" narrative now names a specific reason via a new explainNoViableAction(campaign, f, attemptedAction) — no Homeworld set (blocks buyAsset/expandInfluence outright, diagnosed from a real "13 stuck rounds" report), not enough FacCreds for a first asset, or — once a faction has any active asset (sellAsset is always viable then, so "no viable action" only happens if the heuristic\'s random draw specifically landed on a narrower action instead) — a reason keyed to exactly which action was attempted and failed (attack/seizePlanet: nothing to attack; useAssetAbility: no usable ability; refitAsset: no alternative at its rating; expandInfluence: no known location; changeHomeworld: no other Base). WHERE gained: a header-row read-only quick-awareness summary (cardWithHeaderRight, a WHERE-only variant of the shared card() helper) reusing existing World Profile fields — zone->System, starSystem->Star (confusingly labeled but actually a #star Location\'s name), bases[]->Colony/Base, getContainingLocation->District; a nearbyLocationsBlock (siblings under the same immediate parent, getContainedLocations on getContainingLocation\'s result); and a storyInspirationBlock reusing the existing Site Concept/Adventure Seed oracle generators (domain/worldbuilding.js) verbatim as a second WHERE-scoped entry point to the same data-generate-site/-seed buttons. A Faction\'s Bases of Influence list gained a ✕ remove button (new removeFactionBase(campaign, factionId, locationId) — a plain list edit, no FacCred refund, homeworldId left untouched even if it points at the removed base) and now collapses under its own toggle by default once any bases exist, expanding by default when empty so the "expand influence to" picker is immediately visible (ui.basesOfInfluenceToggled, an inverted-tracking Set mirroring collapsedOverview\'s "tracks explicit closes" shape, XORed against the data-driven default). The Entity Editor\'s .inspector-head (name/type) is now position:sticky inside .mc-drawer\'s scroll box, with a negative-margin/re-pad trick to cancel out .mc-drawer\'s and .entity-inspector\'s own padding/border so it looks identical unscrolled but sticks flush at the top once scrolled ("freeze pane," direct request). Ctrl+Left/Right (onKeydown) now cycles context.active through CONTEXT_QUESTIONS (who/where/what/why/how, wrapping), skipped whenever focus is inside an input/textarea/contenteditable so it never steals the OS/browser "jump a word" shortcut from someone typing. 2 new domain tests (434 total): explainNoViableAction\'s three tiers (via direct calls for the assets-exist branches, since that state is otherwise seed-dependent luck given sellAsset\'s always-available fallback) and removeFactionBase\'s removal/no-refund/homeworldId-untouched/no-op behavior.',
    '2026-07-14 Location ↔ Faction presence, Conflicts, and a Location Story (docs/adr/0038-location-faction-story.md), on direct report that WHERE doesn\'t "select" a location and WHO can\'t associate factions with one, with no digest of what\'s happening there. Mostly a wiring job: `factionsInRegion` (the full location containment-tree "who\'s active nearby," Living Faction Engine Phase C) already existed but was wired to no UI; the generic relationship system\'s `located_at` type (target-constrained to Location, already exposed in the Entity Editor\'s own Relationships block) was an already-correct, unused way for a GM to say "this faction operates here." `factionsPresentAt`/`factionsInRegion` (factionTurnEngine.js) each gained a `viaRelationship` check recognizing a manual `located_at` link, alongside their existing asset/homeworld/Base/governed/membership signals — a strict widening, so every existing caller (the Faction Events panel, the Conflict card\'s local-faction picker) also benefits. `entities.js`\'s `ensureLocationFields` gained `locationStory` (default \'\', mirrors Faction\'s `scenarioSeed`). WHO\'s "Factions active nearby" (workspace/index.js) now queries `factionsInRegion` per current WHERE location instead of an ad hoc single-district loop, and gained a "+ faction operating here" `<select>` (mirrors the Conflict card\'s existing `data-conflict-faction-link` select-picks-then-links pattern) plus a ✕ shown only on manually-linked factions. WHERE gained four new read paths: a persistent "📍 Current location" banner (the missing "this is selected" feedback — confirmed via direct follow-up that WHERE\'s mention-based selection itself already works, just without visual confirmation), a read-only Factions-here digest (name + truncated Agenda), a read-only Conflicts-here digest (conflict.locationId match), and a new per-location rich-text "Location Story" field bound to a new `data-location-story` attribute (not `data-entity-field`, which always targets the Cast-active entity, not necessarily the WHERE-mentioned location). No new mutators anywhere — associate/un-associate/edit all reuse the existing `addRelationship`/`removeRelationship`/`updateEntity`. Verified via 1 new domain test (432 total) covering the `located_at` signal on both presence queries, plus an extended existing `ensureLocationFields` test for `locationStory`\'s default.',
    '2026-07-14 Foreshadowing, World State Flags, NPC current goal (docs/adr/0037-foreshadowing-worldflags-npc-goal.md), on direct request to review the architecture against an external "GMAtlas_Scene_Story_Data_Model.md" spec and implement improvements. Reconciled first (docs/design/scene-story-integration-plan.md): most of the spec already exists under different names (Clocks→Threads, NPC/Faction/Location tables→entities.js, Story-So-Far→recap.js, thread staleness→copilot.js), and its pre-authored branching scene graph (prerequisite/exit-condition/follow-on scenes, a scene\'s own stable identity) is a genuine philosophical mismatch — this app\'s `generateScene()` is 100% live, oracle-driven improv text with no `resolveScene()` anywhere, consistent with Article II and the "flag, don\'t auto-advance" convention throughout; building a scene graph would mean inventing the one concept this app has deliberately avoided everywhere else, so it was explicitly NOT built. Three small genuinely-missing primitives were: new `domain/foreshadowing.js` (`campaign.foreshadowing[]`, additive/lazy-init) — a GM\'s own "I planted this, remind me to pay it off" to-do list, `addForeshadowing`/`markForeshadowingPaidOff`/`removeForeshadowing`/`openForeshadowing`, dropped the spec\'s scene-id linkage (no stable id to point at) in favor of a free-text payoff note; UI is a `threads`-styled block on the WHY tab reusing the exact `.thread-row` CSS Threads already established. New `domain/worldFlags.js` (`campaign.worldFlags[]`) — a lightweight fact ledger, `unknown`/`suspected`/`confirmed`/`false` per entry (richer than the spec\'s plain bool, ignorable by a GM who only wants true/false), `addWorldFlag`/`updateWorldFlagValue`/`updateWorldFlagNotes`/`removeWorldFlag`; UI is a matching block on the WHAT tab (value `<select>` + notes `<input>` per row). New NPC `currentGoal` field (`ensureNpcFields` in entities.js, mirrors Faction\'s own `agenda` exactly) — the smallest possible slice of the spec\'s fuller NPC Roster (role/status/disposition/voice notes all postponed as unvalidated scope), one plain `data-entity-field="currentGoal"` input on a new `npcSection` in the Entity Editor. All three reuse existing mechanisms only — ADR 0022\'s inline-prompt standard for both `-add` triggers, the existing delegated click/change handlers, and the already-fully-generic `data-entity-field` handler for the NPC field (no new handler needed at all). Verified via 4 new domain tests (431 total) plus direct render-path smoke checks confirming the WHY tab\'s Foreshadowing block, the WHAT tab\'s World State Flags block, and the Entity Editor\'s Current Goal field all render correctly against real, populated campaign data.',
    '2026-07-13 Faction Conflict: Location-scoped "Involved" faction picker, closing a gap between the design doc and the shipped build. `conflict.locationId` (the "contested zone" field, docs/adr/0036) had a data-model slot but no UI control and no filtering behavior — the "Involved" factions picker just used the fully generic, unfiltered Relationships editor. Fixed: a new "+ add a local faction" dropdown on the Conflict card, filtered by `factionsPresentAt(doc, e.locationId)` when a Location is set (falls back to every faction, with a hint, when unset) — the general Relationships block still works too for linking any faction regardless (Article II, a curated convenience not a restriction). Per direct follow-up, the Location dropdown itself was relocated OFF the Conflict\'s own Entity Editor card and onto the WHO workspace tab instead (a new `activeConflictLocationPicker`, workspace/index.js, shown only while a Conflict is the active/open entity) — scoping "which factions are eligible" is a WHO-tab concern, not an entity-detail-form one; the field itself is still just `data-entity-field="locationId"`, so no new handler was needed for the relocation, only for the new faction-link dropdown (`data-conflict-faction-link`, wired via the existing `addRelationship`). Verified via direct render-path smoke checks: the picker correctly excludes/includes factions based on Location, the field is absent from the Entity Editor and present on WHO only while a Conflict is active, and the underlying data updates correctly regardless of which tab set it.',
    '2026-07-13 Faction Conflict × Faction Turn Engine escalation suggestions (docs/adr/0036 same-day follow-up), on direct question about SWN-community reception. A second, narrower research pass (SWN/OSR-specific sources, not general GM sentiment) found the escalation clock being entirely GM-clicked — disconnected from the already-faithful mechanical Faction Turn Engine — was the one likely "bolted on" gap for that audience specifically: SWN GMs run faction turns "in the background... where mechanical actions can directly damage or aid the Factions," i.e. they want dice/procedures to generate state, not just narrative judgment. New `suggestedConflictEscalations(campaign, eventIds)` (factionTurnEngine.js): given just-committed Faction Event ids, finds every Conflict entity whose `involves` relationships cover BOTH sides — an Attack\'s named defender (`event.targets[0].factionId`) or any RIVAL entry in a faction-vs-world event\'s frozen `coLocatedFactions`. A suggestion only, never applied automatically (Article II) — wired into the existing commit handler (Step and Full Round both flow through it); right after a commit, matches render as a dismissible "`<Faction>`\'s move may affect `<Conflict>`" prompt with an Escalate (ticks the clock by 1, starting it first if needed) or Dismiss (✕) action, in the same spot the draft review just was. Verified via 3 new domain tests (427 total) exercising the REAL `attack`/`expandInfluence`/`buyAsset` action functions (not synthetic fixtures) — the Attack match, the rival-matches-but-ally-doesn\'t distinction, and self-scoped events never matching — plus a full end-to-end smoke check (real attack → real suggestion → real rendered Escalate button).',
    '2026-07-13 Faction Conflict (docs/adr/0036-faction-conflict.md), on direct request to incorporate an external "FACTION-CONFLICT.md" subsystem spec into the Living Faction Engine — validated against real GM-community sentiment on faction/conflict tooling complexity before building, not just mapped onto the architecture as-is. Research (SWN\'s own faction system reviewed as "complex and fiddly, tolerated not loved"; Blades in the Dark\'s Progress Clock as the most consistently praised "track pressure without homework" pattern found; an indie TTRPG\'s public devlogs describing a faction system redesigned twice for "taking up too much headspace"; Gnome Stew\'s explicit GM complaint about "learning new rules and constantly looking stuff up") drove a validated simplification: a "hero path" (status, an escalation clock, a stated-vs-root-cause gap, a third-party-casualty line, session hooks) that alone is a usable conflict, with everything else from the source spec (deep history, irreversible facts, per-faction posture, information asymmetry, party leverage, GM notes) demoted behind an explicit "Add depth" toggle, collapsed by default. `ENTITY_TYPES` gains `\'conflict\'` — a first-class entity type (matching docs/adr/0012\'s `item` precedent), not a bespoke array, so it gets @mentions/Graph nodes/Universal Search/Cast listing for free and "which factions are involved" is just the existing relationship system (new `involves` type). The escalation clock is a Thread (`kind:\'faction-conflict-escalation\'`), reusing the exact pip UI and generic advance/back controls already built for faction goal tracks — zero new clock mechanism. A single "🎲 Quick-start" button (`generateConflictSeed`, domain/factionConflicts.js) rolls a new small `SCENE_TABLES[\'Faction Conflict\']` oracle group and drops results straight into the editable hero-path fields — no multi-step wizard. `factionPostures` simplified from the source spec\'s 5 structured sub-fields to just cohesion (0-10, this app\'s existing dial convention) + one free-text notes field, stored per-conflict (not on the shared faction entity, so the same faction can hold a different posture in two simultaneous conflicts). New `conflictSection` in the Entity Editor (mirrors `factionSection`\'s pattern exactly) and a compact Conflicts list in the Faction Events drawer. Verified via 8 new domain tests (424 total) plus direct render-path smoke checks of both the full Entity Editor card (collapsed vs. expanded depth) and the Faction Events list.',
    '2026-07-13 Living Faction Engine Phases B/C/D: activity pacing, faction-driven missions, clean turn-processing UI (docs/adr/0035-faction-engine-pacing-missions-turn-ui.md), on direct request — explicitly WITHOUT the retcon/command-log concept originally sketched for Phase D ("remove the retcon concept, keep it simple"). **Phase B**: `settings.factionPacing` (scenesPerRound/scenesSinceLastRound) increments once per scene generated (`continueStory`/`suggestNextWithLens`); new `isFactionRoundDue()`/`resetFactionPacing()` (factionTurnEngine.js) surface a Co-Pilot nudge ("consider Step or Full Round") without ever auto-committing; a new Settings control tunes the cadence (0 = off). **Phase C**: `campaign.missions[]` (domain/missions.js) — `generateMission()` gained an optional `factionId` deriving danger from that faction\'s own goal urgency instead of campaign threat; new `addMission`/`updateMissionStatus`/`removeMission`/`missionsForFaction` CRUD; `copilot.js`\'s `advise()` now names whichever faction signal is driving its observation (`hotFactionId`/`hotFactionName`) so a new Co-Pilot button ("Generate mission from X") turns narration into a real, trackable job; a new Missions section in Faction Events lists them with accept/decline/resolve/remove controls. **Phase D**: `proposeFactionTurn` now computes and permanently attaches a plain before/after `impact` diff (HP/FacCreds deltas, assets added/removed/changed) to every event — NOT a replayable command log, just a frozen summary computed once when both states are naturally on hand (the retcon design this replaces would have needed per-action decide/apply splitting and a full replay engine; cancelled outright per direct instruction). Draft/committed event cards (factionEvents.js) now show this as a structured "assets affected" list instead of only prose, plus a new "recent events at this location" panel (any faction, last 5) shown alongside a draft under review. Every faction name in Faction Events is now a `data-open-entity` link into the Entity Editor; clicking one from a specific turn also highlights that turn\'s own "Impact of this turn" line in a new "Turn History" card on the faction\'s full sheet (`factionTurnSectionHtml`, campaign-wide via `factionEventsByRound`\'s new optional per-faction filter — one function, reused for both this and the new collapsed-by-default Round History browser in Faction Events itself). Verified via 7 new domain tests (416 total) plus direct render-path smoke checks covering every new UI surface.',
    '2026-07-13 Faction Events turn-heuristic fix, Active Location scoping for Full Round, activity-only Roster card, and whole-card WHERE docking, all on direct follow-up report/request. **Real bug fixed**: `candidateActions` (factionTurnEngine.js) never gated the self-heal Repair action on affordability the way every other candidate action already does — a 0-FacCred faction whose max HP had risen above its current HP (a stat raised after creation; entities.js\'s setFactionStat deliberately never auto-heals that gap) got stuck proposing a guaranteed-fail Repair as its ONLY candidate, forever, so Step/Full Round looked broken ("same result every time"). Now gated on `facCreds >= 1` like buyAsset/expandInfluence; a genuinely resourceless faction gets an honest "no viable action this turn" instead. `advanceFactionTurnRound` gained an optional `factionIds` param so Full Round (previously proposing for every faction in the campaign regardless of location) now scopes to `factionsPresentAt` WHERE\'s own Active Location, exactly like Step already did — with a toast prompting a Location pick on WHERE first if none is set. Faction Events\' Roster card is now a genuinely narrow "current activity + social/political ramifications" summary (current goal + progress, busy/seizing status, HP/FacCreds as read-only context, live ally/rival stance toward whoever else is at the Active Location, and a mini feed of this faction\'s own recent events) — the full stat/asset-editing card (`factionTurnSectionHtml`) is Entity Editor-only now, reached via a new "Open in Entity Editor →" button (`data-open-entity`, the same universal entity-chip mechanism every other part of the app already uses) — direct request: "Factions should display everything... in the Entities Editor, but Faction Events should only include... current activities and social and political ramifications." Whole-card relocation: a new down-arrow on the Faction Events card\'s own top row (`data-faction-events-dock`) closes its drawer tab and docks the exact same `renderFactionEvents()` output as a new right-hand column inside the WHERE workspace tab (`workspace/index.js`\'s `where()`, new `.workspace-with-side`/`.workspace-docked-panel` CSS, collapsing to one column under the existing 1023px breakpoint); an up-arrow there (`data-faction-events-undock`) pops it back to the drawer tab group. Only one of "drawer tab" / "docked in WHERE" is ever active — `factionEventsDockedInWhere` (new shell.js ephemeral flag) gates it, threaded through `buildDrawerUi()` into both `renderWorkspace` and the drawer\'s own `renderActiveDrawerHtml`. Verified via 2 new domain tests (409 total: the Repair-affordability regression across 5 rng seeds, `advanceFactionTurnRound`\'s factionIds scoping leaving an excluded faction\'s state untouched) plus direct render-path smoke checks (the narrow Roster card excluding every raw stat/asset control, the dock/undock arrows appearing in the right mode, and the WHERE view\'s two-column layout only appearing when docked).',
    '2026-07-13 Faction Events UI refinement, on direct request following the Living Faction Engine Phase A work: (1) Removed the per-faction Rules Provider selector from the shared Faction Turn card (`factionTurnSectionHtml`, drawers/index.js) entirely — which ruleset a faction uses is now a Settings-only decision (Rules Constitution\'s campaign-wide default), not offered as a second per-faction control; the dead `data-faction-provider-select` change handler in shell.js was removed too. The per-faction `rulesProvider` entity field and its resolution order in `factionProviderFor` are untouched (a campaign that already set one keeps working), just no longer editable from this card. (2) `factionTurnSectionHtml` gained an `{ showHomeworld = true }` option — Faction Events\' Roster card now passes `false` (Homeworld is entity-editor territory, per direct request; the Cast inspector\'s own call site is unaffected and keeps showing it). (3) The Faction Events panel now scopes its Step-select and Faction Roster to `factionsPresentAt` WHERE\'s own Active Location (new `factionTurnEngine.js` export, checking asset/homeworld/Base/governed/member_of presence at one exact location — the single-location, no-exclusions sibling of `factionsAtLocation`/`factionsInRegion`) instead of listing every faction in the campaign; a clear message prompts selecting a Location on WHERE first when none is set, and the list updates automatically as WHERE changes (nothing to manually sync). The committed Events feed\'s own faction/location filter dropdowns are unaffected in breadth (still browse any faction/location\'s history) — only their addition, the location filter, now *defaults* to the Active Location rather than "All locations," via a new `null` "not yet touched this session" sentinel (`factionEventsLocationFilterId`) that stops auto-following WHERE the moment a GM explicitly picks a value (including an explicit "All locations"). (4) The Step select\'s chosen faction now persists as visible ephemeral state (`factionEventsStepFactionId`, new shell.js variable + change handler) — a "Selected: **Name**" line stays under the controls until a different faction is picked, instead of the picker being read directly off the DOM at click time with no persistent confirmation. Verified via 1 new domain test (407 total: `factionsPresentAt`\'s four presence paths plus its empty/null cases) and a targeted Node smoke script exercising `renderFactionEvents` directly against constructed campaign data (no-Active-Location messaging + disabled Step select, Active-Location scoping correctly including/excluding factions, Rules Provider gone, Homeworld hidden in the Roster card specifically, the selected-faction label, and the location filter\'s live default) — `npm test` and `node scripts/build.js` both stay green.',
    '2026-07-13 Living Faction Engine, Phase A: universal faction membership, conquest flips, region-depth queries, faction dossier (docs/adr/0034-faction-membership-and-region-depth.md). First of a four-phase roadmap (A: this pass; B: activity-based pacing; C: faction-driven missions/encounters; D: retcon support) making docs/adr/0031/0032\'s SWN Faction Turn Engine behave as a fully independent, living regional engine per direct request — factions with their own destiny, territory that structurally changes hands, every entity belonging to a faction "even if bystander," and location as the central factor in regional faction activity. New `entities.js` `getEntityFaction(campaign, entityId)`: resolves a real `member_of` edge, or returns a synthetic non-persisted `{id:null, name:\'Unaligned\', synthetic:true}` fallback — no forced migration touching every existing entity, same "derive on read, store nothing" posture `isRelationshipFlagged` already established. New `setEntityFactionMembership(campaign, entityId, factionId)` replaces any existing `member_of` edge outright (never doubles it). `factionTurnEngine.js`\'s `seizePlanet` now calls it on the seized location at both completion points, in addition to its existing `governedLocationIds` append — a conquered location\'s faction membership is now a real, structural fact, not just an entry in the conquering faction\'s own array, and the committed event\'s narrative says so explicitly ("X is now under Y\'s control"). New `factionsInRegion(campaign, locationId, {anchorFactionId, maxDepth})`: the deep-traversal counterpart to the existing single-hop-only `factionsAtLocation`/`isSameDistrict` — walks the FULL `located_at`/`contains` ancestor+descendant tree (visited-set guarded against a cycle) to answer "which factions are active anywhere in this region," the backbone Phase C\'s faction-driven mission/encounter generation will read from. New `getFactionDossier(campaign, factionId)`: a read-only rollup of a faction\'s real member entities, governed locations, current goal+progress, allies/rivals, and its own slice of the Faction Events log — assembling data already tracked elsewhere into the single view "make the faction forms track all faction history, NPCs, assets, colonies, and storyline" asked for, no new stored record. Verified via 6 new domain tests (406 total): the synthetic-fallback and type-degradation paths, membership replace-not-duplicate, `seizePlanet`\'s membership flip including replacing a prior owner, `factionsInRegion` finding a faction two structural hops away plus a cyclic-fixture non-hang check, and the full dossier aggregation shape.',
    '2026-07-10 Mobile-responsive UI: tab unification, phone breakpoint, touch-drag hover (docs/adr/0033-mobile-responsive-tab-unification.md), on direct request that every tab fit the window on a phone/tablet with no horizontal scroll. Removed the general-purpose drawer-anchor mechanism (`anchorDrawerTab`/`unanchorDrawerTab`, `.mc-drawer-anchor`, the tab strip\'s ⇤ icon) entirely — a drawer only ever switches or closes now, never pins beside a sibling. Faction Events (docs/adr/0031/0032) is now an ordinary `DRAWERS` tab instead of its own left-anchored fixed panel — still rendered via its own pure function (circular-import avoidance with `factionTurnSectionHtml`), but driven by the same `openDrawers`/`activeDrawer` every other drawer uses. The document viewer and the main drawer are now mutually exclusive (`.mc-doc-viewer` always full-width, `.mc-drawer` hides while any document tab is open) instead of sitting side by side with an overlap inset. New `@media (max-width: 480px)` tier in `styles/cockpit.css` (layered under the existing 1023px/767px tiers): the one remaining side panel drops its 88vw cap for full width; several fixed-column "table row" grids (Colony crew roster, Party trackers, Threads, Trade contracts, the relationship-add row) collapse to one column; `.statblock-row` switches to the same compact label-over-value badge shape `.attr-badge` already proved out on Party cards (~5 fields/row instead of a 2-column list). `styles/tokens.css` gained a matching denser `--sp-*` spacing override at the same tier. `settings.toolbarCollapsedByDefault` schema default flipped `false`→`true` (additive — an existing explicit value is untouched). Touch-drag (the existing `onTouchStart`/`onTouchMove`/`onTouchEnd` system, already recognizing Cast\'s `[data-drag-entity]` rows) gained hover-to-switch-tab and hover-to-reveal-Mission-Control: dragging over a different tab or the header for 500ms switches to it / collapses the drawer WITHOUT ending the drag, so a field that was hidden when the drag started can still be the eventual drop target. Verification incidentally caught and fixed a real bug: `data/factionRulesProviders.js` (added earlier the same day) used `import * as X from` syntax `scripts/build.js`\'s regex-based import stripper doesn\'t recognize — `node scripts/build.js` reported success while actually emitting a broken bundle (a raw `import` statement inside what must be a classic script); fixed by switching to named imports, the convention every other file already uses. This is a UI/CSS-only change (no domain-layer logic), so `npm test`\'s 401 tests are unaffected and don\'t cover it — verified via `node scripts/build.js`, `npm test` staying green, and direct code-path review; a real narrow-viewport/touch-emulated browser pass is recommended as a follow-up (not completed in this session due to environment tooling constraints around loading the full built app in a headless jsdom context).',
    '2026-07-10 GMAtlas Core faction provider, Game System Activation, event scope/regional responses, read-aloud narrative, WHAT-tab hook (docs/adr/0032-gmatlas-core-faction-provider.md, extending docs/adr/0031), on direct request to make Faction Events show every SWN option/flag/status editable, offer a copyright-safe original-content alternative to SWN\'s transcribed data switchable in Settings, and — on follow-up — gate SWN behind an activation checkbox, classify committed events by scope with logged regional reactions, generate GM-editable read-aloud text, and ripple faction activity into the WHAT tab. New `data/gmatlasFactionData.js`: a full, original-content mirror of SWN\'s 72 assets/20 tags/11 goals — identical ratings/HP/cost/dice/difficulty formulas (mechanics aren\'t copyrightable), every name/flavor text original writing. New `data/factionRulesProviders.js` resolves a faction\'s catalog via `rulesProvider` (per-faction override) → `settings.rulesProviderChoices.factions` (campaign default) → `\'swn\'` (unchanged fallback); `domain/factionTurnEngine.js`\'s every catalog lookup now goes through it, including `attack` resolving each side\'s OWN provider independently (a mixed-provider Attack works). New engine pieces: `toggleAssetStealth` (manual flag, no dice), event `scope` (self/faction-vs-faction/faction-vs-world, a plain lookup table), `generateFactionResponses` (one stance-templated reaction per co-located faction, logged as `event.responses`, only for faction-vs-world events — not separate feed entries), `expandEventReadAloud` (a pure, deterministic 2-4 sentence paragraph composer, same technique `scenes.js` uses, stored as GM-editable `event.readAloud` via `setEventReadAloud`), and a WHAT-tab hook (`pushEvent` nudges `context.what.threat` +1, clamped, for a witnessed non-failure faction-vs-world event — mirrors `continueStory()`\'s "consequences gently escalate" heuristic; `copilot.js` gained a matching observation branch). Game System Activation: `RULES_PROVIDERS.swn` gained `requiresActivation`/`activationText`; `isGameSystemActivated()` is the one gate function everything reads through (the documented seam a future real licensing check replaces); `settings.gameSystemActivations` defaults `{swn:false}` for a fresh campaign, but `migrate.js` grandfathers it to `true` for any campaign that already has real SWN faction-turn data, so nothing already built breaks. Rules Constitution (`GAMEPLAY_AREAS` gained stable `id`s) is now real `<select>`s on all 19 areas (`resolveProviderChoice`) — only the Factions row changes actual behavior today, every other row records a stated preference for the still-future Phase 9 Rules Lens. The Faction Turn card (`factionTurnSectionHtml`, now exported) gained a Rules Provider selector, Governed Worlds/Seize Progress/Busy-Transit/missed-maintenance status, and Refit/Change Homeworld/Stealth-toggle controls that were previously wired in the engine but unreachable in the UI; HP/FacCreds/XP/Homeworld moved off the "active entity"-only `data-entity-field` onto an explicit-id `data-faction-field`, so the SAME card now also renders inside Faction Events\' new "Faction Roster" section (compact list, or one faction\'s full live card on demand) without duplicating any markup. Verified via 20 new tests (401 total): GMAtlas Core\'s data integrity and position-for-position mechanical equality with SWN, provider resolution order including a mixed-provider Attack, all new engine functions, the Co-Pilot signal, and all three Game System Activation migration paths (fresh/grandfathered/already-explicit).',
    '2026-07-09 Faction Events follow-up (docs/adr/0031 addendum), on direct request that a faction turn account for "which factions are presently active around the current star system or region," "same or different district than where the players are," and weight ally/opponent decisions "on the overall relationship rating between the two factions." Renamed "Faction Log" → "Faction Events" throughout (schema `factionLog`→`factionEvents`, `ui/drawers/factionLog.js`→`factionEvents.js`, all `data-faction-log-*`/`.mc-faction-log` renamed) — the user\'s own term, and a better name now that events are per-location. Every event is now a Faction-Location pair: `locationId`, `coLocatedFactions` (every other faction present there, tagged ally/rival/neutral), and `witnessed` — all frozen at propose time. New `entities.js` `getRelationshipBetween` (a real gap — every relationship lookup before this was inline) backs new `relationshipStanceBetween` (explicit allied_with/rival_of wins; otherwise the existing 0-10 strength dial, >=7 ally/<=3 rival; else neutral) — `rivalAssetsAt` generalized into `factionsAtLocation`, and Attack/Seize Planet auto-targeting now never picks an ally, only a rival (falling back to neutral). District support reuses the existing but previously-unused `contains`/`located_at` relationship pair (new `getContainingLocation`/`getContainedLocations`/`isSameDistrict`, one-hop). "Current location" for witnessed-vs-news is parsed from WHERE\'s own Focus `@mention`s (new `getCurrentWhereLocations`), per direct confirmation — not a revived structured pointer, consistent with a past redesign that removed one as duplicative. WHO gained a "Factions active nearby" summary, WHERE a "Faction activity here" summary, both with jump chips into the Events panel (now filterable by location as well as faction). The panel\'s trigger moved from a header button to a new edge-nav slot between Cast and Trade — opening it also anchors Cast filtered to Faction (`anchorDrawerTab(\'cast\')`, the same call Cast\'s own button already makes), independent lifecycles once both are open. Verified via 8 new domain tests (380 total) plus a direct render-path check against a real three-faction (one ally, one rival) scenario confirming witnessed/coLocated framing and the new WHO/WHERE/panel sections all render correctly.',
    '2026-07-09 SWN Faction Turn Engine (docs/adr/0031-swn-faction-turn-engine.md), on request to "Design and build the interface for playing the SWN faction turn mechanics leveraging the WHO-WHERE-WHAT-WHY-HOW tabs... an automated turn sequence that can be advanced in full rounds... or step-by-step." A full, playable Stars Without Number faction-turn system, distinct from `domain/factions.js`\'s existing lighter mini-game (kept, untouched). Per direct confirmation this transcribes SWN\'s real content in full — 72 named assets across Force/Cunning/Wealth (ratings 1-8), 20 faction tags, 11 goals, the XP cost table (new `data/swnFactionData.js`) — explicitly superseding ADR 0011\'s "stay proprietary" stance for factions specifically. New `domain/factionTurnEngine.js`: all 9 SWN actions (Attack/Buy/Sell/Repair/Refit Asset, Expand Influence, Change Homeworld, Seize Planet, Use Asset Ability), turn bookkeeping (FacCred income/maintenance, initiative order), and Goal tracking (reuses Threads exactly like the existing Pressure Track, via a new `kind:\'faction-goal\'`). Automation is propose-then-confirm, not autonomous: Step/Full Round compute a fully-resolved draft — goal, action, targets, every die roll — that the GM reviews and commits; a round\'s drafts chain against each other and commit as one all-or-nothing batch. Per a direct follow-up request, the Faction Log is a left-anchored panel that "opens and resizes like the PDF viewer" — modeled directly on the in-app doc viewer\'s own fixed-panel mechanism (`.mc-doc-viewer`), not the normal `DRAWERS` tab stack — with Step/Full Round controls, a draft review list, and a filterable committed-events feed (new `ui/drawers/factionLog.js`). The Faction inspector gained a "Faction Turn (SWN)" card (HP/FacCreds/XP, Homeworld, Bases of Influence, Tags, Goal + clock, structured Assets with Repair/Sell/Use Ability). Three scope limits called out explicitly in the ADR: Seize Planet is a single-turn HP-pool approximation of the book\'s multi-turn siege; Change Homeworld always takes one turn (no hex-distance modeling); Use Asset Ability mechanically resolves only ~5 simple dice-for-FacCreds abilities, every other asset\'s special text is surfaced for the GM to adjudicate directly, matching SWN\'s own stated philosophy that the system isn\'t meant to run without GM involvement. Verified via 30 new domain tests (373 total) plus a direct render-path check exercising the real production code (inspector card, Faction Log panel, and the full propose→review→commit→feed→filter pipeline against real domain data).',
    '2026-07-09 Fomalhaut Settlement Zone added to HOSTILE Canon Locations (docs/adr/0026 sixth addendum), the second zone on that ADR\'s rollout checklist, on request to "create the importable location database for FOMALHAUT SETTLEMENT ZONE." 24 worlds, 24 stars, 1 zone, transcribed from the sourcebook\'s FOM World Data table (p.29), star catalog (pp.50-53), and "Outer Rim Worlds - Highlights" page (p.75, which gives real prose for Fomalhaut, Medusa, and the stars of LR210/LR203 — the other 20 worlds get the same honest "cataloged with minimal detail beyond its World Data table entry" summary this ADR already used for six sparse Near Earth Zone worlds). No bases catalog entries — FOM\'s zone map only shows generic base-type icons, not the per-world national USSC/JASDF/DRW/MRA attribution NEZ\'s prose named, so none were invented. New `assets/data-packs/hostile-fomalhaut-settlement-zone.json`, same `{zones,bases,stars,locations}` shape as the Near Earth Zone pack, kept as its own file rather than merged into it. `ui/hostileLocationsFetch.js`\'s `fetchHostileLocationsPack()` now fetches every URL in a `PACK_URLS` array and merges the results — `domain/hostileLocations.js`\'s `importHostileLocations()` needed no changes, and Settings\' single "🌍 Import HOSTILE Canon Locations" button now covers both zones with no UI change; a future zone only needs another file + another `PACK_URLS` entry. `HOSTILE_LOCATIONS_META` updated to the combined totals (54 worlds, 54 stars, 4 bases). Verified via 4 new domain tests (354 total): the FOM pack\'s shape, no name collisions against the NEZ pack, a FOM-only import linking the Zone->Star->World containment chain, and a merged-pack import producing the union of both zones\' entities — matching what the real fetch-and-merge does.',
    '2026-07-09 Color picker in rich-text toolbars, closing out this batch\'s "Add color picker to all the text editors" ask. Extends ADR 0018\'s lightweight markup with one more parameterized delimiter, following the exact shape `[label](url)` already established: `[label](color:#hex)`. `domain/documents.js`\'s `parseInlineNodes` branches on a `color:` prefix inside the existing bracket-scan (checked before the url path, since a real url is never literally `color:...`); a new `sanitizeColorValue()` accepts hex only (3/6/8-digit `#...` — exactly what a native `<input type="color">` always produces), rejecting anything else outright so nothing resembling a CSS injection can ever reach a rendered `style` attribute — mirrors `sanitizeExternalLinkUrl`\'s posture for the Link button. `renderInlineNodes` (mentionEditor.js) renders `<span class="rt-color" data-color="#hex" style="color:#hex">` — the color is read back from `data-color` on serialize, not the DOM\'s own `style.color`, since a browser\'s CSSOM commonly normalizes an inline hex color to `rgb(...)` once parsed into real DOM, which would have silently mangled every round-trip through this app\'s own render/serialize cycle. `richToolbarHTML` gained a 🎨 button plus a visually-hidden (not `display:none`, which blocks the native picker in most engines) `<input type="color">`; clicking 🎨 captures the field\'s current Range (mousedown, same reasoning the Link button already established — by the time the native picker closes and fires its own `change`, focus has moved away) and triggers the hidden input\'s picker via `.click()`; the `change` handler restores that Range into the live Selection and reuses the existing `wrapSelectionWithMarkup` Bold/Italic/Underline already share. Verified via 5 new domain tests (350 total: hex validation/rejection, a color node parsing with a nested-bold label, an invalid value falling back to literal bracket text) plus a jsdom smoke test covering the full circuit — render produces the right span/attributes, a round-trip through `buildMentionEditorHTML`→DOM→`serializeMentionEditor` reproduces the original markup exactly, and a real mousedown-on-🎨→native-input-`change`→insertion sequence against the built bundle correctly wraps a live text selection.',
    '2026-07-09 HOSTILE Locations catalog converted to a fetched JSON pack (docs/adr/0026 fifth addendum), on request to "Convert the Locations database done in javascript into a gamesystem specific import JSON file that does not delete existing content." The ~1000-line HOSTILE_BASES/HOSTILE_ZONES/HOSTILE_STARS/HOSTILE_LOCATIONS catalog moved out of bundled JS entirely into `assets/data-packs/hostile-near-earth-zone.json` (an exact structural export, `{zones, bases, stars, locations}`), shrinking the shipped bundle by ~23KB. `domain/hostileLocations.js`\'s `importHostileLocations()` now takes the pack as a second argument instead of importing its own data — its dedup-by-name safety and Contains/Located-At relationship-linking logic are byte-for-byte unchanged, so re-running the import is exactly as safe as before ("does not delete existing content" holds). A new `ui/hostileLocationsFetch.js` (not `domain/`, since `fetch()` isn\'t pure/synchronous) does the actual network call at import-click time, mirroring `ui/mechanicsScan.js`\'s established `file://`-is-blocked guard — this feature joins Mechanics Index/TOC scan as needing `npm run serve`; every other feature still works over a plain `file://` double-click. A new tiny `data/hostileLocationsMeta.js` keeps just the world/star/base counts as bundled JS so Settings\' legend text renders without a network round-trip. The old `data/hostileLocations.js` JS file (and its two unused-elsewhere `findHostile*` helpers) is deleted — its content lives on verbatim as the JSON pack. Verified via 5 new/updated domain tests (345 total — tests read the JSON pack off disk with plain `fs`, standing in for a real fetch) plus two jsdom smoke tests: the `file://` guard\'s error message and a real HTTP fetch-then-import round trip against a local static server (65 entities created correctly), and Settings\' legend/button still rendering correctly from the new metadata file.',
    '2026-07-09 Content Pack import/export (Entities / Guide docs / Journal), on request to "Allow import/export of just entities, Guide entries, and Journal so custom sets could be added to existing campaigns." A new, generic `domain/contentPack.js` — distinct from the existing whole-campaign `store.export()`/`store.import()` (REPLACES the campaign) and from `importHostileLocations()` (a curated catalog with name-dedup, safe to re-run): `exportContentPack(campaign, {entities, guide, journal})` includes only the sections whose flag is true (a section key is entirely absent otherwise, not an empty array), stripping an entity\'s `thumbnailId` since no Gallery data is exported alongside it; `importContentPack(campaign, pack)` is always additive — no dedup, a fresh id for every entity/guide-doc/journal-entry, remapped through an old→new id map so the pack\'s own relationships/guide-tree survive (an entity relationship whose target wasn\'t also in the pack is dropped; a guide doc whose parent wasn\'t also in the pack becomes a new root instead of dangling). Settings\' General tab gained a new "Content Packs" group directly beneath the existing whole-campaign Export/Import buttons — three checkboxes (Entities/Guide docs/Journal entries, `ui.contentPackFlags`, ephemeral) plus Export (Blob-download, same pattern the campaign export already used) and Import (file picker, validates `pack.app === \'GMAtlas\' && pack.kind === \'content-pack\'` before touching the store, toasting on anything else). Verified via 5 new domain tests (344 total: export section-gating + thumbnailId stripping, import\'s fresh-id relationship remap + out-of-pack-target dropping, guide re-parenting to a new root when a doc\'s parent wasn\'t also imported, additive journal append with no id collisions, and a no-op on a malformed pack) plus a jsdom smoke test confirming the checkboxes render/toggle/persist across re-render, the export button produces a real Blob with the correct `{app, kind, ...}` shape, and the import file input is wired.',
    '2026-07-09 Sourcebook Inventory (Settings → Genre & Rules), on request to "keep an inventory in a tab in Settings for Sourcebooks that indicate what third party content is available for use in the Core." New `data/sourcebookInventory.js` — a curated, hand-maintained status/note per real PDF in the Reference Library (assets/docs/, auto-scanned into DOCS_MANIFEST at build time), same RULES_PROVIDERS-shaped label/status/note this Settings tab\'s existing Rules Constitution legend already uses, just keyed by file path instead of gameplay-area provider. All 27 current sourcebooks are covered honestly — "integrated"/"...authored (original)" where a real pass mined the book (Gear Catalog\'s five-system research, ADR 0012; SWN/CWN, ADR 0010/0011; the new Starforged Oracles group, ADR 0030; etc.), "cited, not yet mined" where a book is referenced as inspiration in an ADR but nothing\'s built from it yet (the three Intergalactic Space Trader PDFs, Hostile\'s colony-builder), "in library, not yet used" for the rest. A manifest entry with no curated row yet still renders, flagged "not yet reviewed," so a freshly-added PDF is never silently invisible. Verified via 1 new domain test (339 total — every curated `file` resolves to a real current DOCS_MANIFEST entry, catching any future rename/removal that would otherwise leave a stale, silently-dead row) plus a jsdom smoke test confirming the section renders under the Genre & Rules tab with all entries present and none falling back to "not yet reviewed."',
    '2026-07-09 Starforged/StarSmith-inspired oracle content + source labels (docs/adr/0030-starforged-starsmith-oracles.md), on request to "Add Oracles specific to Starforged and StarSmith with appropriate tags and list them as their \'oracle name (source)\'." A new `SCENE_TABLES[\'Starforged Oracles\']` group (data/tables.js) ships two original tables at the same ~15-entry scale as the existing Stars Without Number precedent — Plot Twist and Combat Gambit — original re-implementations of well-known Ironsworn/Starforged oracle CONCEPTS, not transcribed from the real Starforged/StarSmith PDFs already in assets/docs/ (same honesty posture as ADR 0010/0011). Filed under the existing "📚 Story Beats" category (data/oracleGroups.js) rather than a new tab — a prior AskUserQuestion confirmed one merged tree stays simpler than a second per-system navigation mode. A new `ORACLE_TABLE_SOURCES` lookup renders `${label} (${source})` for a top-level oracle group at display time only (`oracleGroupRow`, drawers/index.js) — `node.label`/`node.path` themselves are untouched, so roll dispatch and search are unaffected. Verified via 1 new domain test (338 total) plus a jsdom smoke test confirming the group appears under a "Plot Twist" search with its source suffix, and that searching "Stars Without Number" still shows the existing SWN group with its own (SWN) suffix, unaffected.',
    '2026-07-09 Battlemap pan/zoom camera, on request for "an unlimited background to move around and place tokens." Rendering model changed: the background/grid/icons now live in a `.battlemap-world` layer inside a fixed-size, overflow:hidden `.battlemap-viewport` (was one `.battlemap-canvas` doing both jobs), with `transform: translate(x,y) scale(scale)` applied to the world layer — icons keep their existing 0-1-fraction-of-the-box positioning unchanged (now relative to the unscaled world layer instead of the viewport), panning/zooming for free since they share the transformed parent. Camera state (`battlemapCamera`, shell.js) is ephemeral and resets on map switch, mirroring the Graph drawer\'s own `graphView` zoom/pan exactly in spirit — cursor-anchored wheel-zoom (the same screen point stays under the cursor after scaling), drag-to-pan (mousedown+mousemove, excluding icon drags and while an icon is armed from the palette), and +/-/reset buttons, all sharing the app\'s single delegated wheel/mousedown/mousemove/mouseup listeners (branching on `.battlemap-viewport` alongside the existing `.graph-svg` checks, not new listeners). The two places that convert a click/drop\'s clientX/Y into an icon\'s stored fraction (click-to-place, `completeBattlemapDrop`) now route through one shared `screenToWorldFraction()` that inverts the current camera transform, so placement stays accurate at any pan/zoom level; `domain/battlemaps.js`\'s existing `clampFraction` still guards the stored value, unchanged. Verified via `npm test` (337/337, no schema change — camera is ephemeral) and a jsdom smoke test confirming the new viewport/world markup, the identity-transform default, and a custom camera value rendering correctly; the zoom-to-cursor and pan math were additionally verified in isolation (the critical "same screen point stays under the cursor through a zoom" invariant holds exactly).',
    '2026-07-09 Trade/Contracts batch: (1) **Real bug fixed**: the Battlemap background upload set the Gallery image\'s title to the raw file.name instead of running it through titleFromFilename() (the same helper Gallery\'s own "+ Upload" flow already used) — the background picker dropdown was showing literal filenames instead of friendly names. (2) Trade gained a Location tag filter (a `<select>` sourced from listTagVocabulary(doc,\'location\'), narrowing the Location dropdown below it — same tag-vocabulary source WHO/WHERE\'s pickers already use, just a single-select here since Trade only needs one Location at a time). (3) A contract\'s name is now a collapse toggle (collapsed to just name + payout by default — ui.expandedContracts — everything else, including the new fields below, is behind it) instead of always showing the full row; the same contractRow()/contractsSection() render on both Trade and Party (from the earlier batch), so this applies in both places for free. (4) Contracts gained three free-text flavor fields — Description, Conflict, Opportunity (domain/trade.js\'s createContract/updateContract, same opt-in posture as Faction\'s HQ/Leadership/Agenda). Verified via 1 new domain test (337 total) plus a jsdom smoke test covering the tag filter narrowing the Location list, contract collapse/expand, the three new fields rendering and holding their values, and the shared contractRow rendering identically on Party.',
    '2026-07-09 Journal/entity polish batch: (1) The Journal drawer\'s action row (Add note, Export, Generate Mission, Advance Faction Turns, Creature Concept, Site Concept, Adventure Seed) is now behind an "Actions" ▸/▾ toggle — defaults OPEN (unlike most of this session\'s collapsibles) since Add Note is the single most common Journal action. (2) A journal entry\'s ✎/✕ icons are now wrapped in their own right-aligned span instead of drifting across the row via a 3-way space-between. (3) An entity\'s own tags (tagEditor) are now clickable — clicking one sets Cast\'s tag filter to just that tag and opens/switches to Cast (data-entity-tag-jump, reusing entityTagFilters/openDrawerTab, no new filter mechanism). (4) Tag chips render smaller (0.78rem → 0.68rem, tighter padding). (5) The entity Photo slot now reserves 40% of the photo row\'s width even when unassigned (a dashed placeholder box, .inspector-photo-empty) instead of collapsing to a bare button — the layout no longer jumps once a photo is added. (6) Overview is now collapsible (▸/▾, defaults OPEN like Journal Actions — it\'s an entity\'s core summary, not a rarely-needed detail) and its label dropped the "(shared)" suffix. Verified via `npm test` (336/336, unaffected) and a jsdom smoke test covering all six changes.',
    '2026-07-08 Colony/Party/Trade UX batch: (1) Both Party Roster and Colony\'s Crew Roster gained a "+ Add NPC" button (neither had a create action before — Party only said "tag an NPC in Cast"; Colony could only assign an existing NPC via dropdown) — Party\'s creates and tags #character in one step; Colony\'s creates, tags, AND adds the matching crew row in one step. (2) Cargo Manifest and Contracts (previously Trade-only) now also render on Party, via two extracted helpers (cargoManifestSection/contractsSection, drawers/index.js) Trade calls too — same party-wide data, zero new click handlers needed (every control already reads by attribute selector). (3) A Party member\'s name is now a collapse toggle (statblocks hidden by default, click to show one at a time — ui.expandedPartyMembers) instead of the whole card opening the entity editor on any click; a dedicated ↗ icon, right-aligned on the name\'s row, opens the editor explicitly. (4) Party gained a Shared Gear rich-text field (party.sharedGear), a "+ Vehicle" quick-create (a blank Asset entity tagged #vehicle — shows up in Colony\'s existing Vehicle/Asset dropdown immediately), and a separate Shared Assets chip list (party.sharedAssets, add/remove — new domain/party.js functions setPartySharedGear/addPartySharedAsset/removePartySharedAsset, mirroring addPartyTracker\'s party-level shape). Verified via 2 new domain tests (336 total) plus a jsdom smoke test covering both Add-NPC flows end to end (mirroring the exact domain call sequence each handler makes), the member collapse/expand and open-icon separation, Cargo Manifest/Contracts appearing identically on both Trade and Party, and Shared Gear/Vehicle/Shared Assets rendering and round-tripping.',
    '2026-07-08 Editor UX batch: (1) The WHAT tab\'s Latest Scene fields (Opening/Driver/Clue/Complication/Decision point/Likely consequence/Current thread) now collapse to just their label by default — click the label to expand just that one (workspace/index.js\'s sceneField, ui.expandedSceneFields, ephemeral, same Set shape as drawers/index.js\'s expandedEnhancements). (2) Every rich-text formatting toolbar (Overview, Journal, Faction Scenario Seed/Agenda, Colony fields, Guide, Document content, WHO/WHERE/WHY/HOW Focus, WHAT Situation — 11 call sites) gained its own ▸/▾ collapse icon (mentionEditor.js\'s richToolbarHTML(key, collapsed) — key identifies the instance, collapsed is resolved by the new toolbarCollapsed(doc, ui, key) helper, which XORs an ephemeral per-key override (ui.collapsedToolbars) against a new persisted default). (3) That default is now a real Settings option ("Rich-text formatting toolbars start collapsed," settings.toolbarCollapsedByDefault, defaults false) in a new "Editor preferences" group. (4) Settings itself is now 4 topical tabs — General (Campaign/Data/Editor preferences/Companion tools/Build), Genre & Rules (Genre lens/Stat system/Statblock Templates/Rules Constitution), Trade & Economy (Trade Economy Model/HOSTILE Canon Locations), Reference Tools (Mechanics Index/Table of Contents) — a pure regrouping of the same 12 sections, ui.settingsTab ephemeral. Verified via `npm test` (334/334, unaffected — schema-additive only) and a jsdom smoke test covering scene-field collapse/expand, toolbar default + per-instance override, and tab switching showing/hiding the right sections.',
    '2026-07-08 Small nav/Settings cleanup batch: removed the entity/link/note-count badges from the edge nav and header drawer tabs (shell.js, dead .badge CSS rules removed too) and the "N entities · N links" line from the Graph drawer — a GM reads relative activity from the drawer content itself, not a running tally on the button. Removed the Settings edge-nav button entirely (EDGE_ORDER) — the header gear menu\'s "Settings" entry is the only trigger now (confirmed New Campaign was already Settings-only from an earlier batch, no change needed there). Settings\' Rules Constitution legend no longer renders each provider\'s long citation `note` text (still present in data/rulesConstitution.js for anyone reading the source, just not displayed inline) — only the label/status line remains, decluttering a panel that had grown a full paragraph per provider. Verified via `npm test` (334/334, unaffected) and a jsdom smoke test.',
    '2026-07-08 Zone/Star/World/Base containment as real relationships (docs/adr/0026 fourth addendum), on request to recommend and apply the clearest way to show this hierarchy in the entity tracker and Graph — recommended reusing the existing Relationships/Graph mechanism (no new hierarchy concept) with one consistent Contains-down/Located-At-up pattern across every pair, accepted and implemented. RELATIONSHIP_TYPES gained `contains` (entities.js); the Relationships add-row now asks for the type before the target entity (drawers/index.js, no shell.js change needed — the handler already read both by attribute selector). A new HOSTILE_ZONES catalog (data/hostileLocations.js, one entry: "Near Earth Zone", tagged #zone) sits above stars; import order is now Bases, Zones, Stars, Worlds. domain/hostileLocations.js\'s new linkContains(campaign, parentName, childName) replaces the previous orbits/located_at loop — sets the parent\'s edge to Contains and explicitly retypes the child\'s mirrored edge to Located At (the default mirror is generic "linked," which needed overriding) — called for every Zone→Star, Star→World, and World→Base pair; a Base entity like USSC ends up with one Located At edge per world it has a presence at. Verified via a rewritten domain test asserting exact type/label on both sides of all three pair kinds (334 total) plus a jsdom smoke test confirming the same chain and the Relationships row\'s new field order.',
    '2026-07-08 HOSTILE Canon Locations third follow-up (docs/adr/0026 addendum), a full redesign of the Location card layout plus a real orbit relationship in the canon import: (1) The old "Location card" and "World Profile (UWP)" cards are gone, replaced by two new cards with every field re-sorted by theme — World Profile (UWP): Hex/Star System/Zone on one row, then World Size, Atmosphere, Biome, Hydrographics, Gas Giant; World Demographics: Starport, Bases, a visual divider, Tech Level + Law Level on one row, Trade Codes, Economy, Population, Government. Biome and Economy (renamed from "Development level," same underlying developmentLevel field/Trade-pricing behavior, label-only change) both moved card with zero storage changes. (2) A self-referencing star (its Star System field points at itself) now hides the entire World Demographics card, plus World Size/Atmosphere/Biome/Hydrographics/Gas Giant within World Profile — widened from the previous follow-up\'s narrower per-field hide list to match the new card boundaries. (3) domain/hostileLocations.js\'s importHostileLocations() gained a final pass linking every world to its star as a real relationship (addRelationship(...,\'orbits\',\'located_at\')) after all three catalogs (bases/stars/worlds) land — idempotent and unconditional, so re-running import after a later zone always backfills any missing link. (4) The star name is no longer duplicated as a tag on the world (hostile-canon/zone/locationKind remain) — the relationship is now the single source of truth for which star a world orbits. Verified via an updated domain test (asserts the tag is gone AND the relationship exists both directions, 334 total) plus a jsdom smoke test confirming the Hex/Star System/Zone row, World Demographics\' Tech Level+Law Level row and divider, the Economy relabel, and both hiding rules (World Demographics absent, five World Profile fields hidden) for a self-referencing star.',
    '2026-07-08 HOSTILE Canon Locations second follow-up (docs/adr/0026 addendum), on direct request to make Star System/Bases self-consistent within the canon import itself rather than needing hand-linking after the fact: (1) data/hostileLocations.js gained two new catalogs — HOSTILE_BASES (USSC/JASDF/DRW/MRA, tagged #base) and HOSTILE_STARS (one per world, tagged #star, each self-referencing its own name via starSystem) — and domain/hostileLocations.js\'s importHostileLocations() now imports Bases, then Stars, then the 30 worlds, in that order, so every world\'s bases/starSystem values resolve to real entities immediately (one star/world name collision, Tau Ceti, disambiguated to "Tau Ceti System"; a new domain test asserts the whole catalog is collision-free). (2) Every world gained a #planet/#orbit/#deepspace tag from a new locationKind field (orbit = an explicit moon of a gas giant/brown dwarf, deepspace = an unclaimed asteroid, planet = everything else — 24/2/4 split across the 30). (3) World Profile field order changed to Hex, Star System, Zone, Tech Level (was Hex/Zone/TechLevel then Star System further down); Hex is now size=4/maxlength=4. (4) A world\'s tags now include both its zone and its star (previously zone only). (5) Bases now works exactly like Trade Codes — a dropdown-add plus removable chips (new addLocationBase/removeLocationBase, entities.js, mirroring addLocationTradeCode/removeLocationTradeCode) — but sourced from #base-tagged entities rather than a static table, replacing the old comma-input. (6) The "(#star)"/"(#base)" label hints now render lowercase (fieldLabelRow gained an optional hint param + a new .field-label-hint{text-transform:none} rule) instead of being shouted uppercase by .field-label\'s own styling. (7) A Location whose Star System self-references (e.name === e.starSystem — true only for a HOSTILE_STARS entity) now hides every planet-only World Profile field (Starport through gas giant, including Bases/Trade Codes), showing only Hex/Star System/Zone/Tech Level plus an explanatory note. Verified via 3 new/rewritten domain tests (334 total: bases-then-stars-then-worlds import order and content, the self-reference check, the collision-free cross-reference check) plus a jsdom smoke test covering field order, Hex maxlength, Earth\'s Bases chips (4) and Star System selection, The Sun\'s self-reference hiding every planet field while keeping Hex/Star System/Zone/Tech Level, and the lowercase hint spans.',
    '2026-07-08 HOSTILE Canon Locations follow-up, three direct refinements (docs/adr/0026 addendum): (1) Star System is now a `<select>` sourced from existing Location entities tagged `#star` instead of free text — the row label reads "Star System (#star)" so a GM knows where the options come from; a world\'s star is modeled by linking to another Location a GM creates and tags, not a descriptive string (the canon import\'s own starSystem text, e.g. "Wolf 359 (M6V Red Dwarf)", won\'t retroactively match until a GM creates a matching #star Location — a known tradeoff of the request, not a bug). (2) Trade Codes became a dropdown-add (options show plain labels, no `#` prefix) plus removable chips, replacing the comma-separated input — two new domain functions, `addLocationTradeCode`/`removeLocationTradeCode` (entities.js), mirror `addFactionAsset`/`removeFactionAsset` exactly. Bases stayed a comma input (not part of this request). (3) Both the Location card and World Profile card are now collapsible sections, collapsed by default (`ui.expandedLocationCard`/`ui.expandedWorldProfile`, ephemeral Sets — same shape as `enhancementsSection`\'s `ui.expandedEnhancements`), so a Cast list full of imported worlds doesn\'t default to a wall of open cards. Verified via 1 new domain test (332 total) plus a jsdom smoke test covering both cards\' collapsed/expanded rendering, the #star-tagged Location appearing as a Star System option, the Trade Codes dropdown/chip round-trip, and a direct addLocationTradeCode/removeLocationTradeCode check.',
    '2026-07-08 HOSTILE Canon Locations (docs/adr/0026-hostile-canon-locations.md, extending ADR 0013/0025), on direct request to build "a detailed and comprehensive entities database of all worlds and destinations" from the Hostile Settings PDF, adding any missing attribute fields to the Location template along the way. Close reading of assets/docs/Hostile setting.pdf (321 pages) turned up a full Cepheus Engine/Traveller-style gazetteer: 108 named worlds/stations, each with a UWP (Universal World Profile) stat line and, for most, a real "Planetology"/"Development" write-up. Locations gained a new, independent "World Profile" card: 13 optional fields (hex, zone, starport, world size, atmosphere, hydrographics, population, government, law level, tech level, bases, trade codes, gas giant, star system — entities.js\'s new ensureWorldProfileFields, wired into the existing ensureLocationFields), decoded against a new data/hostileUwpTables.js reference (Starport Classes/World Sizes/Atmospheres/Hydrographics/Populations/Governments/Law Levels/Bases/Trade Codes, transcribed from the sourcebook\'s own digit-meaning tables, pp.38-49). A new "🌍 Import HOSTILE Canon Locations" Settings button (gated to the Hostile genre pack) bulk-creates real, fully-editable Location entities from data/hostileLocations.js via domain/hostileLocations.js\'s importHostileLocations() — idempotent (dedup by name via the existing findByName()), so re-running it after a later zone is added only creates what\'s new, and a GM\'s own edits to an already-imported world are never overwritten. This pass ships the Near Earth Zone in full: all 30 worlds (Abyss through The Solomons), each with a condensed, GM-scannable summary paraphrased from the sourcebook\'s own prose (not verbatim) and a real page citation; Fomalhaut Settlement Zone and Capella Extraction Zone/New Concessions Zone are queued next, tracked as a living checklist at the top of the data file — a future pass only needs to append entries, no further code changes. bases/tradeCodes (genuinely multi-value) are edited as a plain comma-separated input, needing one small addition to shell.js\'s existing generic data-entity-field handler (CSV-to-array, plus its first checkbox-backed field for gasGiant) rather than a new chip-editor subsystem. Verified via 4 new domain tests (331 total: World Profile field defaults, import creating all 30 with correct fields/tags/overview, re-import idempotency including a GM-edit-survives check, and partial-skip when one name already exists) plus a jsdom smoke test confirming the World Profile card renders/persists through the real inspector, the Settings legend and genre-pack gating both render correctly, and a second import run creates zero duplicates.',
    '2026-07-08 Location Development Level + Biome, and Trade "smart exchange rates" (docs/adr/0025-location-biome-trade.md, extending ADR 0013), on direct request: "Entities of Location type need additional attributes... such as biome or level of development... the Trade tab should include a functionality that applies smart exchange rates suitable to [either]." ADR 0013 already had a development-level-shaped mechanic (tag-matched ECONOMY_TYPES biasing priceAt()) but no dedicated UI for it at all; Development Level is now a real dropdown field on every Location (entities.js\'s ensureLocationFields), with the old tag-scan kept as an automatic fallback so an already-tagged Location prices exactly as before. Biome is a genuinely new, independent axis (data/biomes.js — 7/6/6 curated entries per genre pack, each with per-resource-type scarcity dials) that compounds with, rather than replaces, Development Level: a Waterworld that\'s also tagged Industrial prices Water cheap AND manufactured goods cheap, computed independently (trade.js\'s new developmentLevelBiasAt()/biomeBiasAt(), multiplied together in priceAt()). Commodities gained a finer resourceType (water/fuel/food/ore/tech/luxury, data/commodities.js) alongside their existing raw/manufactured category, giving biome something distinct to bias against. A new "Location card" section in the entity inspector (locationSection(), mirroring factionSection\'s shape) needed zero new event-handling code — the existing generic data-entity-field change handler already patches any field. Settings\' Trade Economy Model section gained a matching biome reference list. Verified via 4 new domain tests (327 total) covering field defaults, the field-preferred/tag-fallback split, biome bias matching/unmatched cases, and priceAt combining both biases, plus a jsdom smoke test confirming the dropdowns render/persist through the real inspector and the Settings reference list renders — caught and fixed one real bug in the process: an unescaped apostrophe inside a single-quoted template string in the new Settings help text broke `src/ui/drawers/index.js`\'s module syntax outright (missed by `npm test`, since the domain test suite never imports UI files — only the smoke test\'s direct import of drawers/index.js surfaced it).',
    '2026-07-08 Follow-up fixes: (1) The doc viewer now reclaims the main drawer\'s width when it\'s collapsed (the tab strip\'s ⌄ arrow from the previous batch) — `sidePanelInsetPx` (shell.js) drops the main drawer from its `--viewer-overlap` calculation while `drawerCollapsed` is true, so a PDF actually expands to cover the freed space instead of leaving a dead gap; an anchored side panel (independent of the main drawer\'s collapse state) still counts. (2) Confirmed, not changed: New Campaign already lived only in the Settings drawer (removed from the gear quick-menu in the prior batch) and mention clicks in WHO/WHERE/WHAT context fields already opened the entity editor / jumped the PDF viewer to the right page — both verified directly against the running app rather than assumed, since a status doc had understandably raised the question. (3) **Real bug fixed**: deleting the active entity while Cast was filtered by type/tag/search fell back to the first entity in the WHOLE campaign (`removeEntity`, domain/entities.js — it has no idea Cast might be filtered, by design, since the domain layer stays UI-state-free) instead of the first one still visible in that filtered view — e.g. deleting an NPC while Cast was filtered to "npc" could silently land the inspector on an unrelated Location. A new pure `filterEntities(campaign, {types, search, tags})` (entities.js) — the Cast drawer\'s own filter logic, pulled out so it has exactly one implementation — lets the `data-entity-del` handler compute the correct next-active id from Cast\'s current filter before deleting, overriding `removeEntity`\'s whole-campaign fallback only when a better candidate exists; the Cast drawer itself was refactored to call the same function instead of keeping a second copy of the filter logic. Verified via 2 new domain tests for `filterEntities` (323 total) and a 9-check jsdom smoke test covering the delete-while-filtered fix, the viewer-overlap shrinking on collapse (with an anchored panel still correctly counted) and restoring correctly, and a separate no-anchor case confirming the overlap reaches a full 0px.',
    '2026-07-08 UX batch, eight items: (1) Gallery gained its own "+ Upload" (previously the only way in was through an entity\'s "+ Photo") — selecting a file resizes it immediately (same client-side pipeline) but opens an inline friendly-name form (preview + editable name pre-filled from the filename, ui/titleCase.js) before the actual addGalleryImages commit, an inline form rather than a popup per the standing rule. (2) The Battlemap\'s background upload now auto-tags itself `battlemap` (locked, same convention an entity photo\'s type tag already uses), and its "pick existing image" `<select>` filters to only battlemap-tagged images instead of every Gallery thumbnail. (3) The header\'s Party/Colony/Journal buttons now always show their text label (matching the right-edge nav\'s own icon+label buttons, which never hid theirs) — the narrow-viewport rule that hid just the label text is gone; `.header-actions` wraps instead of overflowing if that\'s ever tight on a real phone. (4) "New Campaign" now lives only inside the Settings drawer, not the ⚙ gear quick-menu (which now just has Settings/About) — a destructive-adjacent action one level deeper on purpose. (5) The gear button moved to the far right of the header, past Party/Colony/Journal, and keeps its icon-only look (no label, unlike its neighbors). (6) WHO\'s NPC/Faction list is now the same tag-listbox -> candidate-panel -> click-to-mention picker WHERE already had (`whoEntityPicker`, sharing `entity-tag-picker` CSS — renamed from `where-picker` now that it\'s not WHERE-specific) — NPC and Faction tags pooled together; "+ New NPC"/"+ New Faction" still create blank entities the same as before. (7) An NPC tagged `#character` (a Party member, per the Party tab\'s own filter) no longer shows the "🎲 Deepen" button or the "Revealed / hidden (GM)" section — both are for an antagonist/bystander NPC the GM is inventing or keeping secrets about, neither applies the same way to a player-controlled character. (8) The drawer tab strip (visible once 2+ tabs are open) gained a collapse arrow that hides the whole main drawer panel — keeping every tab/scroll/filter state intact — behind a small floating ☰ icon; clicking that icon brings the exact same drawer back. Also: Enhancement types split "Wetware / Bio-Genetics" into two separate options (Wetware, Bio-Genetics) and renamed "Gene-Modification" to "Mutation" (label-only — the stored id is unchanged, so any already-installed enhancement of that type keeps resolving correctly). Verified via two jsdom smoke-test scripts (40 checks total: header layout, New Campaign placement, enhancement dropdown contents, Deepen/Revealed gating before and after tagging #character, the WHO picker\'s mention-insert, the full collapse/restore cycle preserving open tabs, and the full Gallery-upload-to-battlemap-background-tag-filter flow with a stubbed canvas/Image since jsdom has neither) plus the full `npm test` suite (321/321, unaffected — this batch is UI-only, no domain/schema changes).',
    '2026-07-08 Planetfall Grid Battlemap (docs/adr/0023-planetfall-grid-battlemap.md) — the first of Phase 11\'s four remaining tactical-tools items to get built, after four rounds of scoping questions settled what the original one-line ask left open. A new Battlemap drawer: named maps (multiple per campaign, switchable), each with an optional background sourced through the Gallery (upload — reusing ui/imageResize.js\'s loadAndMaybeResize, the same pipeline entity photos already use, just a new call site with no entityId — or pick an existing image), an optional toggleable grid overlay (CSS-only repeating-linear-gradient, adjustable cell size, never snaps placement), a small built-in annotation icon palette (hazard/door/cover/terminal/crate/ladder/wreckage/extraction/alarm/objective/note — data/battlemapIcons.js, since no real Planetfall art exists in this repo), and freeform combatant tokens dragged in from Cast (art from that entity\'s own Gallery thumbnail, or a plain initial fallback). `domain/battlemaps.js` is pure CRUD mirroring threads.js\'s exact clone/ensure/mutate shape, covered by 14 new domain tests. Placement is native HTML5 drag-and-drop, not a new input model: a fourth custom MIME type (application/x-gmatlas-battlemap-icon, alongside the existing entity/document/guide-node types) lets an already-placed icon be dragged to a new position on the same canvas, while dragging a Cast entity onto the canvas creates a NEW token instead of a mention/relationship — completeBattlemapDrop() is the one function that decides which, following the exact "one place decides what a drop means" shape completeDrop/completeGuideNodeDrop already established. Placing a built-in annotation icon uses a simpler click-to-arm-then-click-to-place gesture instead of a third drag flow (arm from the palette, then tap the canvas) — more touch-friendly, no drag gesture required. A token\'s plain click opens its entity (data-open-entity, the same convention every other entity reference in this app uses); an annotation\'s plain click opens the standard inline prompt (docs/adr/0022) to edit its note, shown as a plain `title` hover tooltip exactly as the original ask specified — a small ✕ badge (checked BEFORE the generic data-open-entity handler, since it\'s a descendant of that same div) removes an icon without opening/editing it. Map naming also goes through the standard inline prompt — no window.prompt() anywhere in this feature. Verified via 31 jsdom-driven smoke-test checks across two scripts (map creation, grid toggle, annotation placement/note-editing, token drag-in/reposition/click-vs-remove, and the existing-image background picker) plus the full `npm test` suite (321/321, 14 new). Deliberately out of scope here, left to later still-unscoped Phase 11 items: combat/turn tracking (Encounter Manager), snap-to-grid (Interactive Maps, which this feature\'s toggleable grid overlay was designed to be reused by), hex grids, distance measurement.',
    '2026-07-08 Link toolbar button now asks for a description too, not just the URL: the inline prompt (docs/adr/0022) gained optional multi-field support — a caller passing a flat label/placeholder/value (nine of the ten kinds) still gets exactly the one-field behavior it always had, but the Link button now passes an explicit two-item `fields` array (description, then URL) instead. Submitting inserts `[description](url)` as a clean, literal replacement of whatever was selected (a new insertExtLinkMarkup, distinct from wrapSelectionWithMarkup\'s "wrap the existing selection" behavior, since the GM-typed description may not match the original selection at all, or there may have been no selection to begin with — previously, clicking Link with nothing selected produced an empty `[](url)` needing a second manual edit to add visible text). Any text already selected before clicking Link still pre-fills the description field as a convenience default (editable, not committed until submit) — matches the "select text, then link it" gesture other editors use, without requiring a selection at all. Enter submits from either field, not just the first. Verified via three targeted jsdom checks (two fields render/submit correctly including Enter-from-the-second-field, the rendered link shows the description as its visible text with the raw url only in href/data-ext-link, and a real selection correctly pre-fills the description field) plus a regression check that every single-field kind (Set Objective, etc.) is unaffected, and the full `npm test` suite (307/307, unaffected — domain layer untouched).',
    '2026-07-07 A single standard for inline data entry, replacing window.prompt() everywhere (docs/adr/0022-inline-prompt-standard.md), on direct request: "as a general rule, do not have popup windows for data entry... create a standard approach and assure all similar inputs are done the same way" — specifically called out via WHY\'s "Set Objective," which (like WHO\'s "Introduce NPC") used to blindly window.prompt() for its text. One generic mechanism now covers every remaining case: openInlinePrompt(kind, opts)/commitInlinePrompt()/closeInlinePrompt() (ui/shell.js) show one small floating field — label, input, ✓, ✕ — positioned via getBoundingClientRect() next to whatever triggered it (the same anchor-to-trigger technique the @-mention suggestion popup already used), added once to the static shell skeleton. Nine window.prompt() call sites converted, each a three-line "open it with a kind+meta" at the trigger plus one branch in commitInlinePrompt\'s switch: Introduce NPC/Set Objective (data-shift-prompt), a statblock\'s + Field/+ Track, a custom ruleset name, WHY\'s + Thread/+ Expedition, the rich-text toolbar\'s Link button (added this same session, before this rule was stated — needed wrapSelectionWithMarkup to accept an explicit captured Range, since by the time the GM submits a URL, live window.getSelection() no longer points at the field), and a document mention\'s page number (three sites sharing one applyMentionPage() — Ctrl/Cmd+Click an existing mention anchors to it directly; a fresh drag-drop or @-autocomplete document pick now inserts the mention PAGELESS immediately and opens the page prompt as an optional follow-up anchored to the just-inserted span, instead of blocking insertion on an answer first — insertMentionNode now returns the span it created to make this possible). Deliberately no close-on-blur (the input\'s own ✓/✕ buttons would race a blur-before-click) — only Escape/✕/✓ close it. window.confirm() (delete confirmations) is unaffected — a different interaction (yes/no, not a value) the request didn\'t name. Verified via two jsdom-driven smoke tests (17 + 6 checks) covering all four structurally distinct shapes: a plain named-shift prompt with real Focus-field commit, Enter/Escape/✓-click all correctly submitting or cancelling, the deferred-range Link flow, and the statblock add-field flow with a live group-index meta.',
    '2026-07-07 External links in rich-text fields (Phase 11 backlog item, `docs/adr/next-request.md`: "textboxes [can] have external links that open into a new browser tab. The link cannot have any additions using the ?"), the one Phase 11 backlog item small/independent enough to build without the shared canvas-primitive research the three tactical-tools items (Battlemap/Base Builder/Encounter Manager/Interactive Maps) still need first. Extends ADR 0018\'s existing "plain text is the source of truth, richly rendered" markup (bold/italic/underline/small/large) with one more: `[label](url)`, parsed by `domain/documents.js`\'s `parseInlineNodes` (a new `sanitizeExternalLinkUrl()` there validates/normalizes every url — auto-prepends `https://` to a bare domain, **strips a `?` query string and everything after it entirely** per the explicit security ask, and rejects anything that isn\'t a real http(s) URL once normalized, which is also what keeps a `javascript:`/`data:` scheme from ever reaching a rendered `href`; a link whose url fails this check renders as literal bracket text, never a broken link). A new 🔗 toolbar button (`ui/mentionEditor.js`\'s `richToolbarHTML`) prompts for a URL and wraps the current selection the same way Bold/Italic already do (`wrapSelectionWithMarkup(field, "[", "](" + url + ")")`  — no new insertion mechanism). Renders as a real `<a class="ext-link" data-ext-link="..." target="_blank" rel="noopener noreferrer">` (`renderInlineNodes`) — `rel="noopener noreferrer"` always paired with `target="_blank"` here, since this is the one place this app renders a user-authored href; `serializeMentionEditor` reads it back to the same `[label](url)` text. Clicking it needed its own delegated-click branch (`data-ext-link`, shell.js) rather than relying on native anchor behavior, matching why `.mention-link` navigation already needed one: a plain click on an `<a>` living inside a contenteditable region only places the caret by default, in every engine this app targets. Verified via 8 new `npm test` cases (`sanitizeExternalLinkUrl`\'s normalize/strip/reject behavior, `parseInlineNodes` producing a link node with nested-bold support and rejecting an unsafe scheme) plus three angles of manual/scripted verification the domain suite can\'t reach: a jsdom-driven toolbar-insertion test (query string stripped, unsafe scheme produces no markup at all, and — dropping in the same output a real commit would render — clicking a real `<a class="ext-link">` calls `window.open` with the sanitized url, not native navigation) and two direct calls to `buildMentionEditorHTML`/`serializeMentionEditor` confirming the exact render/round-trip strings (jsdom itself doesn\'t implement `isContentEditable`, so the full blur-commit-then-rerender cycle couldn\'t be driven end-to-end through a simulated DOM — this is a known jsdom gap, not an app bug, and is why the render/serialize halves were verified directly instead).',
    '2026-07-07 Latest Scene readability follow-up: the 7 scene fields are now `<textarea rows="1">` (not `<input>`) in a single column instead of a multi-column grid — a scene field is often a full sentence or two, and one legible column reads far better than several cramped side by side. Each grows with its content on typing (`autoGrowSceneField`, `ui/shell.js`\'s new `input` handler branch, plus a one-time sizing pass after every render for fields that already hold a lot of text) up to a CSS-capped ~4 rows, then scrolls internally rather than growing forever. The text/fields split moved from 60/40 (grid) to 70/30 (flex), and the text panel now has a real `resize: horizontal` handle — dragging it sets the panel\'s own width and the fields panel (flex:1) fills whatever\'s left, so the GM can adjust the split by hand, not just live with the fixed ratio; the narrow-viewport stacked layout drops the manual width and switches to `resize: vertical` instead, since a horizontal drag handle means nothing once the two panels are stacked. Verified via a jsdom-driven structural smoke test (11 checks: 7 real `<textarea>` fields, auto-grow runs without throwing, the stylesheet carries the single-column/4-row-cap/70-30-flex/resize rules) plus `npm test` (299/299, unaffected — this batch is UI-only). jsdom has no real layout engine, so the resize handle\'s actual drag behavior and the auto-grow height in a live window weren\'t visually confirmed here — only the DOM/CSS structure that drives them.',
    '2026-07-07 WHAT-tab tracker layout + Latest Scene 60/40 split, on direct follow-up: (1) The WHAT tab\'s two tracker rows (Intent/Threat/Mystery, Resources/Reputation/Stress) were a fixed 3-column grid that jumped straight to 1 column at the 1023px breakpoint — `.field-row`/`.field-row-3col` (styles/cockpit.css) now use `repeat(auto-fit, minmax(10rem, 1fr))`, so they land at 3/2/1 per row as the window narrows instead of only ever being 3 or 1. (2) Latest Scene\'s combined text and its split fields were stacked vertically, wasting horizontal space on wide screens — a new `.last-scene-body` grid (`ui/workspace/index.js`\'s `lastScene()`) puts the read-heavy text at 60% width and the edit-heavy fields at 40%, side by side, collapsing back to stacked below 1023px. (3) **Real gap found**: two of the combined text\'s sections — "Decision point" and "Current thread" — were never editable at all. "Decision point" was a single fixed sentence hardcoded into `recomposeSceneText()` (domain/scenes.js), identical on every scene, no field behind it; promoted to a real `decisionPoint` field like opening/driver/clue/complication, with its old wording surviving only as that field\'s default value at generation time. "Current thread" was already a real field (`situationLine`, a frozen snapshot of the WHAT tab\'s Situation text taken at scene-generation time) but had no UI control at all — the GM could see it in the read-only text but had no way to correct or update it after the fact; it\'s now the seventh field in the Latest Scene grid. All 7 sections a GM sees in Latest Scene\'s text are now backed by a real, editable field — none are silently un-editable narrative-only text anymore. Verified via a jsdom-driven smoke test against the built bundle (9 checks: both tracker grids present, the 60/40 split renders text-then-fields in that order, all 7 scene fields present including the two new ones, Decision point\'s edited value reflows into the combined text) plus the full `npm test` suite (299/299, two new scene tests added).',
    '2026-07-07 Eight-item follow-up batch submitted directly in chat (not docs/adr/next-request.md), three of them real regressions found and fixed: (1) Latest Scene\'s split fields gained a real `.scene-fields` grid (repeat(auto-fit, minmax(9rem, 1fr))) — they had never had one, so five fields rendered one-per-line despite being individually editable since the prior batch. (2) Scene\'s "Opening" field now stores the FULL generated sentence (domain/scenes.js), not just the sensory-detail fragment the previous split left editable inside a fixed template — recomposeSceneText() simplified to interpolate it directly; "Likely consequence" (already a real field on the scene object) is now rendered as a UI field too. (3) WHERE tab: the "Present Here" curated list (context.where.entityIds) was removed as duplicative of Focus — clicking a matching Location candidate now inserts a real @mention at the end of Focus directly (same insertMentionNode path drag-and-drop mentions already use) instead of adding to a separate list; addContextEntity/removeContextEntity stay in session.js (harmless, tested) but are no longer driven from this UI. (4) The rich-text toolbar\'s small/large-text buttons now show a small "a" and bold "A" instead of plain "S"/"L". (5) **Real bug**: a `@media (max-width: 1023px) { .header-actions .btn.sm { display: none; } }` rule — written back when "▶ Scene" was the only such button — was blanket-hiding Party/Colony/Journal and the ⚙ gear menu on any viewport ≤1023px wide once the QoL batch moved them into that same bucket. Fixed by wrapping each header tab\'s text in a `.btn-label` span and hiding only that at narrow widths, leaving a compact icon-only button that stays clickable. (6) Confirmed "move New Campaign into the Settings dropdown" was already true (it only ever lived in the ⚙ menu and the Settings drawer) — likely reported while bug 5 made the gear button itself invisible. (7) **Real bug**: the anchor panel (`.mc-drawer-anchor`) only ever had a ▶ "merge into tabs" button, no ✕ — closing it when it was the ONLY thing open silently promoted it into the main drawer instead (which alone had a ✕), forcing a confusing second click. Added a real `data-drawer-anchor-close` ✕ that closes it outright; `unanchorDrawerTab()` left unchanged. (8) **Real bug**: the doc-viewer iframe reload guard compared the live DOM `frame.src` (browser-normalized absolute URL) against a locally-computed relative path for Reference Library docs — these could never match, so the guard misfired on EVERY render (any unrelated store.update() anywhere in the app could reset the iframe to blank mid-load). Fixed by tracking `lastDocViewerSrc` — "the last src I set" — instead of reading it back from the DOM. Verified end to end via a jsdom-driven smoke test against the built bundle (17 checks: scene grid/fields, WHERE mention insertion, toolbar glyphs, header button structure, anchor-close-in-one-click, all passing) plus the full `npm test` suite (297/297).',
    '2026-07-07 Gallery (docs/adr/0021-gallery.md), Phase 11\'s first built item — a new top-level drawer: any entity can carry a thumbnail (left-aligned beside Type/Tags), plus a searchable, taggable image collection browsable on its own. An upload ≤256px stores as one Gallery record; a larger one auto-resizes into a linked thumbnail+original pair (both auto-tagged with the entity\'s type, that tag locked from removal, both visible in the Gallery grid — thumbnails circular, originals rectangular). `domain/gallery.js` is a third copy of `entities.js`\'s plain-tags-array + vocabulary pattern (already proven once for Documents\' own tags) — confirmed during research that Oracle\'s own tag/lock mechanism (`isOracleTagLocked` etc.) is hardcoded to oracle-table paths and not a generic mechanism this could plug into instead. `ui/imageResize.js` is this app\'s first-ever pixel-manipulation code — a plain `<canvas>`-based client-side resize (no new dependency), mirroring the `ui/`-vs-`domain/` split `ui/mechanicsScan.js`/`ui/tocScan.js` already established for their own browser-only work. Storing both a full-resolution original and a resized thumbnail is safe specifically because of ADR 0015\'s IndexedDB migration (~3.2GB observed headroom) — would have been a real risk under the old ~5-10MB localStorage ceiling that migration replaced. Verified end to end in a real browser: a small upload creates exactly one record, a large one creates a correctly cross-linked pair, the locked entity-type tag genuinely cannot be removed, and deleting an image from the Gallery drawer clears a dangling entity thumbnail reference instead of leaving it dangling.',
    '2026-07-07 "USER CHANGES" QoL batch (docs/adr/next-request.md), five independent UI edits: (1) The Graph drawer gained a filter input that highlights/dims matching nodes by name (never removes them, since the force-directed layout depends on the whole node set to stay stable) instead of having no way to locate an entity in a busy cluster. (2) Party/Colony/Journal moved from the edge nav (EDGE_ORDER) to their own small tab group in the header, right-aligned (HEADER_ORDER, reusing the identical data-drawer-open routing — confirmed generic, no edge-specific wiring needed). Found and fixed a real pre-existing bug along the way: the edge nav\'s own "+ Type" quick-add buttons (data-entity-add, used by WHO/WHY) had no click handler at all until the earlier WHERE-tab batch fixed it — unrelated confirmation this pattern works. (3) 12 always-visible instructional `<p class="dim small">` tips (Guide\'s intro, Documents\' two intros, eight Settings groups, the Graph toolbar tip) became collapsed-by-default "?" icons next to their section\'s title (new helpToggle/helpBody helpers, drawers/index.js) — the HOW tab\'s transient lens-picker tip was deliberately left alone, since it has no adjacent header to attach an icon to and already only shows while that action is mid-flight. (4) The old click-the-campaign-title-to-open-Settings convention became a real ⚙ gear button with a dropdown (New Campaign/Settings/About) — About is genuinely new content (an overlay showing the real BUILD version); New Campaign\'s confirm-then-store.newCampaign() logic was extracted into one shared confirmNewCampaign() function instead of living only inside the Settings drawer. Found and fixed a real z-index bug during verification: the new dropdown and the main drawer both had z-index:20, so an open drawer painted on top and silently ate clicks meant for the menu — bumped the menu to 25. (5) The header\'s "▶ Scene" button was removed entirely and replaced with two copies: one to the right of "What Happens Next?" in the WHAT tab (alongside the tab\'s own pre-existing "▶ Continue Story," both call the same action, per the literal request), and one in the Co-Pilot panel\'s Quick Apply row, unconditionally present regardless of whether that panel\'s own dynamic "Advance Time" suggestion happens to be showing that turn. Also found and fixed a second real bug during verification: a newly-added `headExtraForDrawer()` case referenced `helpToggle` without importing it into shell.js, silently breaking the Guide and Documents drawers\' entire body render (a thrown ReferenceError inside render(), caught by this app\'s existing guardedHandler wrapper, which is exactly why this was caught only by an actual browser check, not the build step).',
    '2026-07-06 "USER CHANGES" batch (docs/adr/0019-where-tab-and-scene-fields.md, docs/adr/0020-reference-toc-generation.md, ADR 0018 addendum): six related follow-ups to the rich-text work above. (1) The five text fields ADR 0018 missed (entity Revealed/hidden, Faction Scenario seed/Agenda, Colony\'s textarea fields, a Document note\'s content box) all gained the same toolbar; two needed the identical <label>-><div> fix Overview already got. (2) Journal entries gained an edit icon (domain/session.js\'s new editNote) — auto-saves on blur like every other field. (3) The WHERE tab: "every Location in the campaign" is replaced by a Location-tag listbox (not chips, per direct request) filtering a candidate panel, adding to a new curated "present here" list (context.where.entityIds — a schema field that existed since Phase 3A but was dead in the UI until now; new addContextEntity/removeContextEntity mutators). Found and fixed a real pre-existing bug along the way: WHO/WHY\'s own "+ Type" buttons had never had a click handler at all. (4) Verified (no code needed) that an externally-typed @[docname#page] already becomes a real link once it matches a library title. (5) Reference Library Table of Contents generation: a new Settings button plus an upload-time (confirmed via window.confirm) scan reads a PDF\'s real bookmarks (PDF.js\'s getOutline(), never called anywhere in this app before) and writes a linked table of contents per document into the Guide, under a "Table of Contents" entry (domain/toc.js, ui/tocScan.js) — same file:// restriction as the Mechanics Index scan it mirrors. (6) The rich-text toolbar gained Tab-indent, small/large text (~text~/^large^), and a table markup type (a toolbar-inserted GFM-style pipe-table skeleton, rendered left-aligned with thin borders by default; no per-row/column UI, per explicit choice). Also, per direct user testing/clarification: Latest Scene\'s sensory/driver/clue/complication became real, individually 🔮-linked fields (domain/scenes.js\'s recomposeSceneText keeps the combined text view a live, one-directional derivation of them, not a second editable copy).',
    '2026-07-06 Lightweight rich text + mention page-editing (docs/adr/0018-lightweight-rich-text.md), on direct user request: Journal, Guide, WHAT\'s Situation, WHO/WHERE/WHY/HOW\'s Focus, and NPC/Faction/etc.\'s "Overview (shared)" all gained a small B/I/U/bullet-list/numbered-list toolbar that inserts lightweight `**bold**`/`*italic*`/`_underline_`/`- `/`1. ` markup (domain/documents.js\'s new parseTextBlocks/parseInlineNodes, rendered by ui/mentionEditor.js into real <b>/<i>/<u>/<ul>/<ol> — same "plain text is the source of truth, richly rendered" shape @mentions already used, not live execCommand). Overview converts from a plain <textarea> to a real mention-editor (its first-ever @mention support). Found and fixed a real bug while wiring the toolbar into Overview: a <label> containing both the new toolbar\'s <button>s (labelable) and the mention-editor <div> (not labelable) made the browser forward every click on the div to the label\'s first labelable descendant instead — Chromium\'s built-in "click label text, focus its control" behavior misfiring once the actual intended target stopped being a real form control, silently redirecting every click meant for the field to the Bold button (or, for Overview specifically, its 🔮 oracle-link icon) instead, making the field un-clickable-into. Fixed by dropping the wrapping `<label>` for the three affected fields (WHAT Situation, the shared WHO/WHERE/WHY/HOW Focus field, and Overview) in favor of a plain `<div class="field-label">` — same CSS, no label semantics were actually relied on. Also fixed, per direct user clarification mid-implementation: the mention tooltip\'s wording was backwards and non-functional (claimed "Ctrl/Cmd+Click to open — click to edit," but a plain click always navigated and there was no edit gesture at all) — Ctrl/Cmd+Click on a document mention now really does edit its page (`ui/shell.js`\'s `editMentionPage`), and the tooltip (`mentionEditor.js`\'s `mentionTitle`) describes the real behavior. `docs/guide-content/5pfh-campaign-turn-sequence.txt` holds the fully cross-referenced, correctly-renumbered 5PFH Campaign Turn Sequence content ready to paste into a new Guide document.',
    '2026-07-06 Fixed a document-viewer bug found while investigating a user report: two @[Label|Name#Page] mentions of the same document at different pages already correctly carried their own distinct page (the per-mention data was never actually wrong), but clicking a second mention to an already-open doc left the viewer on the first page — reassigning an iframe\'s src to a URL differing only by its #page=N fragment isn\'t reliably treated as a real navigation by a browser\'s built-in PDF viewer. Now forces a real reload (blank, then the real src) whenever the resolved src changes.',
    '2026-07-06 Multi-document Guide tree with drag-and-drop reparenting (docs/adr/0017-multi-doc-guide-tree.md): the Guide tab grows from one freeform field into named, nested documents (campaign.guide: {docs, activeId}) organized in a tree below the main editor, reorganizable via drag-and-drop on both mouse and touch (extending the existing entity-linking touch-drag system, not a second implementation). Create/rename/delete (cascading, confirmed first)/reparent, plus a "Top level" drop zone to un-parent. domain/guide.js\'s ensureGuide() migrates an old single-field campaign losslessly into the first root doc. Found and fixed a real bug before it shipped: a naive random bootstrap id for the migration-created root doc would let a pure read (before any write) disagree with what a real write\'s migration later generates — fixed with a deterministic fixed id both paths share. .guide-editor also gained a resize handle (its scrollbar was already there via overflow-y:auto, just not obviously so).',
    '2026-07-06 Oracle tags + entity-field "jump to relevant Oracle" links (docs/adr/0016-oracle-tags-and-field-links.md): a small reusable tag vocabulary (character/secret/setting/leadership/hook/agenda/fear/faction/trade/discovery) seeded onto real oracle tables (data/oracleTagSeeds.js), same seed-then-campaign-override shape campaign.oracles.overrides already used for edited table content. 13 entity fields across all 6 entity types (Faction\'s hq/leadership/scenarioSeed/agenda/fear/need/secret, plus the shared Overview/Revealed pair) gained a right-aligned 🔮 icon (data/entityFieldOracleLinks.js) that jumps to the Oracle drawer pre-filtered to every table carrying the field\'s linked tag(s), with a "Filtered by: ..." clear-badge. Oracle drawer table rows gained a 🏷 icon (right-aligned alongside the existing ✎/🎲) revealing a hidden-by-default tag editor; any tag a field link depends on is locked (shown 🔒, not removable) so a GM can\'t silently break the link. Caught and fixed a real bug during verification: openDrawerTab() never re-renders itself (every other call site piggybacked on a subsequent store.update()\'s notify()) — the new field-link jump has no campaign mutation to piggyback on, so it needed its own explicit render() call.',
    '2026-07-06 Persistence moved from localStorage to IndexedDB (docs/adr/0015-indexeddb-persistence.md), on direct user request after a real save failure: picking a Suggestion Lens chip threw a localStorage quota-exceeded error (a campaign with a few embedded uploaded documents can reach the ~5-10MB per-origin ceiling). Evaluated and rejected a full Postgres backend as disproportionate — the problem is a storage ceiling, not a missing network layer, and a server would break "double-click index.html, works offline." IndexedDB keeps the exact same local-only architecture with roughly 1000x the headroom (~3.2GB observed), verified directly under both file:// and http:// before committing to it. store.js stays the only persistence module; store.get()/store.update() keep their exact synchronous call shape everywhere (~100 call sites in ui/shell.js untouched) via an optimistic-update-then-background-persist design, with a new store.onPersistError() channel replacing the synchronous throw for the rare failure case. Only store.import()/restoreBackup()/newCampaign() became real async functions, since those already had explicit result handling. Existing localStorage campaigns are migrated in losslessly on first load and left untouched afterward (never re-written) — verified with a real legacy campaign seeded into a fresh browser profile. Also shipped as an immediate stopgap alongside this: the Documents drawer now shows each uploaded file\'s estimated size, so a GM can spot and remove/relocate a large one without waiting on the storage migration.',
    '2026-07-06 docs/adr/0009-situation-engine-revisited.md built (all three Decision items): Diplomacy Engine fields — fear/need/secret on the Faction card, alongside hq/leadership/agenda. Expedition trackers — domain/expeditions.js models an expedition as a Thread tagged kind: \'expedition\' (its own clock is the Progress dial) gaining three additional 0-10 dials (Supplies/Exposure/Morale, neutral midpoint 5) instead of folding into a lifecycle status, per the user\'s explicit correction to ADR 0008\'s original alternative; a "+ Expedition" button and compact 3-slider block live in the WHY workspace\'s Threads list, and copilot.js surfaces an observation once Supplies <=2 or Exposure >=8. Suggestion Lenses — "Continue Story" and "What Happens Next?" were wired to the exact same handler despite separate labels; What Happens Next? now opens a small chip picker (data/suggestionLenses.js\'s Discovery Lens and Approach Lens lists, a random draw of 4) instead of generating immediately, and picking one calls the new suggestNextWithLens() — continueStory\'s own generateScene(), just handed that lens\'s mapped real Oracle categories so its Driver line pulls different content than the generic Plot Engine > Scene Driver a lens-less Continue Story still uses unchanged.',
    '2026-07-06 next-request.md batch: Cybernetics renamed to "Enhancements" (domain/enhancements.js, tolerant legacy read of old `cyberware` data) with a per-item type dropdown (Cybernetics/Wetware-Bio-Genetics/Psionics/Gene-Modification, always shown), collapsed by default, moved under the statblock section, and its 🎲 roll now lands in the add-form\'s name field (overwritten by each reroll, not journaled) instead of a toast+Journal entry, until "Install" commits it. deepenNpc\'s Want/Complication now append to Revealed/hidden (GM) instead of Overview; Revealed/hidden is collapsed by default and persistently stays expanded (entity.revealedOpen) once a GM opens it. Changing an entity\'s Type now confirms via window.confirm before applying. Cast search matches entity type too. Fixed: Oracle search couldn\'t find "Creature Concept" (a composite-generator label, not a literal table name) — GROUP_ALIASES in data/oracleGroups.js. Trade Economy Types (docs/adr/0013): a Location tag can now name an economy type (data/economyTypes.js — Hostile-native or "(Traveller-style)" models, one active at a time via settings.tradeEconomyModel) biasing domain/trade.js\'s priceAt() via scarcity/manufacturing dials instead of a literal tech level; switching models never breaks an already-tagged Location. Game Mechanics Index (docs/adr/0014): a real PDF.js-backed scan (vendored assets/vendor/pdfjs/, one explicit exception to the zero-dependency policy) links game-mechanic terms to their page in the Reference Library from a new Settings "🔄 Refresh Mechanics Index" button, surfaced as clickable links in the Guide drawer — found and handled a real file:// limitation (Chromium blocks reading a local PDF\'s bytes over file://; this one feature needs `npm run serve`) and fixed a real scripts/build.js bundler gap (`export async function` wasn\'t recognized).',
    '2026-07-06 SWN content deepened, CWN cybernetics borrowed, on direct user request after the real Stars Without Number Revised Deluxe and Cities Without Number Deluxe PDFs were added to assets/docs/ (docs/adr/0011-swn-cwn-content.md, extending docs/adr/0010 rather than replacing it — still original content/phrasing throughout, not a transcription of either book\'s actual text/tables). Faction creation: force/cunning/wealth stats (0-10, entities.js) plus a growing Assets list, with a "Faction Asset" oracle table (Corporate Powers group) to roll-and-append one. Turn-based mini-game: domain/factions.js\'s resolveFactionTurn() resolves a faction\'s turn as d10 + its acting stat (Attack/force, Scheme/cunning, Expand/wealth, weighted toward whichever stat is already strongest) vs. a flat difficulty — a strong success raises that stat, a setback adds an extra Pressure Track tick — wired into both the existing bulk "Advance Faction Turns" action and a new per-faction "▶ Turn" button on the Faction card. Deepening NPCs: domain/session.js\'s deepenNpc() rolls new Stereotype/Want/Complication oracle tables onto an EXISTING NPC\'s Overview via a "🎲 Deepen" button in Entity Detail, distinct from generateNpc\'s from-scratch chain. Styling creatures/places/adventure seeds: domain/worldbuilding.js adds three small combinatorial generators — Creature Concept (a new Xenobestiary oracle group: Origin/Method/Trait/Threat), Site Concept (Feature/Danger/Wonder), and Adventure Seed (Hook/Twist, reusing the existing Story Complication table for its third beat) — each with its own Journal drawer roll button. CWN\'s best-known subsystem, cyberware costing Strain against a limited capacity, landed as a new domain/cybernetics.js module (installCyberware/removeCyberware/isOverStrained) and a Cybernetics section in the NPC inspector, with an "Augmentation" oracle group for flavor ideas — deliberately not a new RULES_PROVIDERS entry, since CWN was never one of the six systems the original Rules Constitution roster named.',
    '2026-07-06 Design roadmap reconciliation, round two: ADR 0008 (the gameplay-mechanics.md reconciliation) explicitly declined five proposals; each was put back to the user individually afterward, non-binding and suggestive, rather than treated as a final call. Documentation only, no code yet: `docs/adr/0009-situation-engine-revisited.md` records the result — Expedition Structure and Diplomacy Engine\'s per-faction fields were reversed (scoped: a new `domain/expeditions.js` giving a `kind: \'expedition\'` Thread three 0-10 dials alongside its own clock; three new Faction card fields, `fear`/`need`/`secret`); Discovery Quality and Noncombat Resolution were redirected into one new mechanism instead of a stored field and a resolution mechanic — a "Suggestion Lens" step for *What Happens Next?*, which today is wired identically to Continue Story (a real gap this also names): a lens picker (Discovery/Approach categories) would filter the next Oracle roll toward a chosen creative angle instead of generating blind. The session-composition-ratio decline was reconfirmed unchanged.',
    '2026-07-06 Traveller/SWN content, on direct user request (reverses one specific call from docs/adr/0002-rules-constitution.md — see docs/adr/0010-traveller-swn-content.md): Traveller gains a real data/rulesets.js character ruleset — STR/DEX/END/INT/EDU/SOC collapsed to this app\'s usual small-rollable-modifier abstraction (not literal 2-12 UPP scores + a separately-derived DM, which would make 2d6+value vs 8 trivially easy), one Stamina track (0-8) standing in for Traveller\'s three-stat damage system, finally giving domain/dice.js\'s rollTraveller (2d6 vs 8, unused since Phase 6) an actual sheet to belong to. No sourcebook exists in assets/docs/ for either system, so this is honestly-labeled original content, not a transcription — doc: null, and the Settings drawer\'s Stat System reference link now shows "No sourcebook in this repo\'s library..." instead of a broken link whenever a ruleset has no doc. SWN gains a new "Stars Without Number" oracle group instead (Faction Action, World Tag — original phrasing inspired by SWN\'s faction-turn and two-tag world-generation conventions, not its actual proprietary tables), reachable as a second roll button on the Faction Pressure Track\'s card alongside the existing Hostile-flavored Faction Activity roll — an ordinary oracle addition, not a genre-pack swap. rulesConstitution.js\'s status strings for both updated accordingly.',
    '2026-07-06 Faction-turn/rumor automation, scoped deliberately small (a GM-triggered bulk action, not a background scheduler or live simulation — Article II, the GM always retains creative authority): domain/factions.js\'s advanceFactionTurns() advances every faction that already has a Pressure Track by one tick and rolls a "rumor" for each from the existing Faction Activity oracle table (factions with no track are skipped — nothing to advance for one nobody\'s tracking). A new "Advance Faction Turns" button in the Journal drawer rolls a turn for every tracked faction at once and journals the results together (formatFactionTurnRumors()) — the concrete, small shape "faction-turn/rumor automation" takes here rather than the full Living World Engine the original design corpus describes.',
    '2026-07-06 Faction Pressure Track, Mission generator, and the Faction Rumor -> Mission seed link (the trio ADR 0004\'s ruleset review scoped for Phase 10): domain/factions.js models a faction\'s pressure clock as a Thread tagged kind: \'faction-pressure\' plus a factionId reference — the same "reuse Threads" pattern Contracts already established, so every existing Thread control works on one unchanged. Opt-in per faction (a "+ Pressure Track" button in the Faction card, not auto-created); a new free-text Agenda field and a "Faction Activity" oracle table (Corporate Powers group) round it out. domain/missions.js\'s generateMission() returns payout/deadline/complication/penalty scaled by context.what.threat (higher ambient danger = higher stakes, tighter deadline, automatically) and rolls its complication from the existing Story Complication table; a new "Generate Mission" button in the Journal drawer rolls one straight into a note. copilot.js\'s advise() now surfaces a faction whose pressure track is >=75% full as a mission seed ("a mission tied to them would land naturally now"), ranked just below a hot ordinary thread. Building this surfaced and fixed a real bug: the hot-thread check read campaign.threads unfiltered, so a near-full Contract or Faction Pressure Track (both stored there too) got flagged with the wrong generic thread phrasing instead of their own subsystem\'s message — now excludes anything carrying a kind tag.',
    '2026-07-06 Dice roll window follow-up: repositioned to the bottom-right corner (like a toast, but persistent until dismissed) instead of a centered, backdrop-dimmed modal — the rest of the app stays reachable while a roll result is up, since there\'s no more full-screen backdrop blocking clicks. New "add to Journal" button (an open-book SVG icon, not the 📖 emoji — unreliable glyph coverage here too, same fix as the die-face icons) pastes the same plain-text breakdown the copy button produces, prefixed with the roll\'s label, as a Journal note — distinct from the terse one-line entry every roll already logs automatically.',
    '2026-07-06 Anchor displacement, Cast joins the drawer system, and a graphical dice roll window: (1) Anchoring a second drawer no longer discards whichever one was already anchored — it\'s restored to the tab group first (anchorDrawerTab), so nothing gets silently closed just because something else took the anchor slot. (2) Cast is now a real drawer (its own DRAWERS entry, renderDrawer case, tab strip anchor/close icons) instead of a bespoke separate popout with its own CSS/skeleton/responsive rules — toggleCastDrawer keeps its one special behavior (opens into the anchor slot by default, since a draggable entity list is most useful beside whichever drawer IS active) but once open it behaves exactly like any other drawer, including "move back into the tab group." Its "Generate…" dropdown moved from a hardcoded popout header into a generic .drawer-head-actions/data-drawer-head-extra slot both the main drawer and the anchor panel now support. (3) Statblock rolls open a real dice roll window instead of a plain toast: the action die (or flat/Traveller die) and the field\'s value added up on one row (an SVG pip-die icon, not a Unicode ⚀-⚅ glyph — unreliable font/platform glyph coverage), what it\'s measured against on another (a diamond icon for Starforged\'s 2d10 challenge, a target icon for a flat/Traveller target number), and a big color-coded outcome banner (green/amber/red for strong hit/weak hit/miss, green/red for success/fail). A ⧉ button copies the result as plain text (domain/dice.js\'s new formatRollCopyText/formatFlatRollCopyText/formatTravellerRollCopyText) in the exact tab-indented "Action: .../Challenge: .../OUTCOME" shape requested; still logs to the Journal exactly as before.',
    '2026-07-05 Anchored drawers, genre-aware Bestiary/LifeForm terminology, and Contract journaling: (1) Any drawer tab can now be pulled out of the tab stack and pinned to its own side panel immediately left of the main drawer (the tab strip\'s new ⇤ icon; the anchor panel\'s own ▶ moves it back) — rolling from the Oracle drawer itself now auto-anchors Journal to Oracle\'s left, so the entry the roll just logged is visible without switching tabs, without stealing the anchor slot if something else already occupies it. (2) "Bestiary" (a fantasy-genre term) now reads "LifeForm" for the Hostile/Cyberpunk genre packs, switching back to "Bestiary" only for the Fantasy pack (data/genrePacks.js\'s new bestiaryTerm()) — the Settings template editor, the entity inspector\'s statblock group header, the "+ Add a statblock" chip, and the Party roster\'s per-group caption all read it live off settings.genrePack; the underlying mechanism (data/statblockTemplates.js\'s NPC field manifests) is unchanged, this only ever swaps the label. (3) Creating a contract (domain/trade.js\'s createContract, so both the "+ Contract" form and "Generate" get it for free) now logs a Journal entry with its type/patron/route/payout resolved to entity names, the same way generateNpc()/rollOracle() already record what they created.',
    '2026-07-05 Phase 10 begins — Merchant Rules Lens (ADR 0003/0004): a new Trade drawer (between Colony and Docs) adds a per-Location commodity market (data/commodities.js + domain/trade.js\'s priceAt() — supply/demand dials drive price, buying/selling nudges the local dial so two Locations\' prices never agree), the party\'s shared cargo manifest, and a Contract board. A contract is a Thread with a few extra reference fields (kind: \'contract\', type, patronId/originId/destinationId, payout) rather than a new state machine, so its clock/lifecycle/priority controls are the exact same ones every other Thread already has. "Generate" rolls a new Contract Type oracle table (Trade & Cargo group, ADR 0004\'s 15-type taxonomy) and prices the payout from the real gap between two Locations\' markets when a route is picked, falling back to a flat default otherwise; "+ Contract" opens an inline name/type/patron/route/payout form, not a window.prompt() popup. Cargo capacity rides the existing Vehicle Bestiary template as one more field. Everything ADR 0004 explicitly deferred (ships/crew depth, faction politics, the rest of the 19-generator list, GM-less/VTT modes) stays deferred.',
    '2026-07-05 Stress/Tension — the reopened Phase 6 candidate: a fifth context.what narrative dial (drawn from Hostile\'s horror-design essay, Setting pp.211-219 — uncertainty/isolation/timing as the three horror levers), same shape as Resources/Reputation (0-10, neutral-midpoint-5 default so no old save suddenly reads as already-cracking). New Raise Stress/Ease Stress Shift actions and a third slider on the WHAT view (field-row-3col alongside Resources/Reputation). The Co-Pilot now reacts at both ends: high Stress (>=7) surfaces as an observation/consequence ranked just below Threat ("Stress is high — a scene without combat should follow, or someone breaks" / "Someone cracks under the pressure..."), with a suggested Horror Escalation -> Escalation Beat oracle roll and an Ease Stress quick-action; very low Stress (<=2) offers a calm-holds opportunity, mirroring Resources/Reputation\'s abundance case.',
    '2026-07-05 Three follow-up fixes to the same day\'s UI polish pass: (1) Party roster cards are now full drawer width (was a wrapping row of ~13rem-cap cards) — a full ruleset\'s stat row usually needs no horizontal scrollbar anymore. (2) Party Tracker rows no longer name their own kind ("Meter"/"Counter") — the format (boxes vs. a +/- number) already says which one it is; a Starforged counter\'s difficulty rank still shows (nothing else reveals it), just without the redundant kind word glued in front. (3) The graph layout\'s centering pull was raised from 0.012 to 10 — at the wider ideal-edge-length from earlier the same day, that pull was so weak relative to repulsion that nodes drifted to the pad boundary and stayed there regardless of graph size ("not evenly spaced... pushed to the border"); the new value was tuned empirically (a standalone sweep across 2-50 node graphs) to keep the overwhelming majority of nodes off the border while still spreading them across most of the canvas.',
    '2026-07-05 UI polish pass across Cast/Entity Detail/Graph/Party: (1) Cast\'s row of per-type "+ Add" chips is now one small "Generate…" dropdown docked next to the panel\'s own close ✕ (folding in the old separate "🎲 Generate NPC" oracle-roll button as one of its options), which let the search box move up to sit directly under the Cast header instead of below a whole button row. (2) The Entity Detail tag editor\'s "add tag" input now shares a row with the TAGS caption (right-aligned) instead of stacking below it. (3) The relationship graph\'s force layout uses a larger ideal-edge-length constant (was tuned tight enough that a busy graph\'s lines crossed each other illegibly) — while retuning it, found and fixed a real pre-existing bug: nodes were only clamped to the layout box once, at the very end, so a busy graph\'s outer nodes could overshoot by different amounts each pass and then collapse onto the exact same boundary corner when finally clamped, silently stacking distinct entities on top of each other; clamping every iteration instead means nodes bounce off the wall and settle at genuinely distinct positions. (4) Party roster cards now show one row per statblock GROUP (with a small ruleset/template caption above each), not every group\'s fields flattened together — a character carrying two rulesets\' sheets now reads as two distinguishable rows instead of one undifferentiated pile of badges. (5) Party Trackers: "+ Tracker" opens a small inline name/type form (a text field, not a window.prompt() popup); a tracker\'s kind is fixed forever once created (no more per-tracker kind <select>); a Meter asks its box count at creation ("usually 5 or 10 in Starforged") and renders as click-to-set progress boxes instead of a "value/max" ratio; a Counter offers a Starforged progress-track difficulty rank (Troublesome..Epic) when the campaign\'s stat ruleset is Starforged, stepping by that rank\'s tick count (out of a 40-tick track) instead of a plain +1.',
    '2026-07-05 Four oracle-content additions from the design-roadmap reconciliation\'s unblocked-follow-ups list (data only, no new domain code): "Salvage Investigation" (Derelicts group gains What Happened/What Remains/Still Changing) turns a derelict from a one-note flavor roll into a small mystery chain — what wrecked it, what\'s left behind, what\'s still active and dangerous about it right now. "Site Survey" (Exploration group gains What\'s Normal/What\'s Strange/What\'s Dangerous/What\'s Valuable/What\'s Beautiful) is deliberately discovery-first — only one of its five tables is about danger, the rest are about what a survey actually finds. "Cargo Interest" (Trade & Cargo) answers who unexpectedly wants a given shipment and why. "Anomaly Investigation" (Mysteries & Coverups gains Observation/Hypothesis/Contradiction/Discovery) gives an oracle-driven shape to investigating something strange, the same way Coverup Move already gave one to investigating a lie. All four are ordinary tables rolled in the Oracle drawer like the app\'s other 100+ tables — no new UI, no new reducer, just data alongside each group\'s existing tables.',
    '2026-07-05 Fixed a real all-caps rendering bug + two smaller cleanups: WHO/WHERE/WHAT/WHY/HOW\'s mention-editor fields inherited text-transform: uppercase from their <label class="field-label"> wrapper (that rule is meant for the label\'s own small caption text, e.g. "SITUATION") — with no override, it shouted the field\'s actual content too, entity/doc mention text included, while Journal (wrapped in .drawer-note, no such rule) and Guide (no wrapper) were unaffected, matching exactly what was reported ("caps in most textfields, but Guide is fine"). Fixed with one explicit text-transform: none on .mention-editor. Also: a relationship\'s strength/weight control (and the Bond-tracker display it can stand in for) no longer renders at all unless BOTH sides are an NPC or a Faction — a Location, some Lore, or an Asset doesn\'t have an evolving "how strong is this" dial, so showing an editable number there meant nothing. And: the Guide editor autosaves on blur now (no Save button — nothing else in the app uses one; every other field already commits on blur/change).',
    '2026-07-05 Edge nav reorder + drag-drop/doc-link fixes: edge nav top-down order is now Guide, Journal, Oracle, Party, Cast, Colony, Docs, Graph, Co-Pilot, Settings (Cast/Co-Pilot are special-cased non-drawer buttons, not part of DRAWERS, so this needed one combined EDGE_ORDER list instead of DRAWERS.map() + always-appended extras). Dragging an entity from Cast onto Entity Detail (anywhere in the editor, not just the Relationships section) now adds it as a relationship, the same addRelationship() a Cast-row-to-Cast-row drop already used — the whole .entity-inspector is a drop target now, not just other Cast rows. Root-caused why a Guide/Journal doc mention link sometimes refused to open the PDF viewer ("Text notes open inline below — not a PDF file"): linkDocumentMentions never checked the Reference Library before deciding a mentioned title didn\'t exist yet, so it could create a phantom same-titled TEXT NOTE that permanently shadowed the real Reference Library PDF for every future mention of that title (findDocumentTabByTitle checked the uploaded library first, unconditionally). Fixed both ends: linkDocumentMentions now skips Reference Library matches too, and findDocumentTabByTitle now prefers whichever match is actually openable over a same-titled non-PDF note, which also self-heals any campaign where the phantom note already exists. Also: uploaded file titles are no longer the raw filename verbatim (extension included) — titleCase.js (new, shared with the Reference Library\'s own build-time title generation) strips the extension/hyphens and proper-cases ALL-CAPS segments, so an upload named "HOSTILE-SHORTS-999-Test.pdf" shows as "Hostile Shorts 999 Test" instead of shouting inline wherever it\'s mentioned.',
    '2026-07-05 Design roadmap reconciliation: evaluated `requirements/design-principles/gameplay-mechanics.md` (a campaign-structure/mechanics research transcript) against this repo (`docs/adr/0008-situation-engine.md`). Documentation only, no code: most of what it proposed — a "GM Prompt Hierarchy," an "Oracle Prompt Chain" technique, "Campaign Momentum" — turned out to already be this repo\'s WHAT card + Shift Story reducers, generateNpc\'s multi-table roll, and Session Recap + Co-Pilot, respectively, arrived at independently; now named and cross-referenced instead of re-treated as gaps. Four small oracle-content additions (Salvage Investigation, Site Survey, Cargo Interest, Anomaly Investigation) were scoped as unblocked, low-effort follow-ups (see `DESIGN-NEW-FUNCTIONALITY.md`\'s new unphased content section); an Expedition four-dial tracker, structured Diplomacy fields, a Discovery-classification field, a Noncombat taxonomy, and a mechanized session-composition budget were all explicitly declined, with reasons, in the ADR.',
    '2026-07-05 Cast/Entity Detail split: Cast is no longer a drawer tab — it\'s a Co-Pilot-style independent panel (toggled from its own edge button, same list — search, type filter, add-entity buttons, draggable/clickable rows — the drawer always rendered) that stays open alongside whichever drawer IS active, so an entity can be dragged into Journal or Guide while the Cast list stays visible instead of competing with them for the one active-drawer slot (this supersedes the separate 📌 entity-popout added the day before — same need, now solved by promoting Cast itself rather than a second parallel panel). The name/tags/overview/statblocks/relationships form Cast used to show inline moved to its own "Entity Detail" tab — it has no edge nav button at all, and only ever opens by clicking an entity somewhere (a Cast row, a mention link, a relationship chip, a graph node, a WHO/WHERE chip, a search result, ...), never picked from the tab list directly. Both panels get the same phone (bottom sheet) / tablet (edge slide-in) responsive treatment Co-Pilot already had, so Cast staying reachable on a phone didn\'t regress now that it has no drawer-tab fallback.',
    '2026-07-05 Relationship type validity + Starforged Bond tracker: Rival Of/Allied With/Bond are social relationship types that only make sense between people and factions (an Asset or Location can\'t have a rivalry) — extended the existing "flag, don\'t delete" pattern (already used for Member Of/Owns/Controls/Located At\'s directional target-type checks) to these three as well, checking BOTH sides now (entities.js\'s new RELATIONSHIP_TYPE_MUTUAL), not just the target. A Bond\'s progress no longer lives on the relationship object\'s generic 0-10 strength field at all — the moment a relationship becomes bond-typed (between an NPC/Faction pair), if the source entity has a Starforged character sheet, a real "Bond: <Name>" progress track is auto-created there (entities.js\'s ensureBondTrack), matching how the rulebook actually presents Bonds (pp.163-166/233) instead of inventing a second progress-track shape. The relationship chip shows that track\'s current value read-only (not a second editable copy of the same number) with a "view in Character Sheet" jump button that expands the group if collapsed and scrolls to it. Also added: a "✕ close all" button in the drawer tab strip\'s corner (separate from a single tab\'s own ✕, which only ever closed that one tab), and Reference Library titles generated from ALL-CAPS filenames (e.g. "HOSTILE SHORTS 001GhostShip") are now proper-cased at build time ("Hostile Shorts 001GhostShip") since those titles are exactly what a @[Title] mention shows as link text — a short allowlist (5PFH, GM, NPC, PDF, SWN) keeps real acronyms from being mangled.',
    '2026-07-05 Mention click behavior + a real auto-create bug: reverted yesterday\'s Ctrl/Cmd+Click requirement for opening a mention — a single click now opens the entity (and focuses+selects its name field in the Cast inspector) or the document (PDF viewer) directly, matching Notion/Google Docs\' convention rather than Word\'s; editing a mention\'s label is done by arrow-keying the cursor into it like any other inline text, not by clicking it (clicking is unambiguously "go there" now). Root-caused a real reported bug along the way: asking a document mention for a page (@[Title#12], from the "which page?" prompt) was silently creating a new NPC entity named literally "Title#12" instead of linking the document — entities.js\'s parseMentions/linkMentions had never stripped a page anchor (only a @[Label|...] prefix), so a page-anchored mention\'s "name" was the whole "Title#12" string, which never matches an existing entity and so always got auto-created as one. Fixed the stripping, and added a second, deeper guard: linkMentions now takes a `skip` set of names already known to resolve to a real document (documents.js\'s new resolvedDocumentMentionNames) — without it, a mention of an existing document that ISN\'T also an uploaded library entry (e.g. anything in the Reference Library) would still have gotten auto-created as a phantom entity, page anchor or not, since linkMentions has no way to know the document library exists at all.',
    '2026-07-05 @mentions are now inline hyperlinks in the text itself, not badges below it: Journal, Guide, and WHO/WHERE/WHAT/WHY/HOW are now contenteditable (ui/mentionEditor.js — buildMentionEditorHTML/serializeMentionEditor), not <textarea>, so a mention renders as real inline text living exactly where it was typed, matching how a hyperlink sits inline in a web page or an MS Word document. Editing its display label is just editing text — click into the mention and type, the same convention Word itself uses for an editable hyperlink; the underlying target is a fixed data attribute on the span, never touched by editing the visible label (confirmed: precisely editing a label leaves data-open-entity pointing at the same entity). Since editing-in-place needed a real distinction between "editing the label" and "following the link," navigating is now Ctrl/Cmd+Click — a plain click falls through to the browser\'s normal caret placement instead (this is also gated to only inline .mention-link spans; every other data-open-entity/data-doc-open use — WHO/WHERE chips, Party cards, relationship chips, the graph — still opens on a plain click as before). Drag-and-drop and the @-suggestion popup now insert a real mention element directly (mentionEditor.js\'s insertMentionNode via the Selection/Range API) instead of raw @[...] text waiting for a future re-render to become a link. This fully supersedes the previous day\'s "chip below the textbox with a ✎ button" mechanism — removed the now-dead relabelJournalMention/relabelGuideMention/relabelContextMention/relabelMention functions and their UI wiring along with it, rather than leaving two mention-editing paths half-working side by side.',
    '2026-07-04 @-mention autocomplete, entity popout, whole-row drag, clickable relationships: typing "@" plus a few characters of a name in Journal, the Guide, or any WHO/WHERE/WHAT/WHY/HOW field now pops up a filtered list of matching entities/documents (Up/Down/Enter/Escape or click) that inserts a finished @Name/@[Name] mention in place — for a document, asking which page first, same as the drag-and-drop path. Every one of those fields also now renders its @mentions as clickable badges (not just Journal/Guide, which already got this in an earlier pass this same day) — a real reported case, @[AR-5 "Prospector"], is now clickable end-to-end confirmed with an actual quote-containing entity name. New: an Entity popout (📌 Entities, next to the Co-Pilot toggle) — a draggable-only Cast quick-list docked beside whichever drawer is open, so an entity can be dragged into Journal or Guide while the Cast list stays visible instead of replacing it (only one drawer body is ever in the DOM at a time, so this was otherwise impossible); desktop-only, since phone/tablet already have the autocomplete and touch drag-and-drop for the same need. Also: the Guide editor was never actually wired into the drag-and-drop drop-target list at all (despite its own placeholder text describing @mentions) — entities and documents can now be dropped there same as Journal. Cast list rows are draggable from anywhere on the row again, not just the small ⠿ handle (reverted from an earlier click-vs-drag-conflict fix, accepting the known tradeoff — real mouse jitter can occasionally start a drag instead of a click — in exchange for "grab it anywhere" being more discoverable). And: an entity\'s relationship chips now have a clickable name that jumps to that entity, instead of being plain text next to the type/label/strength controls.',
    '2026-07-04 Graph zoom/pan + two more fixes: the relationship graph (SVG, fixed 600x520 layout space) had no way to zoom in — a campaign with a lot of entities/links just got visually cramped with no recourse. Added scroll-wheel zoom (centered on the cursor), drag-to-pan, and ＋/－/⟲ toolbar buttons, all manipulating the rendered SVG\'s viewBox directly rather than a full re-render (buildGraph/computeLayout, a force-directed layout, has no reason to re-run just because the view into it moved) — clicking a node still opens its Cast inspector as before, unaffected by the new pan handler. Also: the Documents drawer\'s rename ✎ button only opened the inline input — clicking it again while already editing (instead of blurring/tabbing away) silently did nothing, which read as "doesn\'t save"; it now saves-and-closes on that second click (and shows 💾 instead of ✎ while open, as a hint). And: dragging a Reference Library entry into a note or context field inserted a raw `file:///.../index.html#` string instead of an @mention — Reference Library rows were never wired into the custom drag-and-drop system (missing draggable/data-drag-document), so the browser fell back to its native default drag behavior for the link. Fixed by giving them the same drag wiring as uploaded documents (keyed by the existing "ref:<path>" tab-key format, resolved via resolveDocumentTab) — dropping one now inserts a real, clickable @[Title] mention like an uploaded document always did.',
    '2026-07-04 Clickable/editable @mentions + Cast drawer cleanup: @mentions in Journal entries and the Guide now render as one clickable badge each — data-open-entity for an entity match (jumps to its Cast inspector), data-doc-open for a document match (opens the PDF viewer, to the mentioned page), plain/non-clickable if neither resolves — instead of entity mentions rendering only their stat badges (no click-to-open at all) while the Guide\'s own mentions were static, non-clickable spans. New syntax `@[Label|Target]` (documents.js\'s splitLabel/relabelMention, entities.js\'s parseMentionRefs) lets a mention\'s displayed text diverge from what it resolves to — "the topic of the page" for a document anchor, or a narrative nickname for an entity — auto-/link-create still keys off the real target, never the label. Every badge gets a small ✎ that swaps it for an inline rename input (relabelJournalMention/relabelGuideMention persist the rewritten @[...] text) instead of a window.prompt() popup. Also: the Cast drawer\'s entity-list collapse toggle used to be a second "Cast — N entities ▾" row directly under the drawer\'s own "Cast" title — redundant, two labels for one thing. The drawer title itself is now the toggle (collapsed by default every time the tab is (re)opened, and now hides the add-entity buttons too, not just the search/filter/list below them), and the Cast type-filter chips (All/NPC/Location/...) are alphabetized by label instead of ENTITY_TYPES\' declaration order.',
    '2026-07-04 Tag UX unification + Documents cleanup: the entity tag editor (Cast drawer) used an older pattern — a `<select>` dropdown plus a separate "+ New…" window.prompt() button — while the Documents drawer\'s tag editor (from the previous fix pass) used a single input+datalist that just commits on its own. The doc version is now the one tag-entry design everywhere: entity tags dropped the select/prompt pair for the same auto-committing input (data-entity-tag-input), sharing its datalist-suggestion-or-type-new-then-blur/Enter behavior and even its CSS class. Documents drawer: removed the "Add note" button and its window.prompt() flow entirely (uploading a file is still there; existing note-kind documents from before this change still display and save normally, just can\'t be created fresh anymore) — the "No documents match…" placeholder that referenced the now-gone note feature is gone too (an empty list is just empty). The tag-filter chip row is now collapsed by default behind a "▸ Tags (N)" toggle instead of always showing every tag in the campaign at once.',
    '2026-07-04 Documents/mentions fix pass: root-caused "the @ link to docs is not working" and "entities showing up as documents" to the same bug — bare @Name and bracketed @[Name] mentions share identical syntax (brackets only exist so a multi-word name survives the parser), so linkDocumentMentions (documents.js) couldn\'t tell an entity mention from a document one and auto-created a same-named phantom "document" (empty note body) for EVERY entity mentioned in a Journal note or context field; documentBadges then rendered a dead, non-clickable 📄 badge for that same name right next to the entity\'s real attribute badge, which is what read as a broken link. Fixed at both ends: linkDocumentMentions now skips names that already resolve to an entity, and documentBadges skips names that resolve to an entity (entities get exactly one badge — their own — not two). Also, while in this area: Reference Library entries now respect the Documents drawer\'s search box and tag filters (previously only the uploaded/note library did); gained a delete (✕) icon — since they\'re build artifacts from assets/docs/, this hides the entry via a persisted per-campaign override rather than touching the file, with the same confirm-dialog pattern as deleting an uploaded document; and switched their internal tab-key scheme from a positional array index to the manifest\'s stable file path (a latent bug the new search/hide filtering would otherwise have exposed — a filtered list\'s Nth item is not the same as the manifest\'s Nth item). The rename (✎) button on both uploaded docs and Reference Library entries now opens an inline textbox in place of the title instead of a window.prompt() popup. Tag entry (both libraries) no longer needs a separate "+" click: picking a tag from the datalist or typing a new one and tabbing/Enter-ing away both commit immediately (mirrors how the entity tag dropdown already worked).',
    '2026-07-04 Cast/statblock UX pass: investigated a "drag-and-drop is not working" report and confirmed the actual behavior (dragging from a Cast row\'s body correctly does nothing — only the ⠿ grip drags, a deliberate earlier fix for a click-vs-drag conflict) is by design, not a regression; made the grip visually more discoverable (bigger hit target, hover background/border) since it\'s easy to miss coming from the old whole-row-draggable behavior. Each statblock group (Bestiary/Character Sheet/Vehicle) in the entity inspector is now independently collapsible via a ▾/▸ toggle in its header (ui/shell.js\'s collapsedStatblockGroups, keyed per entity+group so collapsing one doesn\'t affect others) — the Cast list itself already had this from an earlier fix; this extends it to the statblock sections within an entity\'s inspector. Character-sheet stat rows (Edge/Heart/Iron/Shadow/Wits, Reaction/Speed/Combat/Savvy/Tough) are narrower and no longer wrap to a second line — a full 5-stat ruleset now reads as one row in the drawer, with horizontal scroll as the fallback if a custom ruleset ever has more. Follow-up: the Party tab\'s member-card stat badges (.attr-badge — a separate, older read-only-summary style shared with Journal entity-mention badges, not the character-sheet rows above) were still full-size and the card itself had no width cap, so 5+ badges just kept the card growing wider instead of wrapping; narrowed .attr-badge to match and capped .party-member-card at 13rem so badges wrap into tidy rows instead.',
    'Phase 9 complete — genre packs: data/genrePacks.js registers three selectable oracle table sets (Hostile sci-fi, the pre-existing default; Cyberpunk/Shadowrun; Fantasy/D&D-style — the two new ones are original content in this project\'s own voice), switchable from a new Settings → "Genre Pack" dropdown (settings.genrePack, defaults to \'hostile\' so every existing campaign is unaffected). domain/oracles.js\'s tablesWithOverrides() now takes a genrePackId and every caller threads campaign.settings.genrePack through — Continue Story, oracle rolls, Generate NPC, Universal Search, and the Oracle drawer\'s tree/entry editor all respect the active pack. The two new packs deliberately reuse the exact category names (Characters, Location Themes, Plot Engine, Miscellaneous, Trade & Cargo) that the Co-Pilot\'s suggested-oracle logic and Generate NPC\'s NPC chain reference by hardcoded path, so those features work unchanged no matter which pack is active — this is a data swap, not a new mechanism. Verified in a browser: switching packs changes Generate NPC\'s name pool, the Oracle drawer\'s content, scene generation, and search results, with zero effect on anything else.',
    '2026-07-04 reliability + UX pass, run after a full regression check confirmed Phase 9\'s Activity picker and everything before it: (1) Settings shows campaign storage size + a "Restore last backup" button (store.js\'s storageInfo()/restoreBackup() — the one-slot backup from ADR 0005 had no restore UI until now). (2) The Pages deploy workflow gained a post-deploy smoke check that fetches the live site and verifies the bundle actually resolves, plus (added the same day) a check that a sample LFS-tracked PDF resolves to real content, not a pointer file. (3) The Reference Library\'s PDFs (~445MB) moved to Git LFS, including a history rewrite (backed by a mirror clone, confirmed via a fresh clone afterward) — caught and fixed a real regression this surfaced (actions/checkout needs lfs:true or LFS files deploy as pointer text). (4) Drawers are now tabbed: multiple can stay pinned open at once (openDrawers/activeDrawer replacing the old single openDrawer), with a tab strip that only appears once 2+ are open. (5) Three real responsive tiers instead of one: tablet (768-1023px) now gets a Co-Pilot side panel that slides in from the edge like a drawer, distinct from phone\'s (<768px) bottom sheet. (6) Two keyboard shortcuts: Escape (close whatever\'s topmost) and Ctrl/Cmd+K (open Universal Search). (7) Touch drag-and-drop: a gesture recognizer for entity linking and @mention-by-drag now works on real touch devices (HTML5 DnD never fires for touch at all), sharing the same completeDrop() logic the mouse path uses — see docs/mobile-drag-drop-test-cases.md for the manual device checklist. (8) A PWA installability audit (Chrome DevTools Protocol, not just eyeballing the manifest) came back clean — zero installability errors, service worker/offline mode both verified by actually going offline and reloading — except the app icon still read "SA" (leftover Saga Atlas branding); regenerated as "GM" in the exact same style.',
    'Phase 9 (Activity-driven gameplay) begins — the HOW workspace question gains an Activity picker (Explore/Investigate/Negotiate/Travel/Trade/Combat/Faction dealings/Downtime/Horror/World-building, new domain/activities.js) that turns data/rulesConstitution.js\'s Rules Constitution from a read-only Settings reference into an actual recommender: picking an Activity looks up its registered gameplay area and shows the Rules Lens provider(s) responsible for it (e.g. Combat -> Five Parsecs From Home), with a "Use as default" button that sets settings.statRuleset — suggestion only, never automatic, extending the existing per-entity ruleset selector rather than replacing it. Genre packs (this phase\'s other item) remain open.',
    'Root cause found and fixed (the previous build\'s toast surfaced the symptom; this fixes the real bug): store.js\'s persist() wrote a full duplicate of the previous save to a backup key BEFORE writing the real one — momentarily needing quota for two whole copies of the campaign at once. Once a campaign (typically one with large embedded document uploads) crossed roughly half of localStorage\'s quota, that backup write started failing, and because it was treated as fatal, it silently blocked EVERY subsequent save — including something as simple as clicking to select a Cast entity — even though the real save alone would still have fit. Confirmed via a live error report ("Failed to execute \'setItem\' on \'Storage\': Setting the value of \'sagaatlas.campaign.backup\' exceeded the quota") and reproduced deterministically (forced only the backup key\'s write to throw; confirmed the real save failed too, then confirmed it succeeds under the fix, including surviving a reload). The backup write is now best-effort — if it can\'t fit, it\'s skipped (logged, not thrown) and the actual campaign save proceeds; the backup has no restore UI wired to it yet, so a skipped backup costs nothing today, but a blocked real save cost everything.',
    'Fixed: any interaction that saves (store.update() — includes something as simple as clicking to select a Cast entity) writes the whole campaign document to localStorage; if that write fails (most commonly localStorage quota exceeded — real-world cause: large uploaded PDFs stored as embedded data, see store.js\'s persist()), the change was correctly rolled back but this threw with no visible feedback, so the click just looked like it did nothing. Root-caused via a deterministic repro (forced localStorage.setItem to throw, confirmed clicking a Cast row silently failed to select while filter-only interactions — pure in-memory state, no save involved — kept working, matching a real report: "filter works, select doesn\'t"). Fixed at the one choke point every interaction already passes through (ui/shell.js\'s single delegated click/change/input/dblclick listeners, rule 4): a `guarded()` wrapper now catches any handler failure and shows a toast ("Couldn\'t save — ... Storage may be full; try exporting your campaign as a backup") instead of swallowing it silently. If you\'re seeing this: export your campaign (Settings → Export Campaign JSON) as a backup, then free up space by removing large uploaded documents (or moving big rulebooks into assets/docs/ + rebuild instead, which has no storage-quota limit).',
    'Phase 8 (Unified Discovery) complete — four items: (1) Universal Search: a new 🔍 Search button in the header opens an overlay searching entities, journal, oracle tables (names and individual entries), documents (uploaded/text + Reference Library), Party trackers, and Colony fields/crew in one query (domain/search.js\'s universalSearch(), pure and DOM-free — it returns declarative targets, ui/searchPanel.js and shell.js do the navigating); clicking a result jumps straight to it (opens the entity, the document tab, or the Oracle drawer pre-filtered to the matched table). (2) Oracle table editor: each table in the Oracle drawer gets an ✎ toggle to add/edit/remove its entries and a "↺ Reset to default" once changed, built on the existing oracles.overrides mechanism (no second data path) — the drawer\'s tree also now reads through tablesWithOverrides so an edited table displays correctly, not just rolls correctly. Two new content additions shipped alongside it: a "Scenario Framing" oracle group (Dilemma/Objective/Framing NPCs/Map Feature) and an "Environmental Hazards" group (25-entry Environmental Event table + Survey Problem table), both original content. (3) Cast drawer gained a name/tag search box and entity-type filter chips above the list. (4) NPC generation oracle chain: the existing Characters oracle group (Role/Goal/Revealed Aspect/Disposition) gained a Name table to complete the chain, and a new 🎲 Generate NPC button (domain/session.js\'s generateNpc()) rolls all five into a new NPC in one click — plus a 5PFH-style Patron Benefit/Hazard/Danger Pay job-offer table set under Missions.',
    'Fixed: a Cast-list row was both the click target (select this entity) AND the drag source (draggable="true", to create a relationship) — for a real mouse, a few pixels of jitter between mousedown and mouseup is enough for the browser to start a native drag instead of firing a click, which suppressed selection entirely and made the whole Cast list feel unresponsive (reported: "clicking an entity does not make it active, dragging does not work either"). Dragging is now confined to a small ⠿ grip inside each row; the row itself is click-only (drop targets never needed draggable in the first place). The Relationships section\'s "Link" button is now icon-labeled (🔗 Link), and each relationship\'s remove control is titled "Remove link" for clarity. The Cast drawer\'s entity list can now be collapsed to just a header (▸ Cast — N entities · editing X) via a toggle, leaving the full inspector for the active entity with more room.',
    'Phase 7 (Context Graph depth) complete — three more items: (1) Typed/weighted relationships: a relationship now carries a semantic type (Linked/Member Of/Owns/Controls/Located At/Allied With/Rival Of/Bond, domain/entities.js\'s RELATIONSHIP_TYPES) alongside its free-text label, plus a 0-10 strength weight — a Starforged Bond is just a `bond`-typed relationship whose strength a GM raises over time, not a second mechanism. The requested type applies only to the side that requested it (per-side, like the existing label editor); every pre-existing relationship normalizes to linked/0 the moment any setter touches it. (2) "Flag, don\'t delete" invalid relationships (pack 9): a directional type (Member Of/Owns/Controls/Located At) implies a target entity type; when the target\'s own type is edited out from under it, the relationship chip gets a ⚠ (tooltip explains why) and the Co-Pilot gains a "Relationships to review" card — nothing is auto-corrected or removed. (3) Faction card template: faction entities gain hq/leadership/scenarioSeed fields (Hostile\'s "Agencies & Corporations" pattern, Setting pp.79-111), shown as a "Faction card" section in the inspector.',
    '`@` pointers into documents gain page anchors: `@[Doc Title#12]` or `@[Doc Title p.12]` names a page alongside the title (domain/documents.js\'s parseDocumentMentionRefs). A document badge that resolves to an openable PDF — an uploaded/library file or a Reference Library entry, whichever matches the title (findDocumentTabByTitle) — now renders as a clickable button (Journal entries, Guide) that opens the existing in-app viewer and jumps straight to that page via a `#page=N` fragment (openDocumentTab/resolveDocumentTab track a per-tab requested page); re-focusing an already-open tab without a page anchor leaves it wherever it already is. A text-note document mention still renders as a plain, non-clickable badge, same as before. Closes out the last piece of the old "@ pointers... like the ChatGPT version did" ask.',
    'Phase 7 (Context Graph depth) begins — tag fields as dropdowns: an entity\'s tags now render as removable chips plus a dropdown of tags already used by other entities of the same type (domain/entities.js\'s listTagVocabulary, computed live off existing entities — no separate stored vocabulary to drift out of sync). Selecting a listed tag adds it immediately; "+ New…" still takes a one-time freeform name via prompt, same pattern as "+ Field"/"+ Track" elsewhere. Replaces the old plain comma-separated text input.',
    'Fixed: a statblock value (or any text/tag field) typed but never blurred — then the browser was refreshed, closed, or the tab switched away — was silently lost, since every field only commits on the \'change\' event (fires on blur). Root-caused via a direct Playwright repro (typed a value into an attribute field, reloaded without tabbing away first, confirmed the edit reverted to its old value) after ruling out data-loss in the normal add-group/edit-value/refresh flow across file:// and http://, and across an actual rebuild. Fixed with one small addition in ui/shell.js\'s mountShell(): a \'beforeunload\'/\'visibilitychange\' listener blurs the currently-focused field, firing its pending \'change\' through the existing delegated handler — no new commit path, no per-element listeners added. This is what looked like "added statblocks are not retained" — the groups themselves were always saved instantly on click; it was in-progress field edits (a just-typed attribute value, an NPC\'s just-typed #character tag) that could be lost.',
    '5PFH dice roll toast now shows the breakdown, not just the combined total: "d6(2) + 0 = 2 vs target 6 → Fail" instead of "Fail — 2 vs target 6" — matching what the Journal entry already showed. `ui/shell.js`\'s `performFieldRoll` (\'flat\' branch) only — Starforged/Traveller toasts unchanged, not reported as an issue.',
    'Phase 6 complete — Narrative Trackers: two new campaign-level dials, Resources and Reputation (context.what, alongside threat/mystery — same "GM-set gauge the Co-Pilot reads" pattern, defaulting to a neutral 5/10 midpoint rather than 0 so an old save never suddenly reads as destitute or reviled). New Shift Story actions (Gain/Spend Resources, Raise/Lower Reputation) and two range sliders on the WHAT view. The Co-Pilot now reacts to both: critically low Resources surfaces a scarcity observation/consequence and points at the Trade & Cargo > Cargo Problem oracle table; soured Reputation surfaces as an observation/consequence about closed doors and refused help; abundant Resources or high Reputation open a new opportunity line (a favor, discount, or the room to be generous). This closes out Phase 6 (Campaign Continuity) — Session Recap, Thread lifecycle, and "What did I overlook?" all shipped in prior builds this phase.',
    'Attribute fields (Edge, Combat, Speed, ...) redesigned: the +/- steppers are gone in favor of a directly-editable, numerically-validated input (no min/max clamp — a modifier can go negative or past an old 1-5 scale). Three per-field display formats, editable in Settings\' Bestiary template editor: sign ("+3", Starforged-style, default), inches ("3\"", 5PFH Speed-style), plain ("3"). The field\'s LABEL is now the roll trigger (click "EDGE" to roll); a field with no roll method gets a plain, non-clickable label. Four dice models now available (none/Starforged d6-vs-2d10/5PFH d6-vs-target/new Traveller 2d6-vs-target, domain/dice.js\'s rollTraveller) — room is left to add more (5PFH Planetfall, Stars Without Number) as those systems get authored mechanics. 5PFH\'s Speed field ships as the concrete example: inches-formatted, not rollable.',
    'Phase 6 continued — Thread lifecycle: domain/threads.js threads now carry a 7-state narrative stage (Seeded/Active/Escalating/Dormant/Converging/Resolved/Archived) and a low/normal/high priority, GM-set via dropdowns on the Threads block (WHY question) — never auto-inferred, except that filling a clock still auto-marks it Resolved (unchanged prior behavior). "What did I overlook?" — a new Co-Pilot card (observation-only, never auto-correcting) surfaces threads gone quiet (Dormant, or untouched since creation), excluding ones already Resolved/Archived.',
    'Rules Constitution — requirements/initial design inputs/gameplay-goals.md sharpened "genre-aware, not genre-locked" into a concrete claim: every ruleset is a content provider, not the application. data/rulesConstitution.js records six named systems (Starforged, Traveller, Five Parsecs From Home, Hostile, Stars Without Number, Planetfall) mapped to gameplay areas, plus four responsibilities reserved for Saga Atlas itself (campaign memory, story continuity, rules switching, recommendations) — never delegated to a ruleset. Surfaced read-only in Settings; each provider honestly status-tagged (two of the six have zero authored data yet). Reference for Phase 9\'s future Activity → Rules Lens recommender, not an engine built ahead of its turn — see docs/adr/0002.',
    'Phase 6 (Campaign Continuity) begins — Session Recap ("Previously on...", domain/recap.js): one click in the Journal drawer composes what happened last time, open threads, the current objective, relevant entities, pressure, and a Co-Pilot recommendation from data already kept; "Save as Journal note" files it, excluding prior recaps from its own recap. Pure, read-only, and tested. Remaining Phase 6 items (Narrative Trackers beyond threat/mystery) are still open.',
    'Statblock polish — Bestiary is now explicitly an NPC subtype (like Character): statblock add-choices are scoped by entity type (Character Sheet/Bestiary only for NPCs, Vehicle Stats only for Assets; other entity types get no statblock options). Attribute/stat fields (Edge, Combat, ...) now render as a single signed number with +/- steppers instead of a 1-5 click-to-set meter — matching how Starforged/5PFH sheets actually present a stat; the 1-5 track remains for genuine depleting resources (Health, Hull). The "+ Add a statblock" chip row collapses behind a gear icon to save space once a few groups exist.',
    'Statblock redesign — an entity now holds an ARRAY of statblock groups (entity.statblocks) instead of one: a character can carry both a Starforged and a 5PFH sheet, or an NPC two Bestiary templates, simultaneously — adding one is always additive (a "+ Add" chip row), never a toggle that replaces another\'s data; each group is removable on its own via its own trash icon. Per-field UI simplified to label + value only — the rename input and the ★ (attribute)/Aa (track)/✕ (remove) buttons are gone from the entity view, since a field\'s name/kind is now fixed by its template (Settings owns that); ad-hoc "+ Field"/"+ Track" additions are named via a one-time prompt instead of an inline-editable label.',
    'Phase 5 audit — browser-verified every Phase 5 feature end to end (Playwright against the real bundle). Found and fixed one real bug: the in-app PDF viewer\'s close button was rendered but unclickable — the always-open Documents drawer sat at a higher z-index and physically overlapped it. The viewer now insets its right edge by the current drawer\'s width instead of extending underneath it.',
    'Phase 5 — design review pass against PROGRESS.md USER NOTES: three new drawers — Party (NPC #character roster + party-wide resource trackers), Colony (5PFH Planetfall turn sheet, entity-referencing crew roster, #lifeform encounter filter), Guide (freeform @mention/@[Doc]-linked reference document). Oracle drawer rebuilt as a grouped, collapsible, searchable tree (was a flat un-filterable list — the search box was silently broken). Bestiary statblock templates: per-game-system NPC field manifests (kind, roll method, sort order) editable in Settings, with a per-NPC template selector so a Bestiary can assign the right ruleset per creature; attribute badges now show a signed value ("EDGE +3"). Document Library: multi-file upload, tags + tag-filter + search, rename-the-label (not the file) via an edit button, name-as-link opening an in-app PDF viewer panel (not a new tab) beside the drawer. Entity relationships gained an editable free-text note per link. Fixed: a missing import that threw whenever the Journal rendered an entry with a @[Document] mention; the service worker cache-first strategy that could mask a rebuilt bundle indefinitely (now network-first); a GitHub Pages deploy of this repo having no dist/ bundle at all (added a build+deploy Actions workflow).',
    'Phase 4 — Document Library: assets/docs/ is auto-scanned at build time into a Reference Library (rulebooks/settings PDFs, always current with the folder); the docs drawer also supports real file uploads (stored as data URLs) alongside text notes. Character Sheets: a ruleset-driven statblock kind (Starforged or 5PFH, chosen in Settings > Stat system) builds a full rollable stat/resource-track sheet in one click, switchable between rulesets; an optional Crew Link link-out lives in Settings for full character-building wizards this app does not replicate.',
    'Phase 3D — statblock fields can be numeric tracks: a Crew-Link-style row of click-to-set boxes, with double-click on the value badge to roll (d6 + value vs 2 challenge dice, filed to the Journal). Health/Hull default to tracks; any field converts with the # / Aa toggle; "+ Track" adds a new one.',
    'Phase 3C — NPC/vehicle statblocks, drag-and-drop entity linking + drop-to-mention in Journal and context fields, this Build panel.',
    'Phase 3B — force-directed relationship graph over entity links, click-through to inspector, live edge-tab badges.',
    'Phase 3A — Entity Inspector + @mention auto-linking; WHO/WHERE cards populate from real entities.',
    'New — Threads (progress clocks) with Co-Pilot awareness.',
    'Phase 2 — interactive cockpit: Continue Story, Shift Story reducers, Oracle + Journal drawers, live Co-Pilot, timeline.',
    'Phase 1 — oracle roll engine, scene generation, context model ported as pure, tested modules.',
    'Phase 0 — single versioned campaign document, lossless migration from v0.53, three-tier cockpit shell.',
  ],
};
